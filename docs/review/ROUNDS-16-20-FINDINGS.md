# Rounds 16-20 — consolidated bug findings (review-only scan)

> Read-only multi-agent review. NO code changes made. Post-adversarial-verification. DB rounds 17-19 were told to evaluate the LATEST effective migration definition and NOT re-report issues already fixed this session by migs 190-195.


## Round 16 — simulator UI / hooks / PDF / routes

**raw 15 -> confirmed 13 / refuted 2  (High 0, Medium 2, Low 11)**

### Medium (2)

#### `SIM-UI-1` — PDF "generating" button label uses a missing i18n key (renders raw key path in prod)
- **File:** src/features/simulators/components/scenario-report-actions.tsx:88
- **Evidence:** Line 88 renders `{isPending ? t('generating') : t('download')}` under the `simulators.report` namespace. The key `simulators.report.generating` does NOT exist in either messages/he.json or messages/en.json (verified: report subkeys are openReport, editScenario, title, subtitle, conclusionLabel, conclusionPlaceholder, download, sendToClient, emailDialogTitle, emailDefaultSubject, emailDefaultBody, emailAttachNote, emailSent, backToScenario, backToSimulator, errors — no `generating`). No custom `getMessageFallback`/`onError` is configured in src/i18n/request.ts or src/app/layout.tsx, so next-intl's default applies: it throws in dev and renders the literal key path (`simulators.report.generating`) in production. This component is rendered in the live mix-workspace footer for every saved scenario (mix-calculator.tsx:151) and on the report page, so the user sees the raw key as the button label while the PDF is being rendered.
- **Why:** A core, client-facing flow (download scenario report PDF) shows an untranslated machine string ("simulators.report.generating") on the primary action button during the loading state, in both Hebrew and English. It looks broken to the advisor and to anyone watching, and in dev it can throw.
- **Fix:** Add `"generating"` to the `simulators.report` block in both messages/he.json and messages/en.json (e.g. He: "מפיק…" / En: "Generating…"). Optionally add a global `getMessageFallback` so future missing keys degrade gracefully instead of leaking the key path.
- **Verify:** Confirmed by reading the code. scenario-report-actions.tsx:29 scopes translations with useTranslations('simulators.report'); line 88 renders `{isPending ? t('generating') : t('download')}`. The key simulators.report.generating does NOT exist in messages/he.json:649-674 or messages/en.json:649-674 — both blocks contain exactly: openReport, editScenario, title, subtitle, conclusionLabel, conclusionPlaceholder, download, sendToClient, emailDialogTitle, emailDefaultSubject, emailDefaultBody, emailAttachNote, emailSent, backToScenario, backToSimulator, errors. A case-insensitive grep for "generating" across messages/ returns zero matches. No custom error handling is configured: src/i18n/request.ts returns only {locale, messages}; src/app/layout.tsx:80 passes only locale+messages to NextIntlClientProvider; grep for getMessageFallback/onError finds no next-intl config (only unrelated <img onError> handlers). next-intl is ^4.12.0 (package.json:37), whose default behavior for a missing message is to return the full key path string in production and surface an error in dev — so the button label shows the literal "simulators.report.generating" during the loading/isPending state, in both He and En. The component is on a live, client-facing path: mix-calculator.tsx:151 renders <ScenarioReportActions> in the workspace footer whenever scenarioId is set, plus the report page. Note: the doc-path in CLAUDE.md (src/messages) is stale — the real files are at repo-root messages/, which the finding cited correctly. Severity Medium is appropriate: user-visible broken UI on a core flow (download scenario report PDF), but only during a transient loading state and non-crashing in production.

#### `SIM-ROUTE-1` — Mix workspace edit affordances are not gated on use_simulators — a view_simulators-only user sees editable inputs + a failing auto-save (C-036)
- **File:** src/app/(app)/simulators/mix/page.tsx:12-33; src/app/(app)/cases/[id]/simulators/mix/page.tsx:18-62
- **Evidence:** view_simulators and use_simulators are distinct, independently-grantable permissions (supabase/migrations/095_simulator_permissions.sql:5-29; spec docs/ISRAEL_MORTGAGE_SIMULATOR_PLATFORM_SPEC.md:814-815 — 'view_simulators: view hub and saved scenarios' vs 'use_simulators: create/update/delete scenarios'). The standalone mix page only checks `view_simulators` and renders `<MixWorkspace ... />` with no readOnly prop (defaults false) and no caseId. The case mix page checks `view_simulators` and passes `readOnly={!canEdit}` where canEdit = userCanEditCase — it never consults use_simulators. So a user granted view_simulators but NOT use_simulators gets fully editable inputs, the '+' new-tab button (mix-workspace.tsx:114-124), and the editable <fieldset> (mix-calculator.tsx:104). use-scenario-autosave.ts then fires saveScenarioAction, which correctly rejects with `unauthorized` (save-scenario.ts:23), surfacing the red 'error' SaveStatus (mix-calculator.tsx:191-193). The server is safe; the UI is not — this is exactly the C-036 'view-only users must not see edit affordances that fail at the server' invariant. SIM-PERSIST-2 only closed the can_edit_case axis for the case page; the use_simulators axis is still open on BOTH the standalone and case mix surfaces.
- **Why:** A view-only simulator user (e.g. a secretary granted view_simulators only) can type into every field, add tabs, and edit a saved scenario's title/conclusion, then see a confusing 'error' instead of being told they cannot edit. No data is leaked or written (RLS + the action gate hold), but it is a real authz-UX gap and the same defect class the round was created to close.
- **Fix:** Compute `canUse = await userHasPermission('use_simulators')` in both mix pages and thread it into MixWorkspace's readOnly: standalone `readOnly={!canUse}`; case `readOnly={!canEdit || !canUse}`. (Optionally fold use_simulators into the readOnly condition inside the page rather than adding a second prop.) The pure calculators — compare/scenario/tax/affordability — need no change since they never persist.
- **Verify:** Confirmed against the code. (1) view_simulators and use_simulators are distinct, independently-grantable permissions — supabase/migrations/095_simulator_permissions.sql lines 5-29 insert both as separate rows; spec docs/ISRAEL_MORTGAGE_SIMULATOR_PLATFORM_SPEC.md:814-815 defines view_simulators='view hub and saved scenarios' vs use_simulators='create/update/delete scenarios'. (2) Standalone page src/app/(app)/simulators/mix/page.tsx:13 gates only on view_simulators and line 33 renders <MixWorkspace .../> with no readOnly prop (defaults false in mix-workspace.tsx:61) and no caseId. (3) Case page src/app/(app)/cases/[id]/simulators/mix/page.tsx:19 gates on view_simulators, computes canEdit=userCanEditCase (line 28) and passes readOnly={!canEdit} (line 61) — it never consults use_simulators. (4) With readOnly=false the '+' new-tab button renders (mix-workspace.tsx:114-124) and the <fieldset disabled={readOnly}> is editable (mix-calculator.tsx:104), so a view_simulators-only user gets fully editable inputs. (5) use-scenario-autosave.ts then fires saveScenarioAction, which gates on use_simulators at save-scenario.ts:23 and returns {ok:false,error:'unauthorized'}, surfacing the red 'error' SaveStatus at mix-calculator.tsx:191-193. (6) Decisive corpus check: grep of src/ shows use_simulators is referenced ONLY in save-scenario.ts:23 and delete-scenario.ts:16 (both server actions) — never in any page/component/hook for UI gating; no simulators layout.tsx exists to gate it either. (7) Pure calculators (compare/scenario/tax/affordability) never persist — grep confirms only mix-calculator.tsx (via use-scenario-autosave) calls the save path — so they correctly need no change. The server is safe (RLS + the action gate hold; no data leaked or written), but the UI exposes edit affordances that fail at the server — exactly the C-036 invariant this round closes, with the use_simulators axis left open (SIM-PERSIST-2 only closed the can_edit_case axis). Medium severity is accurate: authz-UX affordance gap, no data exposure, but confusing failing-write experience. The suggested fix (thread userHasPermission('use_simulators') into readOnly on both pages) is correct and consistent with the existing action gates.

### Low (11)

#### `SIM-UI-2` — track-table.tsx exceeds the 250-line component hard limit
- **File:** src/features/simulators/components/track-table.tsx:1
- **Evidence:** File is 256 lines (verified via awk END{print NR}). CLAUDE.md Engineering Standards: components max 250 lines, and 'Exceeding limit means SPLIT, not deferred' / 'God files (>250 lines for components) are an auto-reject anti-pattern.' The file already contains four sub-components (TrackTableRow, TrackCard, ResultCell, Th/Td) plus the parsing helpers (parseTrackType, parseRepayment) and the field-definitions array.
- **Why:** Violates a non-negotiable project size limit; the file mixes the table shell, the mobile card layout, the row layout, the shared field-definition builder, and value parsers in one module.
- **Fix:** Extract the row/card presentational pieces (TrackTableRow, TrackCard, ResultCell, Th, Td) into a co-located file (e.g. track-table-rows.tsx) and/or move parseTrackType/parseRepayment + the trackTypes/repayments consts into a small module, bringing the main component under 250 lines.
- **Verify:** Confirmed by reading the file and counting lines. src/features/simulators/components/track-table.tsx is exactly 256 lines (verified via awk END{print NR}), 6 lines over the CLAUDE.md "Components: max 250 lines" hard limit. The finding quotes the standard accurately ("Exceeding limit means SPLIT, not deferred"; "God files (>250 lines for components) are an auto-reject"). The composition claim is substantively correct: the file holds the main exported TrackTable (35-150) plus presentational sub-components TrackTableRow (160-186), TrackCard (188-219), ResultCell (221-228), Th (230-232) and Td (234-236), the trackTypes/repayments consts (238-239), the parseTrackType/parseRepayment parsers (241-256), and the inline fields field-definition builder (39-99) — multiple concerns in one module. The finding says "four sub-components"; there are arguably five (it bundles Th/Td), a trivial miscount that doesn't affect the conclusion. No AUDIT-ACK or eslint-disable comment exempts the file, and it is the single largest component in the simulators feature (next is 221). Severity correctly stays Low: the 250-line cap is a prose convention in CLAUDE.md, not an enforced ESLint max-lines rule (none configured in eslint.config.mjs), so it does not break the build; the overage is only 2.4% and the suggested extraction is a trivial mechanical refactor with no functional impact.

#### `SIM-UI-3` — Tabbed workspaces expose role="tab" without matching tabpanel semantics
- **File:** src/features/simulators/components/mix-workspace.tsx:127
- **Evidence:** MixWorkspace renders a `role="tablist"` with `role="tab"`/`aria-selected` buttons (lines 94-113), but the content panels (lines 127-146) are plain `<div hidden={...}>` with no `role="tabpanel"`, no `id`/`aria-labelledby` linking tab→panel, and the tabs have no `aria-controls`. Same pattern in comparison-variant-tabs.tsx:33-65 (tablist + tabs, no tabpanel association on the TrackTable/variant editor it drives in mix-comparison.tsx).
- **Why:** Screen-reader users hear the tabs announced as a tablist but the activated panel is not announced as a tabpanel nor associated with its tab, so the relationship and the read-only/active context are lost. The app targets bilingual a11y (Definition of Done: works for assistive tech in He/En).
- **Fix:** Give each panel `role="tabpanel"` + a stable `id`, add `aria-controls` on each tab and `aria-labelledby` on each panel (or use a small tabs primitive). Apply the same to ComparisonVariantTabs and its driven editor panel.
- **Verify:** Every code claim in the finding is verified against the source.

mix-workspace.tsx: lines 94-113 render a `role="tablist"` (line 94) containing `role="tab"` buttons with `aria-selected={active}` (lines 101-102) but NO `aria-controls`. Lines 127-146 render the content panels as plain `<div key={tab.key} hidden={tab.key !== activeKey}>` (line 128) wrapping `<MixCalculator>` — no `role="tabpanel"`, no `id`, no `aria-labelledby`. So there is no tab-to-panel ARIA association. Confirmed exactly as described.

comparison-variant-tabs.tsx: lines 33-65 render `role="tablist"` (line 33) with `role="tab"` + `aria-selected` buttons (lines 41-43) and no `aria-controls`. In mix-comparison.tsx, ComparisonVariantTabs (lines 42-51) drives the RegulatoryViolationsBanner + TrackTable panel (lines 52-53), which carry no `role="tabpanel"` or association. Confirmed.

There is no shared tabs primitive (no src/components/ui/tabs.tsx; Glob of components/ui shows none), so these are hand-rolled. The same incomplete tab ARIA pattern already exists in src/features/settings/components/roles-permissions-editor.tsx (lines 87-112: tablist + tab + aria-selected, no tabpanel), so it is an established (incomplete) codebase convention rather than an isolated slip.

This is a genuine WAI-ARIA authoring gap: per the tabs pattern, a tablist's tabs should reference their panels via aria-controls and each panel should be role="tabpanel" with aria-labelledby pointing back to its tab. Screen readers will announce the tabs as a tablist but won't announce the revealed content as the associated tabpanel.

Severity correctly stated as Low: the controls remain operable, are labeled, and convey selected state via aria-selected; content stays in the DOM and reachable; no keyboard trap or feature breakage. The finding slightly over-leans on the "targets bilingual a11y" framing (CLAUDE.md's Definition of Done says "works on assistive tech" loosely; it does not mandate full ARIA tab semantics), but the underlying technical defect is real and the Low rating is appropriate.

#### `SIM-UI-4` — Track rate/term inputs lack min bounds, so negative/zero values are silently accepted with no validation feedback
- **File:** src/features/simulators/components/track-table.tsx:61
- **Evidence:** The rate field is `<input type="number" step="0.01" ... onChange={(e)=>onUpdate(track.id,{annualRatePct:Number(e.target.value)})}` (line 61) and term is `<input type="number" ... value={track.termMonths} onChange Number(...)` (line 67) — neither has `min`. By contrast the loan inputs do (mix-inputs-bar.tsx:38 and loan-base-panel.tsx:41 both use `min={1} max={480}`). A user can enter a negative rate or term; the value flows straight into the engine via normalizeTrack (utils/track-factory.ts:21, which does not clamp amount/rate/term) with no inline cue. For `type=number` an empty field yields `Number('') === 0`, so clearing term silently sets a 0-month track rather than surfacing an error.
- **Why:** Inconsistent input guards across the same family of fields; nonsensical inputs (negative rate, 0/negative term) are accepted without feedback, producing misleading per-track results in a financial advisory tool.
- **Fix:** Add `min={0}` (rate) and `min={1} max={480}` (term) to the track inputs to match the loan-level inputs, and/or surface a per-field validation hint when out of range. Consider clamping in normalizeTrack as a defensive backstop.
- **Verify:** Confirmed from the code. In src/features/simulators/components/track-table.tsx the rate input (line 61: type="number" step="0.01", no min) and term input (line 67: type="number", no min/max) lack bounds, while the loan-level term inputs DO bound them: mix-inputs-bar.tsx:38 and loan-base-panel.tsx:41 both use min={1} max={480}. Real inconsistency within the same field family. The live edit path (use-mix-calculator.ts:91-97 updateTrack) applies the patch then normalizeTrack (track-factory.ts:21), which only normalizes cpiAnnualPct/graceMonths and does NOT clamp amount/rate/term — confirmed. So negative/zero values flow straight into aggregateMix/validateMix and are rendered with no inline cue. Number('')===0 means clearing term yields termMonths:0; amortization-spitzer.ts:13 / amortization-equal-principal.ts:13 loop `month <= termMonths` so it produces an empty (zero-month) schedule rendered as misleading/blank results, no error. The engine does not crash on negatives (payment.ts only special-cases rate===0). Two refutation attempts partially temper but do not kill the finding: (1) Server Zod (simulator.schema.ts ScenarioTrackSchema) DOES enforce annualRatePct in [-20,100] and termMonths in [1,480], but only at save/autosave time — the interactive advisory numbers shown before save are already wrong, which is exactly the finding's concern. (2) The suggested fix is partly over-stated: the schema deliberately permits negative rates (min -20), so adding min={0} to the rate input would wrongly block legitimately-negative real rates; only the term-field bound (min={1} max={480}) is unambiguously correct. Net: a genuine but Low-severity UI input-guard/feedback gap. No data corruption (save is Zod-gated), no auth/security impact. Low is the right severity.

#### `SIM-UI-5` — Money inputs round to whole shekels on every keystroke, blocking partial/decimal entry
- **File:** src/features/simulators/utils/format.ts:9
- **Evidence:** `agorotToNis` returns a rounded **number** (`Math.round(value/100)`, line 9-11) which is bound directly as the controlled value of every money input (mix-inputs-bar.tsx:57, loan-base-panel.tsx:60, affordability-calculator.tsx:175, tax-costs-calculator.tsx:169). Each keystroke round-trips through `nisToAgorot`→`agorotToNis`, so the displayed value is always the rounded whole-shekel number; a cleared field shows `0` rather than empty, and decimals/agorot cannot be entered. nisToAgorot itself correctly guards NaN/negatives (`Number.isFinite` + `Math.max(0,...)`, lines 13-16), so this is a UX/affordance issue, not a data-integrity one.
- **Why:** Whole-shekel-only entry is reasonable for mortgage amounts, but binding a rounded numeric value (not a string) to the input forces a permanent `0` placeholder when cleared and prevents intermediate states, which feels stiff and can confuse data entry on mobile.
- **Fix:** Hold the raw input as local string state (or allow an empty string to render as empty) and only normalize to agorot on change/blur, so the field can show empty and the caret/intermediate typing isn't fought by per-keystroke rounding.
- **Verify:** Confirmed by reading the code. src/features/simulators/utils/format.ts:9-11 — agorotToNis returns Math.round(value/100), a rounded number. All four cited MoneyField call sites bind it directly as the controlled input value with onChange routing through nisToAgorot, creating a per-keystroke round-trip:
- mix-inputs-bar.tsx:57 — value={agorotToNis(value)} onChange={(e)=>onChange(nisToAgorot(e.target.value))}
- loan-base-panel.tsx:60 — identical pattern
- affordability-calculator.tsx:175 — value={agorotToNis(value)}; setMoney (line 100-102) stores nisToAgorot(value) as MoneyAgorot state
- tax-costs-calculator.tsx:169 — identical pattern
The inputs are fully controlled (state holds agorot; value reads agorotToNis(state)). Verified consequences: (1) clearing the field -> nisToAgorot("") -> Number("")=0, Number.isFinite(0) true -> 0, so the field displays 0 rather than empty; (2) decimals are lost — e.g. 1500.50 -> 150050 agorot -> agorotToNis=Math.round(1500.5)=1501; (3) intermediate typing states are normalized away by the rounding on every re-render. The finding correctly notes nisToAgorot guards NaN/negatives (format.ts:13-16, Number.isFinite + Math.max(0,...)), so state is always valid agorot — this is purely a UX/affordance issue with no data-integrity impact. All cited line numbers are exact. Severity Low is appropriate: whole-shekel entry is reasonable for mortgage amounts; the only real friction is the permanent 0 placeholder on clear and inability to type decimals, which is cosmetic data-entry stiffness, especially on mobile.

#### `SIM-HOOK-1` — Autosave 'saved' status can show stale-success after a transient save failure
- **File:** src/features/simulators/hooks/use-scenario-autosave.ts:64-92
- **Evidence:** On a failed save the hook sets status 'error' (line 91) but does NOT update savedSigRef. The debounce effect only re-arms `save` when `signature !== seedSig` (dirty). After an error, if the user makes no further edit, the failed content is never retried — the effect won't fire again because nothing changed, so the unsaved edit stays unsaved indefinitely while the user has stopped typing. There is no retry timer and no flush-on-blur/unmount. The next keystroke does re-arm it, but a user who edits, sees a transient network 'error', and then walks away loses that edit silently. (Distinct from the documented in-flight overlap, which IS re-armed by the next edit.)
- **Why:** Autosave's whole contract is 'you don't have to press save.' A transient server/network error that is never retried unless the user happens to type again is a quiet data-loss vector for advisor scenario edits. The user sees a red 'error' chip but has no save button to retry (the UI removed the explicit save).
- **Fix:** On error, schedule a bounded retry (e.g. re-arm the debounce once after a short backoff) or keep the effect armed by tracking a 'needsSave' ref that the effect checks in addition to `dirty`. At minimum, surface a retry affordance in SaveStatus so an idle user can recover.
- **Verify:** Verified against src/features/simulators/hooks/use-scenario-autosave.ts and its only consumers (mix-calculator.tsx, mix-workspace.tsx). The mechanism is exactly as described:

1) On a failed save the else-branch (lines 90-91) calls setStatus('error') only; savedSigRef.current is NOT updated. Confirmed.
2) The debounce effect (lines 99-103) re-arms save ONLY when its deps [signature, dirty, disabled, save] change. `save` is useCallback([], ...) so it is stable; after an error, if the user makes no further edit, `signature` is unchanged and the effect does not re-run — no retry. Confirmed.
3) There is no retry timer and no flush on blur/unmount/beforeunload: a feature-wide grep for beforeunload|visibilitychange|pagehide|onBlur|flush returned nothing. Confirmed.
4) The UI is implicit-save only. SaveStatus (mix-calculator.tsx:158-195) renders a red errors.unknown chip on 'error' with NO save/retry button — the only other affordance is ScenarioReportActions (PDF/send), not save. Confirmed.
5) The finding is genuinely distinct from the documented in-flight overlap (hook line 41 comment), which IS re-armed by the next edit; this is the never-re-armed-after-error case. Confirmed.

Additional corroboration: the action (save-scenario.ts) returns distinct codes (unauthorized/validation/rate_limited/regulatory/unknown) but the hook collapses all of them to one 'error' state — so even a rate_limited failure (60 saves/hr, plausibly reachable for an advisor autosaving across many scenarios on a 1200ms debounce) lands in the same never-retried state, making the transient-failure path more reachable than network errors alone.

Two accuracy nuances that do NOT invalidate it: (a) "silently loses" is slightly softened by the visible red chip — but the reviewer explicitly acknowledges this. (b) The edited state is not lost immediately: calculators stay mounted across tab switches (mix-workspace uses hidden, not unmount), so the in-memory delta survives until full page navigation/reload/close, and any keystroke during that window would re-arm and succeed if the transient error cleared. So the loss occurs specifically when the user errors, stops editing, then leaves the page.

Severity Low is correctly calibrated: a real autosave-reliability gap (the contract is "you don't press save"), but narrow probability (transient error precisely on the debounced save AND zero further edits AND navigating away before noticing the red chip) and low blast radius (a secondary analytical artifact — a mortgage simulation scenario, not core case data — losing one editing session's unsaved delta), with a visible error indicator present. Not High/Medium. Suggested fixes (bounded backoff retry on error, or a needsSave ref the effect also checks, or a retry affordance in SaveStatus) are sound.

#### `SIM-HOOK-2` — Final edit can be dropped when a save runs longer than the debounce window
- **File:** src/features/simulators/hooks/use-scenario-autosave.ts:57-103
- **Evidence:** `save()` early-returns when `inFlightRef.current` is true (line 58). The debounce timer that triggered that call is already consumed and is not rescheduled. The header comment claims a skipped overlap is 're-armed by the next edit' — true only if there IS a next edit. Concretely: if the user's last keystroke fires its 1200ms timer while a prior save is still in flight (possible only when a single saveScenarioAction round-trip exceeds ~1200ms — slow network / cold serverless), `save` no-ops, status is left at 'saving', and that last edit is never persisted because no further edit arrives to re-arm the timer.
- **Why:** Narrow (requires save latency > debounce) but it is a silent loss of the user's most recent edit with a misleading 'saving…' spinner that never resolves. The action is rate-limited to 60/hr and renders a snapshot, so a >1.2s tail latency is plausible under load.
- **Fix:** Set a `pendingRef` flag when `save()` skips due to in-flight, and at the end of the in-flight save (after line 81) re-invoke `save()` (or re-arm the debounce) if `pendingRef` is set and `contentSignature(latest.current) !== savedSigRef.current`.
- **Verify:** Confirmed against src/features/simulators/hooks/use-scenario-autosave.ts. The race is real. save() early-returns at line 58 when inFlightRef.current is true. The debounce effect (lines 99-103) only re-runs/re-arms when `signature` or `dirty` change (save is a stable []-dep useCallback), so a timer that fires during an in-flight save is consumed with no rescheduling. Trace: edit A starts a slow save (await saveScenarioAction, >1200ms); edit B (the final keystroke) re-runs the effect and schedules timer B; timer B fires while A is still in flight -> early-return, timer consumed; when A resolves it sets savedSigRef to edit A's signature (sig captured at line 64 BEFORE the await), so edit B's content is never persisted. The completion block (lines 81-93) has no "content changed during save" re-check, confirming the suggested pendingRef fix targets a genuine gap. No blur/unmount flush exists either (mix-calculator.tsx only reads `status`), so there is no other safety net. The header comment's "re-armed by the next edit" claim is accurate only if another edit arrives. ONE factual error in the finding: it claims status is "left at 'saving'" with a spinner that never resolves. That is wrong — when the in-flight save for edit A succeeds, line 89 runs setStatus('saved'), so the user sees a green "saved" checkmark while their last edit is silently dropped. That makes the bug more deceptive (false success) than described, but the specific spinner assertion is incorrect. Severity Low is correct: trigger is narrow (requires a single saveScenarioAction round-trip >~1200ms AND the final edit landing in that window AND no subsequent edit), the action is server-side rate-limited/RLS-guarded with no security dimension, and impact is loss of only the most-recent edit. Cold-start serverless tail >1.2s is plausible but uncommon on a warm function.

#### `SIM-HOOK-3` — Three calculator hooks duplicate the same setMoney/setTermMonths/addTrack/removeTrack/updateTrack logic
- **File:** src/features/simulators/hooks/use-mix-calculator.ts:87-112
- **Evidence:** use-mix-calculator (lines 87-112), use-scenario-calculator (lines 58-75), and use-mix-comparison (lines 82-95) each re-implement byte-for-byte identical track mutators: `setMoney`, `setTermMonths`, `addTrack` (newTrack('fixed_unlinked', remainingAmount(...), 4.5)), `removeTrack` (length===1 guard), `updateTrack` (map + normalizeTrack). The only structural difference is mix-comparison stores tracks per-variant. This is the same shape repeated across three hooks.
- **Why:** Not a bug, but it is exactly the 'no duplicated logic' standard in CLAUDE.md. A future fix to track-mutation semantics (e.g. clamp amount, recompute remaining) has to be applied in three places and will drift. The default-mix constants also diverge between use-mix-calculator (rate 4.9/6/3.5) and use-scenario-calculator (4.5/4.5/4.2) with conflicting comments about whether prime is entered as base or all-in rate.
- **Fix:** Extract a `useMixTrackMutators(setMix)` (or a pure `mixReducer`) helper that returns the shared setMoney/setTermMonths/addTrack/removeTrack/updateTrack, and have all three hooks consume it. Reconcile the prime-rate comment so it states one convention.
- **Verify:** Confirmed by reading all three hooks. The track mutators are duplicated: setMoney (use-mix-calculator.ts:87-88, use-scenario-calculator.ts:58-59, use-mix-comparison.ts:82-83), addTrack with the identical newTrack('fixed_unlinked', remainingAmount(...), 4.5) literal (mix-calc:98-102, scenario:61-65, comparison:90-91), removeTrack with the length===1 guard (mix-calc:103-107, scenario:66-69, comparison:92-93), and updateTrack with map + normalizeTrack (mix-calc:91-97, scenario:71-75, comparison:94-95). All consume the same shared track-factory.ts helpers (newTrack/normalizeTrack/remainingAmount), so a semantic change to track mutation would need to be applied in three places and could drift. The reviewer's structural caveat is accurate: use-mix-comparison stores tracks per-variant via mutateActive and money on `base` (setBase, not setMix), so it differs in plumbing — a naive useMixTrackMutators(setMix) cleanly covers only the two setMix hooks; the truly shared piece across all three is the pure track-transform function. Grep confirms no useMixTrackMutators/mixReducer extraction exists yet, so the duplication is live. The default-mix divergence is also real and concrete: mix-calc rates 4.9/6/3.5 with a comment saying prime is entered as the all-in rate, vs scenario rates 4.5/4.5/4.2 with a contradictory comment saying prime is the BoI base and the engine adds the 1.5% margin — the two conventions genuinely conflict. This is not a bug (the mutators are correct pure state updates and defaults are user-editable), so the self-assigned Low severity maps correctly to CLAUDE.md's 'no duplicated logic' standard and is neither over- nor under-stated.

#### `SIM-PDF-1` — fmtAgorot omits the Number.isFinite NaN/Infinity guard that the sibling bank-PDF fmtCurrency and the app-wide formatCurrency both carry (R7 precedent divergence)
- **File:** src/features/simulators/pdf/report-formatters.ts:14-16
- **Evidence:** fmtAgorot is `return ${Math.round(value / 100).toLocaleString(intlLocale(locale))} ₪;` with no finite/null check. The sibling bank PDF formatter guards: src/features/cases/pdf/formatters.ts:22 `if (v === null || v === undefined || !Number.isFinite(v)) return dash;` (R7 added exactly this), and the app-wide src/lib/utils/format-currency.ts does the same (documented in docs/review/ROUNDS-09-15-FINDINGS.md:1348). If a NaN/Infinity ever reaches fmtAgorot it renders the literal string 'NaN ₪' / '∞ ₪' / '-∞ ₪' in a client-facing PDF instead of a dash. fmtPct (report-formatters.ts:18-21) already has the guard; fmtAgorot is the lone outlier in this same file. I confirmed the value path is currently finite: report-data.service.ts re-runs aggregateMix on a MixInputSchema-validated input (tracks.min(1), amount.min(1), mortgageAmount.min(1), propertyValue.min(1)); the domain guards division (mix-metrics.costPerShekel returns 0 when principal<=0, money.averageMoney returns 0 on empty, blendedEffectiveRatePct returns 0 when total<=0), so no NaN reaches the report on the validated path today.
- **Why:** This is the exact class of defect the R7 round fixed in the bank PDF. The report is a client-facing artifact emailed to borrowers; a literal 'NaN ₪' would be an embarrassing visible defect rather than a graceful dash. It is latent today (every value is finite via the validated re-aggregation path) but the missing belt-and-suspenders means any future domain change or a new non-mix kind that forwards an un-guarded figure would surface raw NaN to the client, with no test pinning the behavior. Consistency with the two established sibling formatters is the cheap fix.
- **Fix:** Mirror cases/pdf/formatters.ts: `export function fmtAgorot(value: MoneyAgorot | null | undefined, locale: Locale, dash = '—'): string { if (value == null || !Number.isFinite(value)) return dash; return `${Math.round(value / 100).toLocaleString(intlLocale(locale))} ₪`; }`. This also aligns its signature with fmtPct/fmtDate (which both take a dash param) and the bank fmtCurrency.
- **Verify:** Verified against actual code; every claim holds.

1) Source defect (src/features/simulators/pdf/report-formatters.ts:14-16): fmtAgorot(value: MoneyAgorot, locale) returns `${Math.round(value/100).toLocaleString(...)} ₪` with NO null/Number.isFinite guard. A non-finite input would render literal "NaN ₪" / "∞ ₪".

2) Sibling divergence within the same file confirmed: fmtPct (line 19) guards `if (value === null || !Number.isFinite(value)) return dash` and fmtDate (line 24) guards via Number.isNaN; both take a dash param. fmtAgorot is the only un-guarded formatter and the only one lacking a dash param.

3) R7 precedent confirmed: src/features/cases/pdf/formatters.ts:22 fmtCurrency guards `if (v === null || v === undefined || !Number.isFinite(v)) return dash`; fmtNum:42 same. App-wide src/lib/utils/format-currency.ts:23 carries the identical guard. So the consistency-with-two-established-siblings argument is accurate.

4) "Latent today" claim verified true. Traced all fmtAgorot callsites in report-sections.tsx (lines 36-38, 63, 83-88): every arg is a money field (loan.propertyValue/equity/mortgageAmount, track.amount, result.firstPayment/averagePayment/maxPayment/totalInterest/totalIndexation/totalCost). report-data.service.ts:43-46 re-runs aggregateMix on a MixInputSchema.safeParse'd input. MixInputSchema (simulator.schema.ts:75-81) enforces mortgageAmount.min(1), propertyValue.min(1), tracks.min(1) with each track.amount.min(1). These figures derive from sumMoney/averageMoney/Math.max/array-index of those validated, finite values, so no NaN reaches fmtAgorot. Notably the division-prone field costPerShekel (mix-metrics.ts:6-8, guards principal>0) is a ratio and is NOT passed to fmtAgorot at all — it is not rendered via this formatter. money.averageMoney (money.ts:11-14) returns 0 on empty. So the reviewer's own no-NaN-on-validated-path analysis is correct.

Net: a genuine defensive-coding / consistency gap with a correct, cheap fix, but no active reproduction path today (validated re-aggregation guarantees finite money values; the only division-prone metric isn't formatted by fmtAgorot). Reviewer self-classified Low and was explicit it is latent — not overstated. Low is the right severity; not High/Medium because there is no current path to surface NaN to a client.

#### `SIM-PDF-2` — Report subtitle silently mislabels reserved scenario kinds (capacity / early_repayment / refinance) as 'Mortgage mix'
- **File:** src/features/simulators/pdf/report-page.tsx:21
- **Evidence:** report-page.tsx:21 renders `{strings.kind[data.meta.kind] ?? strings.kind.mix}`. data.meta.kind is typed ScenarioKind, which (types.ts:19 / schemas/simulator.schema.ts:15-22 / migration 093 CHECK) includes six values: mix, comparison, scenario, capacity, early_repayment, refinance. But STRINGS_HE.kind / STRINGS_EN.kind (report-strings.ts:65 and :123) only define mix, comparison, scenario, and the Strings.kind type (report-strings.ts:16) makes the other three optional (`Partial<Record<ScenarioKind,string>> & Pick<...,'mix'|'comparison'|'scenario'>`). So a saved scenario with kind 'capacity'/'early_repayment'/'refinance' resolves `strings.kind[kind]` to undefined and falls back to the 'mix' label ('תמהיל משכנתא' / 'Mortgage mix'). report-data.service.ts:53 uses ScenarioKindSchema.catch('mix') so the value is preserved, not normalized, before reaching the page. SaveScenarioSchema.kind (simulator.schema.ts:101) accepts all six, so the DB can legitimately hold those kinds.
- **Why:** A client-facing report that mislabels the simulation type is misleading. It is currently latent because no save path in the feature emits capacity/early_repayment/refinance (grep for `kind: 'capacity'|'early_repayment'|'refinance'` across src/features/simulators returns nothing — the save flow only stores 'mix'), so this never fires today. But the enum, schema, and DB CHECK all permit those kinds, so the moment a future simulator persists one, its report silently claims to be a mortgage mix with no error.
- **Fix:** Add Hebrew + English labels for capacity / early_repayment / refinance to STRINGS_HE.kind and STRINGS_EN.kind and tighten the Strings.kind type to a full Record<ScenarioKind,string> so a missing label becomes a compile error rather than a silent runtime fallback. The `?? strings.kind.mix` fallback can then be dropped (or kept only as a defensive default).
- **Verify:** All claims verified against the code. report-page.tsx:21 renders `{strings.kind[data.meta.kind] ?? strings.kind.mix}`. data.meta.kind is typed ScenarioKind (report-data.service.ts:26), which types.ts:19 and ScenarioKindSchema (simulator.schema.ts:15-22) define as 6 values: mix, comparison, scenario, capacity, early_repayment, refinance — and migration 093_mortgage_scenarios.sql:13 CHECK permits all six. But STRINGS_HE.kind (report-strings.ts:65) and STRINGS_EN.kind (:123) define only mix/comparison/scenario, and the Strings.kind type (:16) is `Partial<Record<ScenarioKind,string>> & Pick<...,'mix'|'comparison'|'scenario'>`, so omitting the other three is NOT a compile error. A stored kind of capacity/early_repayment/refinance therefore resolves strings.kind[kind] to undefined and silently falls back to the 'mix' label (תמהיל משכנתא / Mortgage mix). report-data.service.ts:53 uses ScenarioKindSchema.catch('mix').parse — catch only fires on parse failure, so a valid reserved kind passes through unchanged (preserved, not normalized), confirmed. SaveScenarioSchema.kind (simulator.schema.ts:100) accepts all six and save RPC 096 writes p_payload->>'kind' directly, so the DB can legitimately hold those kinds. Latency confirmed: grep for `kind: '(capacity|early_repayment|refinance)'` across src/features/simulators returns nothing; the only save paths are use-scenario-autosave.ts:75 (hardcodes kind:'mix') and save-scenario.ts:65 (passes input.kind through). So no current path emits the reserved kinds and the mislabel never fires today. Severity Low is correct and the reviewer was honest that this is purely latent — it is a genuine type-safety gap and client-facing mislabel risk the instant a future simulator persists one of those kinds, but it cannot produce a wrong PDF today. Suggested fix (full Record<ScenarioKind,string> + labels to make a missing label a compile error) is sound.

#### `SIM-PDF-1` — fmtAgorot has no non-finite guard while fmtPct/fmtDate do — a NaN/Infinity money value renders 'NaN ₪' in the client PDF
- **File:** src/features/simulators/pdf/report-formatters.ts:14-16
- **Evidence:** `fmtPct` (line 18-21) and `fmtDate` (line 23-27) both short-circuit on `!Number.isFinite(value)` / NaN and return the dash. `fmtAgorot` does not: `Math.round(value/100).toLocaleString()` on NaN yields 'NaN ₪' and on Infinity yields '∞ ₪'. fmtAgorot is used for every money cell in the report (report-sections.tsx:36-41, 63, 83-88). In practice the values come from aggregateMix(mix) where mix is re-validated by MixInputSchema in report-data.service.ts:43-46, so inputs are bounded and a non-finite money result is unlikely — hence Low, not the data-loss tier. But it is the exact R7 guard pattern (Number.isFinite before formatting) applied inconsistently.
- **Why:** Defense-in-depth and consistency with the R7 PDF hardening: a single non-finite money value would leak 'NaN ₪' into a client-facing branded report rather than a clean dash.
- **Fix:** Mirror fmtPct: `export function fmtAgorot(value, locale, dash = '—') { if (!Number.isFinite(value)) return dash; return `${Math.round(value/100).toLocaleString(intlLocale(locale))} ₪`; }`.
- **Verify:** Confirmed by reading the code. src/features/simulators/pdf/report-formatters.ts:14-16: fmtAgorot does `Math.round(value/100).toLocaleString(intlLocale(locale))` with NO non-finite guard — NaN -> 'NaN ₪', Infinity -> '∞ ₪'. Its siblings fmtPct (18-21: `if (value === null || !Number.isFinite(value)) return dash`) and fmtDate (23-27: `Number.isNaN(d.getTime()) ? dash`) both guard. The inconsistency is real. fmtAgorot is used for every money cell in report-sections.tsx (lines 36-38, 63, 83-88), so a non-finite value would leak into the client-facing branded PDF. The cited R7 precedent is genuine: src/features/cases/pdf/formatters.ts:22 guards its money formatter with `if (... !Number.isFinite(v)) return dash` before the identical Math.round().toLocaleString() — the exact pattern fmtAgorot omits.

Reachability supports the Low rating (the reviewer's own caveat holds and is arguably conservative): inputs are re-validated by MixInputSchema in report-data.service.ts:43-46; moneyAgorot = z.coerce.number().int().min(0) — .int() rejects Infinity/NaN, .min(0) rejects NaN, so mortgageAmount/propertyValue/equity/track.amount are all finite, non-negative, bounded integers. Computed result flows through sumMoney (plain + reduce), averageMoney (guards empty), and Math.max over finite arrays (money.ts, track-summary.ts, mix-aggregate.ts), which cannot produce NaN/Infinity from finite bounded inputs. ltv routes through the guarded fmtPct, not fmtAgorot. So no non-finite money value can realistically reach fmtAgorot today — this is a defense-in-depth/consistency gap, not a live data bug. Severity Low is correct; not refutable since the inconsistency and the missing guard are exactly as described.

#### `SIM-SEED-1` — seedBaseFromCase propagates NaN when a case money field is a non-numeric string (?? only catches null/undefined, not NaN)
- **File:** src/features/simulators/utils/seed-mix.ts:16-18
- **Evidence:** `Math.max(1, Number(c.requested_mortgage_amount ?? 800000) * 100)`. The `?? default` only substitutes for null/undefined. If the field is a string that does not parse (e.g. 'abc'), `Number('abc')` → NaN and `Math.max(1, NaN)` → NaN, which then seeds NaN into propertyValue/equity and into every calculator and (for mix) into the auto-saved scenario. Empty string is safe (`Number('') === 0` → Math.max(1,0)=1). The CaseSeedFields type permits `number | string | null` (lines 5-7). In practice the case columns are numeric, so the string path is theoretical → Low.
- **Why:** A malformed/legacy case value would render NaN across the simulator inputs and could be persisted into a scenario snapshot via the case mix auto-save. The defaults exist precisely to avoid garbage seeds, but they miss the NaN case.
- **Fix:** Coerce defensively, e.g. a small helper `const n = (v, d) => { const x = Number(v); return Number.isFinite(x) ? x : d; }` and use `Math.max(1, n(c.requested_mortgage_amount, 800000) * 100)` etc.
- **Verify:** Confirmed the code pattern at C:\Users\shh92\Projects\first-crm\src\features\simulators\utils\seed-mix.ts:16-18: `Math.max(1, Number(c.requested_mortgage_amount ?? 800000) * 100)` (and the analogous propertyValue/equity lines). The reviewer's core mechanics are correct: `??` only substitutes for null/undefined, so a non-numeric string would yield `Number('abc') === NaN` and `Math.max(1, NaN) === NaN`, propagating NaN into mortgageAmount/propertyValue/equity. CaseSeedFields (lines 4-8) does type these as `number | string | null`, so the string path is type-permitted.

However the practical reachability is even weaker than "theoretical," and the persistence claim is over-stated:
1. The actual DB columns are NUMERIC(15,2) (supabase/migrations/006_cases.sql:19-21) and the generated types (src/types/database.ts:1880/1888/1891) type them as `number | null`. getCaseById (src/features/cases/services/cases.service.ts:84-96) returns CaseWithRelations carrying that numeric Row typing. PostgREST returns NUMERIC as JSON numbers, so the real callers (case mix/scenario/compare/affordability/tax pages, all passing `caseData`, and the global mix page passing `{}`) never feed a string — the string branch is effectively dead. So no DTI/calculator garbage can occur in practice from real data.
2. The "persisted NaN into the auto-saved scenario" claim is refuted: the save path saveScenarioAction (src/features/simulators/actions/save-scenario.ts:34-35) runs SaveScenarioSchema.safeParse before any DB write, and MixInputSchema (src/features/simulators/schemas/simulator.schema.ts:45-48,75-81) validates mortgageAmount/propertyValue via `z.coerce.number().int().min(1)`. Zod's number type rejects NaN, so a NaN seed fails validation and the action returns {ok:false,error:'validation'} — nothing reaches the DB. The persistence is fail-closed.

Net: a genuine but defensive-only robustness nit (NaN would only render transiently in client inputs, and only if a malformed legacy string ever appeared, which the schema does not permit). Low severity is correct, arguably even below Low. The suggested Number.isFinite coercion helper is a reasonable hardening but not a functional bug fix.

### Refuted (2)

- `SIM-HOOK-4` useMemo deps in use-mix-calculator omit the result fields the dependent memo actually reads (over-broad recompute) — This is a non-finding by its own construction — the body explicitly states "No change required" and "Confirms the memoization is correct, not a defect." The title alleges a defect ("useMemo deps ... omit the result fields the dependent memo actually reads (over-broad recompute)") that the evidence then contradicts. I verified every concrete claim against the code and they are all accurate, and crucially they show there is NO defect:

- src/features/simulators/hooks/use-mix-calculator.ts:79 — aggregateMix memo deps [mix]; aggregateMix (domain/mix-aggregate.ts:10) reads only fields inside mix. Correct.
- :80-83 — validateMix memo deps [mix, propertyKind, thresholds]; validateMix(input, thresholds, propertyKind) (domain/regulatory-rules.ts:13) reads exactly those three. Correct.
- :84 — composeMixByFamily memo deps [mix.tracks]; composeMixByFamily(tracks) (domain/mix-composition.ts:24) reads only tracks. Minimal/correct.
- :85 — rateExposure memo deps [composition]; rateExposure(slices) (domain/mix-exposure.ts:20) reads only slices. Correct.
- use-scenario-calculator.ts:56 — stressMix memo deps [mix, scenario]. Correct.
- use-mix-comparison.ts:73 — compareMixes memo deps [mixes]. Correct.
- The named DTI memo (mix-calculator.tsx:65-76) has deps [monthlyNetIncome, monthlyObligations, calc.result.firstPayment, calc.result.maxPayment], which are EXACTLY the four inputs mixDti() consumes (domain/mix-dti.ts:21-26: firstPayment, stressPayment←maxPayment, netIncomeMonthly, obligationsMonthly). types.ts:81-95 confirms firstPayment/maxPayment exist on MixResult. So the deps neither omit nor over-list anything the dependent memo reads.

There is no missing dependency, no omitted field, and no over-broad-recompute bug — the title's claimed defect is self-refuted in the body, and the suggested fix is "no change." It describes the absence of a problem, so it is not a genuine code defect. Marking invalid.
- `SIM-ROUTE-2` Case simulator routes have no loading.tsx skeleton, unlike the standalone /simulators group — The finding's core claim — that case simulator routes have "no route-level fallback" and "blank/stall instead of showing the same branded loader" — is refuted by the code. Verified facts: (1) The case simulator routes exist with no own loading.tsx (cases/[id]/simulators/ + mix/tax/scenario/compare/affordability subdirs) — confirmed via find. (2) The standalone group ships src/app/(app)/simulators/loading.tsx (LoadingLogo + skeleton) — confirmed. (3) mix/page.tsx:22-30 awaits a Promise.all of 6 data fetches (getCaseById, getRegulatoryThresholds, listScenariosForCase, listIncomesForCase, listObligationsFlatForCase, userCanEditCase) plus getTranslations before first paint — confirmed.

BUT the decisive fact the reviewer missed: src/app/(app)/cases/[id]/loading.tsx EXISTS and renders a branded LoadingLogo + full case-detail skeleton. In the Next.js App Router a loading.tsx establishes a Suspense boundary covering its segment AND all nested child segments. There is no intervening layout.tsx between cases/[id] and the simulator pages (glob for cases/[id]/**/layout.tsx returned nothing), so navigation into /cases/[id]/simulators/* is wrapped by the nearest ancestor boundary — cases/[id]/loading.tsx — which is itself a branded LoadingLogo loader (and behind that, cases/loading.tsx and (app)/loading.tsx also exist). The routes therefore do NOT blank/stall; they show a branded loader during the awaited fetches.

The only residual issue is cosmetic: the inherited skeleton mirrors the case-detail layout (sticky action bar + 7-block grid) rather than the simulator layout, so the skeleton shape doesn't match the simulator page. That is a far weaker concern than "no loader / blank stall," is not what the finding describes, and falls below Low actionability. The finding's own Suggested fix offers the escape hatch "or confirm the parent cases/[id] segment already supplies an acceptable Suspense fallback" — and it does. The reviewer over-stated the impact by not accounting for App Router loading-boundary inheritance. Marking invalid because the asserted defect (missing/blank fallback) does not hold.


## Round 17 — DB foundation — migrations 001-055

**raw 8 -> confirmed 7 / refuted 1  (High 0, Medium 1, Low 6)**

### Medium (1)

#### `AUTH-01` — case_borrowers UPDATE/DELETE RLS still uses the legacy assigned_advisor check — excludes associated advisors, so role-change and remove-borrower silently fail for them at the DB
- **File:** supabase/migrations/024_rls_layer2_and_constraints.sql:43-80 (effective latest); src/features/borrowers/actions/update-borrower-role.ts:43-64; src/features/borrowers/actions/remove-borrower-from-case.ts:60-75
- **Evidence:** case_borrowers_update (024:43) and case_borrowers_delete (024:68) gate writes on `public.has_permission('edit_any_case') OR (public.has_permission('edit_own_case') AND c.assigned_advisor_id = auth.uid())`. This is the LEGACY guard that predates associated advisors (mig 146/147). The R8/R9-11 canonicalization series (190-195) repointed borrowers/incomes/obligations/case_banks/case_expenses/documents/scenarios at can_edit_case, but NEVER touched case_borrowers write policies (confirmed: grep of 190 shows it only references case_borrowers inside RPC bodies/joins, no DROP/CREATE POLICY for case_borrowers_update/delete; no migration after 024 redefines them). Meanwhile updateBorrowerRoleAction (update-borrower-role.ts:38) and removeBorrowerFromCaseAction (remove-borrower-from-case.ts:26) both gate on `userCanEditCase()` which calls the can_edit_case RPC (mig 147) that DOES admit associated advisors, then perform a DIRECT `.from('case_borrowers').update(...)` / `.delete(...)`. An associated advisor passes the app gate but the direct write matches 0 rows under the legacy RLS, so the action returns `{ok:false,error:'unauthorized'}` (role change) or `{ok:false,error:'not_found'}` (remove). INSERT is unaffected because it goes through SECURITY DEFINER RPCs (add_empty_borrower_to_case / save_borrower_for_case_full).
- **Why:** This is the exact base invariant: 'the LEGACY guard assigned_advisor_id = auth.uid() OR edit_any_case EXCLUDES associated advisors = a bug.' An associated advisor (יועץ משוייך) legitimately on a case cannot change a borrower's role on the case, nor detach a borrower — two core case-workspace flows the UI presents to them (the C-036 edit gate shows the affordances because userCanEditCase is true). It is the only case-child write path left un-canonicalized after the 190-195 sweep, so it is a real, currently-shipping authz/broken-flow gap for that role, with a misleading error code to boot.
- **Fix:** Add a small follow-up migration mirroring 190/192: DROP+CREATE case_borrowers_update and case_borrowers_delete (and for symmetry case_borrowers_insert, harmless since it's RPC-only) using `public.can_edit_case(case_id)` in USING/WITH CHECK, replacing the inline cases-join legacy check. can_edit_case is a superset of the legacy check, so this only widens to associated advisors/admins/responsible exactly as the other child tables — it cannot grant a user who lacked edit authority.
- **Verify:** CONFIRMED by reading the code. The case_borrowers UPDATE/DELETE RLS policies still use the legacy guard, while the matching server actions gate on can_edit_case (which admits associated advisors), so role-change and remove-borrower silently fail at the DB for associated advisors.

Evidence verified:
1. 024_rls_layer2_and_constraints.sql:43-66 (case_borrowers_update) and :68-80 (case_borrowers_delete) gate writes on `has_permission('edit_any_case') OR (has_permission('edit_own_case') AND c.assigned_advisor_id = auth.uid())` — the legacy check, no associated-advisor branch.
2. Grep over ALL migrations for case_borrowers_update/_delete returns ONLY 024. No later migration (147, 190-195, etc.) redefines them. Migration 039 redefines only case_borrowers_select (read, via can_view_case). So the write policies were never canonicalized.
3. Migration 190 (the R8 canonicalization) repoints borrower_incomes/borrower_obligations policies + the borrower RPCs at can_edit_case/_assert_can_edit_case, but its only references to case_borrowers are inside RPC bodies/joins — it never touches case_borrowers write policies. Confirmed verbatim.
4. update-borrower-role.ts:38 gates on userCanEditCase(caseId); :43-48 does a DIRECT `.from('case_borrowers').update({role_in_case}).eq(...).select('borrower_id')`; :62-63 returns `{ok:false,error:'unauthorized'}` when 0 rows match (the RLS no-match outcome). 
5. remove-borrower-from-case.ts:26 gates on userCanEditCase; :60-65 does a DIRECT `.from('case_borrowers').delete({count:'exact'}).eq(...)`; :75 returns `{ok:false,error:'not_found'}` when count===0.
6. permissions.ts:102-122 userCanEditCase delegates to the can_edit_case RPC; mig 147:81-101 defines can_edit_case to include `is_case_associated_advisor(c.id)` (the OR-branch the legacy 024 policy lacks). mig 146:42-54 defines is_case_associated_advisor. can_edit_case is therefore a strict SUPERSET of the legacy 024 check.
7. INSERT is indeed not affected: add_empty_borrower_to_case (190:270) and save_borrower_for_case_full (190:130) are SECURITY DEFINER RPCs that bypass case_borrowers_insert RLS.

Net effect: an associated advisor passes the app gate (userCanEditCase=true) and the C-036 UI edit gate shows the affordances, but the direct case_borrowers UPDATE/DELETE matches 0 rows under the legacy RLS, producing a false 'unauthorized' (role change) / 'not_found' (remove). Two core case-workspace flows are broken for that role, with a misleading error code. It is the only case-child write path left un-canonicalized after the 190-195 sweep.

Severity Medium is correct, not overstated: this OVER-denies (fails closed) rather than over-grants — no privilege escalation — but it is a real, currently-shipping broken authorized flow for a legitimate role plus a misleading error. The suggested fix (mirror 190/192: DROP+CREATE case_borrowers_update/_delete using can_edit_case) is sound and only-widens-by-superset, cannot grant a user who lacked edit authority.

### Low (6)

#### `AUTH-03` — Core SECURITY DEFINER functions has_permission() and is_admin() never REVOKE EXECUTE from PUBLIC — left granted to PUBLIC/anon, against the stated least-privilege invariant
- **File:** supabase/migrations/002_auth_core.sql:221-300
- **Evidence:** has_permission(TEXT) (002:221) and is_admin() (002:283) are SECURITY DEFINER with SET search_path = public (correct), but there is NO `REVOKE ALL ... FROM PUBLIC` + `GRANT ... TO authenticated` for either — confirmed by grep across all migrations (no REVOKE/GRANT line ever references has_permission or is_admin). Postgres defaults new functions to EXECUTE for PUBLIC, so both are callable by the anon role. Contrast with every newer definer fn (can_edit_case mig 106:74-75, _assert_can_edit_case mig 099:296, is_case_associated_advisor mig 146:56-57, has_permissions mig 091, has_permission_for/can_view_case_for mig 188/194, the 190/192/195 RPCs) which all REVOKE-from-PUBLIC then GRANT-to-authenticated.
- **Why:** No actual bypass: both functions return FALSE when auth.uid() IS NULL, so an anon caller gains nothing. But the round-specific SQL invariant is explicit — 'never leave EXECUTE to PUBLIC on a definer fn' — and these are the two most security-load-bearing functions in the system (every RLS policy calls has_permission/is_admin). The inconsistency means the foundational functions are the only definer fns not following the project's own hardened grant pattern; tightening removes anon's ability to even probe them and aligns with least-privilege defense-in-depth.
- **Fix:** In a follow-up migration: `REVOKE ALL ON FUNCTION public.has_permission(TEXT) FROM PUBLIC; GRANT EXECUTE ON FUNCTION public.has_permission(TEXT) TO authenticated;` and the same for `public.is_admin()`. (Also consider adding the matching REVOKE for can_view_case(uuid), which got a GRANT in mig 040 but no REVOKE-from-PUBLIC.)
- **Verify:** Verified against the actual code. In supabase/migrations/002_auth_core.sql, has_permission(perm_key TEXT) (line 221) and is_admin() (line 283) are both SECURITY DEFINER with SET search_path = public. A grep across the whole file finds NO REVOKE/GRANT statements at all (only is_granted/override_granted column hits), so both keep Postgres's default EXECUTE TO PUBLIC and are callable by the anon role. Both functions are defined ONLY in 002 — grep -l for "FUNCTION public.has_permission(" and "FUNCTION public.is_admin(" returns 002 alone; they are never re-created later with a grant block.

The contrast claim is accurate: the codebase has an established REVOKE-ALL-FROM-PUBLIC + GRANT-TO-authenticated pattern (grep finds 56 migration files / 83 lines of "REVOKE ALL ON FUNCTION"). Confirmed concrete examples: can_edit_case at 106_simulator_market_data_foundation.sql:74-75 (the cited "mig 106:74-75" is correct — can_edit_case lives inside that multi-purpose file), _assert_can_edit_case 099:296, is_case_associated_advisor 146:56-57, has_permission_for 188:69-70, can_view_case_for 194:60-61. The two most security-load-bearing functions are precisely the ones NOT following the project's own hardened pattern.

No actual bypass exists, exactly as the reviewer states: has_permission returns FALSE when auth.uid() IS NULL (002:235-237), and is_admin's EXISTS matches p.id = auth.uid() so a NULL uid yields no row → FALSE. An anon caller gains nothing functional; this is defense-in-depth / least-privilege consistency only. Severity Low is correct (not higher, since there is no exploitable bypass; not invalid, since the inconsistency is real and the SQL invariant cited is explicit). The secondary observation also checks out: 040_storage_rls_can_view_case.sql:18 GRANTs can_view_case(uuid) to authenticated with no paired REVOKE-from-PUBLIC. The suggested fix (a follow-up migration with REVOKE ALL ... FROM PUBLIC + GRANT EXECUTE ... TO authenticated for has_permission(TEXT), is_admin(), and can_view_case(uuid)) is appropriate and low-risk.

#### `DEF-1` — cleanup_rate_limit_counters() (SECURITY DEFINER) keeps default PUBLIC EXECUTE — anon/authenticated can invoke a privileged purge
- **File:** supabase/migrations/048_rate_limit_counters.sql:85-99
- **Evidence:** cleanup_rate_limit_counters() is declared SECURITY DEFINER + SET search_path = public and runs `DELETE FROM public.rate_limit_counters WHERE window_start < now() - INTERVAL '1 day'`, bypassing the table's RLS (the table has RLS enabled with NO policies — only the DEFINER RPCs are meant to touch it). Unlike its two sibling cleanup functions, it has NO grant treatment at all: no `REVOKE ALL ... FROM PUBLIC` and no explicit GRANT, so Postgres' default `EXECUTE TO PUBLIC` stands. `anon` is a live role in this project (151/153/154/165 GRANT submit_public_intake / consume_public_contact_rate_limit TO anon), so PostgREST will execute it for an unauthenticated caller. The two siblings WERE locked down — migration 173:70-71/94-95 does `REVOKE ALL ON FUNCTION public.cleanup_old_audit_logs()/cleanup_soft_deleted_records() FROM PUBLIC` — and consume_rate_limit's identical PUBLIC-execute hole was closed in 164:51-52, but cleanup_rate_limit_counters was missed by both. `grep -rln 'FUNCTION public.cleanup_rate_limit_counters'` returns only 048, confirming no later migration redefines or revokes it. It is invoked legitimately only by pg_cron (057:57-61), which runs as postgres and does not need the PUBLIC grant.
- **Why:** Least-privilege violation on a SECURITY DEFINER function that mutates a table protected by RLS. Blast radius is small (it only deletes rate-limit buckets older than 24h, and is idempotent/cheap), so it is not a data-loss or auth-bypass issue — but it is an inconsistency with the rest of the cleanup family and a minor DoS-adjacent surface: any unauthenticated visitor can repeatedly trigger the privileged DELETE, and the function is the only rate-limit RPC still reachable without the service-role key after 164 locked down consume_rate_limit/refund_rate_limit.
- **Fix:** Add, in a new migration (mirroring 164/173): `REVOKE ALL ON FUNCTION public.cleanup_rate_limit_counters() FROM PUBLIC, anon, authenticated;` and, if it is ever called outside pg_cron, `GRANT EXECUTE ... TO service_role;`. pg_cron runs as postgres so it does not require an explicit grant.
- **Verify:** Confirmed by reading the code. cleanup_rate_limit_counters() at supabase/migrations/048_rate_limit_counters.sql:85-99 is SECURITY DEFINER + SET search_path=public and runs DELETE FROM public.rate_limit_counters WHERE window_start < now()-INTERVAL '1 day' against a table that has RLS enabled with NO policies (048:29-30, "the RPC is the only door"). The function definition ends at $$ with NO grant treatment — no REVOKE, no GRANT. A repo-wide grep for `REVOKE ALL ON FUNCTION public.cleanup_rate_limit_counters` returns zero matches; the only references are its definition (048), its pg_cron schedule (057_schedule_cleanup_jobs.sql:57-61), and the generated RPC type in src/types/database.ts:3659. So Postgres' default EXECUTE TO PUBLIC stands.

The contrast claims check out: refund_rate_limit and consume_rate_limit were locked to service_role in 164_rate_limit_refund_lockdown.sql:48-52 (REVOKE ALL ... FROM PUBLIC, anon, authenticated; GRANT to service_role), and the two sibling cleanup functions were revoked from PUBLIC in 173_retention_purge_switch.sql:70-71,94-95. cleanup_rate_limit_counters was missed by both. anon is a live role (submit_public_intake granted TO anon in 151/153/154; consume_public_contact_rate_limit in 165), so PostgREST resolves anon callers and a default-PUBLIC SECURITY DEFINER fn is reachable unauthenticated. The function is invoked legitimately only by pg_cron, which runs as postgres and needs no PUBLIC grant (057:57-61).

Adversarial refutation failed: no project-wide ALTER DEFAULT PRIVILEGES or schema-level USAGE revocation from anon exists (grep clean), so the default PUBLIC EXECUTE is genuinely effective. Every comparable SECURITY DEFINER RPC in this repo carries an explicit REVOKE ALL ... FROM PUBLIC (dozens of them), making this a real deviation from the codebase's own established convention — and src/lib/rate-limit.ts:38-43 documents the exact anon-key/PostgREST DoS threat model for this RPC family.

Severity Low is correct and not over/understated: blast radius is tiny — it only deletes buckets older than 24h (cron runs hourly so there is normally near-nothing past 24h to remove), it is idempotent/cheap, no data loss, no auth bypass, and it does NOT reset current-window counters (so the "burn a victim's rate-limit" framing overstates impact). It is a genuine least-privilege/consistency gap plus a marginal DoS-adjacent surface. Fix is the one-liner the finding suggests: REVOKE ALL ON FUNCTION public.cleanup_rate_limit_counters() FROM PUBLIC, anon, authenticated; in a new migration.

#### `DB-001` — case_number default generator races under concurrent case creation (MAX+1 with no lock)
- **File:** supabase/migrations/001_setup.sql:31-47
- **Evidence:** generate_case_number() computes the next sequence with `SELECT COALESCE(MAX(CAST(SPLIT_PART(case_number,'-',2) AS INT)),0)+1`. It is wired as the DEFAULT for cases.case_number, which carries a UNIQUE NOT NULL constraint (006_cases.sql:13). create_case_draft (074) and the lifecycle RPCs INSERT into cases relying on this default. Under READ COMMITTED (the Postgres default), two transactions inserting a case in the same year concurrently both read the same MAX and compute the same next_seq; the second INSERT fails on the UNIQUE constraint (23505). generate_case_number is never redefined in any later migration (grep over the whole dir: only 001 defines it).
- **Why:** A second advisor (or a lead-conversion + manual create overlapping) creating a case at the same instant gets a hard unique-violation error and an aborted save instead of a clean number. Blast radius is small for this office (1 manager + a few advisors, ~80 cases) so concurrency is rare, but it is a real, silent correctness gap in the generator.
- **Fix:** Serialize the generator: take a transaction-scoped advisory lock keyed on the year before computing MAX (e.g. `PERFORM pg_advisory_xact_lock(hashtext('case_number'||current_year))`), or back the sequence with a real per-year SEQUENCE / a small counter table updated with `... RETURNING`. Either removes the read-then-insert race. Also consider filtering soft-deleted rows is unnecessary (gaps are fine), but the lock is the fix.
- **Verify:** Confirmed in code. generate_case_number() in supabase/migrations/001_setup.sql:31-47 computes the next sequence as `SELECT COALESCE(MAX(CAST(SPLIT_PART(case_number,'-',2) AS INT)),0)+1` with NO advisory lock, NO backing SEQUENCE, and NO retry. It is wired as the column DEFAULT for cases.case_number, which carries `TEXT UNIQUE NOT NULL DEFAULT public.generate_case_number()` (006_cases.sql:13). A grep over the entire migrations dir confirms the reviewer's central claim: generate_case_number is defined ONLY in 001 and never redefined; there is no `pg_advisory`, `CREATE SEQUENCE`, or `nextval` anywhere in the schema. Three independent insert paths rely on this default and do not supply case_number themselves: create_case_draft (074_create_case_draft_rpc.sql:78-89, explicitly commented "case_number auto-generates via the table default"), convert_lead_to_case (031:44-48), and import_cases (037:79-84). Under READ COMMITTED (Postgres default), two concurrent transactions inserting cases in the same year both read the same MAX and produce the same next_seq; the second to commit fails on the UNIQUE constraint (23505). There is no ON CONFLICT clause on any of those inserts and no application-layer retry — saveCaseDraftAction (src/features/cases/actions/save-case-draft.ts:68-77) and importCasesAction surface a generic 'unknown'/error, so a collision aborts the save with an opaque error rather than a clean number. The technical claim is fully accurate. Severity Low is correct and the reviewer self-calibrated well: the race window is microseconds, the office is tiny (1 manager + a few advisors, ~80 cases lifetime), import_cases runs in a single transaction so it has no intra-loop self-collision (only inter-transaction races count) and is additionally rate-limited to 5/hr, and the failure mode is a visible hard error the user can simply retry — not silent data corruption. Real, low-probability correctness gap; the suggested advisory-lock or per-year sequence fix is appropriate.

#### `DB-002` — case_expenses.amount has no non-negative CHECK constraint (every other money column does)
- **File:** supabase/migrations/081_case_expenses.sql:21
- **Evidence:** case_expenses.amount is `NUMERIC(15,2)` with no value-range CHECK. Migration 052 added `>= 0` guards to every other monetary column (cases.property_value/requested_mortgage_amount/equity, case_financials.fee_amount/expected_income, borrower_incomes.amount_monthly, borrower_obligations.monthly_payment/loan_amount), but 052 ran before 081 created case_expenses, and no later migration backfills the constraint (grep for `ADD CONSTRAINT ... case_expenses` returns nothing; only 052's pre-check comments mention case_expenses-adjacent columns).
- **Why:** A typo or hostile REST call can store a negative office-expense amount. The 081 header frames case_expenses as the per-case bookkeeping ledger ('I paid the appraiser 1,200 ₪'); a negative row silently skews any per-case cost total computed from it. Inconsistent with the explicit money-constraint discipline established in 052.
- **Fix:** Add `ALTER TABLE public.case_expenses ADD CONSTRAINT case_expenses_amount_nn CHECK (amount IS NULL OR amount >= 0);` in a new migration (run the same pre-check `SELECT id, amount FROM case_expenses WHERE amount < 0;` first). Optionally add an `expense_date <= CURRENT_DATE`-style bound to match the borrowers birth_date discipline if future-dated expenses are not expected.
- **Verify:** Substantiated by the code. case_expenses.amount is NUMERIC(15,2) with no CHECK constraint (supabase/migrations/081_case_expenses.sql:21). The constraint migration the finding references is actually named 052_data_constraints.sql (finding cites "052_money_constraints.sql" — minor cosmetic title error, immaterial) and it adds `>= 0` guards to exactly the columns listed: cases.property_value/requested_mortgage_amount/equity, case_financials.fee_amount/expected_income, borrower_incomes.amount_monthly, borrower_obligations.monthly_payment/loan_amount (052:28-53). 052 ran before 081 created the table. An exhaustive `ADD CONSTRAINT` grep across all migrations confirms no later migration backfills a CHECK on case_expenses; the only later ALTER (101_add_case_expense_receipts.sql) adds receipt_* columns, not a constraint. So the inconsistency with 052's money-constraint discipline is real and the DB would accept a negative value.

Severity correctly stated as Low (not over/under-stated). The finding's "a typo ... can store a negative amount" is largely mitigated at the application layer: the only value-bearing write path, updateExpenseFieldAction (src/features/case-expenses/actions/update-expense-field.ts), validates amount through optionalCurrency (src/lib/validators/form-primitives.ts:110-124) which enforces .int().min(0).max(100M) before any DB write; createEmptyExpenseAction inserts amount as NULL; backup-restore only round-trips already-validated rows. The remaining gap is the "hostile REST call" — an authenticated edit-capable actor POSTing a negative value straight to PostgREST, which RLS gates by identity but not by value range. That is a genuine but minor defence-in-depth/consistency gap, exactly matching a Low rating. The suggested fix (new migration adding `CHECK (amount IS NULL OR amount >= 0)` with the same pre-check SELECT) is appropriate.

Key files read: supabase/migrations/081_case_expenses.sql, 052_data_constraints.sql, 101_add_case_expense_receipts.sql; src/features/case-expenses/{schemas/expense.schema.ts, actions/update-expense-field.ts, actions/create-empty-expense.ts, domain/editable-expense-fields.ts, services/case-expenses.service.ts}; src/lib/validators/form-primitives.ts; src/features/backup/services/backup-snapshot.service.ts.

#### `DB-003` — FK-index migrations 042/051 use bare CREATE INDEX on populated tables (no CONCURRENTLY)
- **File:** supabase/migrations/051_fk_indexes.sql:18-59
- **Evidence:** 051 (and 042) create ~20 indexes with plain `CREATE INDEX IF NOT EXISTS` on already-populated tables (cases, leads, borrowers, borrower_incomes, borrower_obligations, documents, tasks, notifications, audit_log) and even DROP+recreate idx_tasks_due. The project standard (CLAUDE.md Database section) requires `CREATE INDEX CONCURRENTLY` on a populated table, each in its own statement-only migration, precisely because a bare CREATE INDEX takes a write lock (ShareLock) that blocks writes for the build duration. None of these use CONCURRENTLY.
- **Why:** Documented discipline violation. Practically negligible at this data scale (~80 cases / a few hundred child rows builds in milliseconds), but it is the exact anti-pattern the standard calls out, and the same pattern at larger scale would stall live writes during deploy. The deploy path runs migrations pre-swap, so the lock would be held against the still-live previous build.
- **Fix:** For consistency with the documented rule, future FK-index additions should use `CREATE INDEX CONCURRENTLY` in single-statement migrations (which cannot run inside the migration runner's wrapping transaction — hence one index per file). Given these are already applied to prod, no action is needed retroactively beyond honoring the rule going forward; flagging for the standard's sake.
- **Verify:** Substantiated by direct reading. supabase/migrations/051_fk_indexes.sql lines 17-59 contain ~20 bare `CREATE INDEX IF NOT EXISTS` on already-populated tables (leads, borrowers, documents, borrower_incomes, borrower_obligations, tasks, notifications, audit_log) plus a DROP+plain-CREATE of idx_tasks_due (lines 57-59) — all in one multi-statement file, none CONCURRENTLY. supabase/migrations/042_cases_fk_indexes.sql lines 8-11 likewise use 4 bare CREATE INDEX on cases. CLAUDE.md lines 158-164 explicitly mandate CREATE INDEX CONCURRENTLY on populated tables, each alone in its own statement-only migration, citing the write-lock-stalls-traffic rationale the finding describes. Grep confirms CREATE INDEX CONCURRENTLY appears zero times anywhere in supabase/migrations (129 CREATE INDEX across 37 files); the finding correctly scopes blame to 042/051 because those are retrofit indexes added well after their tables (migs 005-028) were created and populated — early table-creation migs build on empty tables where CONCURRENTLY is moot. So this is a genuine, exact instance of the documented anti-pattern. Severity Low is correct and honestly stated: at ~80 cases / a few hundred child rows builds complete in milliseconds, indexes are already applied to prod (no retroactive action), and impact is a deploy-time write-lock window that is sub-second at this scale. Could arguably be informational, but Low is defensible and not overstated. No attempt to refute succeeded.

#### `DB-004` — case_financials money columns declared as unbounded NUMERIC (no precision/scale), unlike cases
- **File:** supabase/migrations/025_case_financials_table.sql:21-22
- **Evidence:** case_financials.fee_amount and case_financials.expected_income are declared as bare `NUMERIC` (arbitrary precision), whereas the columns they were migrated FROM on cases were `NUMERIC(15,2)` (006_cases.sql:21-22) and every sibling money column uses NUMERIC(15,2). The non-negative CHECK was later added in 052, but the precision/scale inconsistency remains.
- **Why:** Functionally harmless (Postgres stores either correctly), but it allows storing fractional-agora noise (e.g. 1200.12345) and is inconsistent with the rest of the money schema, which can surface as off-by-rounding display differences between the financials block and other monetary fields in PDFs/exports.
- **Fix:** For consistency, `ALTER TABLE public.case_financials ALTER COLUMN fee_amount TYPE NUMERIC(15,2), ALTER COLUMN expected_income TYPE NUMERIC(15,2);` in a new migration (verify no existing values exceed 13 integer digits first). Low priority — cosmetic/consistency only.
- **Verify:** All factual claims verified in code. case_financials.fee_amount/expected_income are bare NUMERIC (025_case_financials_table.sql:21-22), whereas they were migrated from NUMERIC(15,2) on cases (006_cases.sql:22-23) and every sibling money column is NUMERIC(15,2) (grep confirms: cases.*, borrower_incomes.amount_monthly, borrower_obligations.loan_amount/monthly_payment, case_expenses.amount, case_properties.*, case_payouts.amount). The non-negative CHECK was indeed added later — in 052_data_constraints.sql:37-41 (the finding mis-names the file as 052_case_financials_nonneg_check.sql, but migration number 052 and substance are correct). No subsequent migration ever altered the column TYPE (grep for ALTER COLUMN ... fee_amount/expected_income and TYPE NUMERIC found nothing), so the precision/scale inconsistency persists.

Severity stays Low (the finding self-labels cosmetic/consistency), and the stated harm is largely neutralized in practice: every write path validates through the Zod optionalCurrency primitive (src/lib/validators/form-primitives.ts:110-124) which both Math.trunc()s (line 115) and enforces .int() (line 119), so fee_amount/expected_income reach the DB as integers. The full edit form (case.schema.ts:41-42) and the dedicated inline action (update-case-fee-amount.ts:36, reusing CaseFormShape.shape.fee_amount) both go through it; upsert_case_financials receives already-truncated values. So the 'fractional-agora noise / PDF rounding drift' scenario cannot occur via the application. The issue is real but purely schema-hygiene / defense-in-depth, no functional impact. Suggested fix (ALTER COLUMN ... TYPE NUMERIC(15,2)) is sound and low-risk.

### Refuted (1)

- `AUTH-02` save_borrower_for_case RPC (mig 064, still effective) keeps the legacy assigned_advisor-only scope check, excluding associated advisors — REFUTED. The finding's premise — that mig 064's non-_full save_borrower_for_case (9-arg) is "still effective" with the legacy assigned_advisor-only scope check — is false. Migration 138 (supabase/migrations/138_drop_dead_055_rpcs.sql:28-30) explicitly DROP FUNCTION IF EXISTS public.save_borrower_for_case(UUID,UUID,TEXT,TEXT,TEXT,TEXT,TEXT,DATE,BOOLEAN), the exact signature defined at 064:149-159; its header (138:13-16) states the bare version "has no app or DB caller" and was superseded by save_borrower_for_case_full. No migration after 138 (verified through 195) re-creates it — every later reference to the name is to _full (123 redefines _full, 065 defines _full, 119 just comments). So as of the current schema the function does not exist. Migration 190 (which the finding itself cites) repointed save_borrower_for_case_full, update_borrower_in_case, and income/obligation deletes at _assert_can_edit_case (190:57,157,346,393); it did not "leave a sibling on the legacy check" because that sibling had already been dropped in 138 — nothing to repoint. App code calls only save_borrower_for_case_full (borrowers.service.ts:230); a regex for save_borrower_for_case not followed by _full across src/ returns zero matches. The suggested fix (DROP the dead overload) was already done ~52 migrations ago. The "latent landmine / future rpc() call" rationale collapses: a POST /rpc/save_borrower_for_case 9-arg would 404 since the function is gone. The reviewer read mig 064 as current schema without tracing the chain forward to the 138 drop. Severity = invalid: the described code does not exist in the deployed schema.


## Round 19 — final DB state — migrations 111-latest + tests/seeds/types

**raw 16 -> confirmed 16 / refuted 0  (High 3, Medium 6, Low 7)**

### High (3)

#### `DB-BACKUP-1` — Backup/restore allowlist omits 5 persistent business tables (case_payouts, case_properties, case_associated_advisors, case_comments, checklist_templates) — silent data loss on restore
- **File:** src/features/backup/services/backup-snapshot.service.ts:14-47; supabase/migrations/193_backup_restore_includes_expenses_checklist_comments.sql:34-47
- **Evidence:** Migration 193 was written explicitly to fix 'persistent business tables in neither the backup writer's allowlist nor the restore RPC' (silent-data-loss class, same as mig 115's scenario fix), and added case_expenses/case_checklist_items/task_comments/case_bank_statuses. But it MISSED other durable tables created earlier in this very range: case_comments (mig 107 — team thread, durable), case_associated_advisors (mig 146 — access-control links), case_properties (mig 156 — additional case properties, soft-delete), case_payouts (mig 186 — manager-only commissions/salaries feeding the NET-fee statistic), and checklist_templates (mig 189 — manager-authored custom templates). Verified absent: `grep -E "'(case_comments|case_payouts|case_properties|case_associated_advisors)'" backup-snapshot.service.ts` => NOT FOUND; none appear in restore_backup_snapshot v_tables (mig 193:34-42). A disaster-recovery restore would silently drop all five while reporting success — financial payout records, additional-property records, the entire case-comment history, every associated-advisor link, and custom checklists are lost. The parity test (backup-restore-allowlist.test.ts) only checks backup<->restore lockstep, so it stays green while both lists are incomplete.
- **Why:** This is the exact silent-data-loss failure mode the project already paid to fix twice (migs 115 and 193). For an Israeli mortgage office under a legal retention hold (mig 173), losing financial payout breakdowns, case property records, and the team's case-comment audit trail on the one operation meant to recover them (restore) is a data-integrity and compliance failure. case_payouts loss also silently corrupts the statistics NET-fee figure post-restore.
- **Fix:** Add case_comments, case_associated_advisors, case_properties, case_payouts, and checklist_templates to BACKUP_TABLES and to restore_backup_snapshot's v_tables (FK-parent-first; the soft-delete ones — case_properties, case_payouts, case_comments has none — to v_tables_with_deleted_at as appropriate). This requires DB-DRIFT-1 to be fixed first (the `satisfies keyof Database['public']['Tables']` clause rejects these names today). Strengthen backup-restore-allowlist.test.ts to also assert coverage of every non-ephemeral table, not just backup<->restore parity, so future tables can't silently fall out.
- **Verify:** Verified directly against the code. (1) BACKUP_TABLES in src/features/backup/services/backup-snapshot.service.ts:14-47 contains 32 tables; grep for case_comments|case_payouts|case_properties|case_associated_advisors|checklist_templates returns 0 matches — all 5 are absent from the backup writer. (2) Migration 193's restore_backup_snapshot v_tables array (lines 34-42) likewise contains 0 of the 5; grep confirms. So a backup never writes them AND a restore never reads them. (3) All 5 are genuine durable business tables, not phantoms: 107_case_comments.sql (team thread, hard-delete, ON DELETE CASCADE from cases), 146_case_associated_advisors.sql (access-control links, hard-delete), 156_case_properties.sql (additional properties, soft-delete via deleted_at + soft_delete_case_property RPC), 186_case_payouts.sql (manager-only commissions, soft-delete, explicitly feeds the statistics NET-fee per its own COMMENT and mig 187_statistics_net_fee), 189_checklist_templates.sql (manager-authored custom templates). All are actively used by live feature services/actions (17 files matched). (4) Restore is driven solely by the restore_backup_snapshot RPC (restore.service.ts) which iterates only its hardcoded v_tables — no compensating path; the BackupSnapshotSchema envelope is permissive (data: record of string->unknown[]) and does not require complete table coverage, so an incomplete snapshot passes silently. (5) The parity test backup-restore-allowlist.test.ts only asserts backup<->restore lockstep (every backed-up table restorable and vice versa) plus a scenario-table regression lock; it never asserts coverage of the full durable-table set, so omitting all 5 from BOTH lists keeps it green — exactly as the finding states. (6) DB-DRIFT-1 prerequisite confirmed: none of the 5 appear in src/types/database.ts (0 matches), so the `as const satisfies readonly (keyof Database['public']['Tables'])[]` clause would currently reject adding them. This is the identical silent-data-loss class the project fixed twice before (migs 115 and 193). Severity High is justified: silent loss of financial payout records, the entire case-comment trail, associated-advisor access links, additional-property records, and custom checklist templates on the one operation meant to recover data, for an office under a legal retention hold (mig 173), plus statistics NET-fee corruption post-restore. Minor nuance not affecting validity: the 12 is_system checklist_templates re-seed via mig 189 ON CONFLICT, but manager-created custom templates are still lost; the soft-delete tables (case_properties, case_payouts) also need v_tables_with_deleted_at entries per the suggested fix.

#### `INTAKE-BUNDLE-1` — Stale root INTAKE_MIGRATIONS_151-153.sql re-opens the anon door on submit_public_intake (regresses mig 166/175)
- **File:** INTAKE_MIGRATIONS_151-153.sql:36-117,389-471
- **Evidence:** The untracked root bundle (dated Jun 8, file mtime 2026-06-08, NOT gitignored so it appears in `git status`) is explicitly meant to be pasted into the Supabase SQL Editor and Run ('Paste ALL of this into the editor and Run'). It re-creates the ORIGINAL 1-arg `CREATE OR REPLACE FUNCTION public.submit_public_intake(p_payload jsonb)` and ends with `GRANT EXECUTE ON FUNCTION public.submit_public_intake(jsonb) TO anon, authenticated;` (lines 117 and 471). But the live function evolved: migration 175 (175_intake_source_basis.sql:27-34,157-159) drops the old overload and defines the 4-arg `submit_public_intake(jsonb, text, text, text)` which is REVOKE'd from anon/authenticated and GRANT'd only to service_role; migration 166 (166_lock_public_intake_service_role.sql:28-30) did the same lockdown precisely because 'ANYONE holding the public anon key... can call the RPC DIRECTLY via PostgREST bypassing every application-layer defense'. Because a 1-arg signature differs from the 4-arg one, re-running the bundle does NOT replace the hardened function — it ADDS a second, anon-grantable overload alongside it. A PostgREST anon caller posting {"p_payload": {...}} resolves to the 1-arg overload and bypasses the service_role lockdown.
- **Why:** Restores the exact lead-flooding / admin-notification-spam vulnerability migration 166 closed, AND the bundle's 1-arg body skips the mandatory affirmative-consent check and legal-basis recording added in migrations 154/175 (no metadata.consent / privacy_notice written) — a privacy/compliance regression. The bundle header even says it targets staging (eyujzasggzjocsxakkoi), but it is loose at repo root with no guard against being run anywhere.
- **Fix:** Delete INTAKE_MIGRATIONS_151-153.sql from the repo root — migrations 151-153 are long applied and superseded by 154/166/175, so the bundle is a pure liability. If a paste-helper is still wanted, regenerate it from the CURRENT effective function bodies (4-arg signature, service_role-only grant) and keep it out of the repo root / under .gitignore.
- **Verify:** Every claim verified against the code. (1) INTAKE_MIGRATIONS_151-153.sql is untracked AND not gitignored: `git status` shows `?? INTAKE_MIGRATIONS_151-153.sql`, `git check-ignore -v` returns nonzero, `git ls-files --error-unmatch` fails. (2) The bundle's header literally says "Paste ALL of this into the editor and Run" and is "Idempotent ... safe to re-run", targeting staging eyujzasggzjocsxakkoi with NO guard against being run on prod. (3) It re-creates the original 1-arg `CREATE OR REPLACE FUNCTION public.submit_public_intake(p_payload jsonb)` twice (lines 36 and 389) and both sections end with `GRANT EXECUTE ON FUNCTION public.submit_public_intake(jsonb) TO anon, authenticated;` (lines 117 and 470-471). (4) The live function evolved away from this signature: mig 154 dropped the 1-arg and made it 3-arg; mig 166 (lines 28-30) revoked anon/authenticated and granted only service_role, with a header explicitly describing the anon-key-direct-PostgREST lead-flooding/admin-spam attack; mig 175 (lines 27-34, 157-159) dropped the 3-arg and created the 4-arg `(jsonb,text,text,text)`, again REVOKE FROM PUBLIC,anon,authenticated and GRANT TO service_role only. (5) Because the bundle's 1-arg signature differs from the live 4-arg signature, re-running the bundle's CREATE OR REPLACE does NOT replace the hardened function — Postgres treats different arg-lists as distinct overloads, so it ADDS an anon-grantable 1-arg overload alongside the locked 4-arg one; an anon PostgREST caller posting {"p_payload":{...}} reaches the 1-arg overload, bypassing the service_role lockdown that migs 166/175 deliberately established. (6) Compliance regression confirmed: grep for consent/privacy/p_policy/p_source/p_ip in the bundle returns nothing (exit 1), whereas mig 154/175 added a mandatory DB-enforced consent='true' check and a first-class metadata.consent / privacy_notice legal-basis record — the 1-arg body writes neither. Severity stays High as stated: although it is a latent operator-triggered regression (the deployed repo is currently safe; the hole only opens if someone runs the file), the file is a self-executing "Run this" playbook with no remaining legitimate purpose (migs 151-153 are long applied and superseded by 154/166/175) that re-opens an intentionally closed anon attack surface and drops a privacy control. Suggested fix (delete it; regenerate a paste-helper from the current 4-arg/service_role-only bodies and keep it out of repo root) is appropriate.

#### `DRIFT-BACKUP-PAYOUTS-1` — case_payouts missing from database.ts blocks its inclusion in BACKUP_TABLES -> silently dropped on restore (data-loss class mig 193 fixes)
- **File:** src/types/database.ts:3655-3833 (Tables block has no case_payouts); src/features/backup/services/backup-snapshot.service.ts:14-47
- **Evidence:** case_payouts (migration 186_case_payouts.sql) is a persistent, soft-deletable business table holding financial data (per-case commissions/salaries paid out of the fee, feeds the NET fee in statistics). `grep checklist_templates|case_payouts src/types/database.ts` returns ZERO matches — the table is entirely absent from the generated Tables type. BACKUP_TABLES (backup-snapshot.service.ts:14-47) is constrained `as const satisfies readonly (keyof Database['public']['Tables'])[]`, so `case_payouts` CANNOT be added to the backup allowlist without first existing in database.ts (it would be a type error). It is therefore absent from BACKUP_TABLES, and also absent from the restore RPC v_tables (193_backup_restore_includes_expenses_checklist_comments.sql:34-42). The parity test (backup-restore-allowlist.test.ts) only checks the two lists agree with EACH OTHER — since neither contains case_payouts, the test passes and gives false confidence.
- **Why:** A disaster-recovery restore silently drops every case payout row while reporting success — the exact silent-data-loss failure mode migrations 115 and 193 were created to eliminate, now reintroduced for the case_payouts financial table specifically because the type drift blocks the natural fix.
- **Fix:** Regenerate src/types/database.ts so case_payouts (and checklist_templates) appear in Tables, then (a) add 'case_payouts' to BACKUP_TABLES with case_payouts + deleted_at handling, (b) add it to v_tables and v_tables_with_deleted_at in a new restore_backup_snapshot migration in correct FK order (after cases), and (c) extend backup-restore-allowlist.test.ts with an explicit assertion that case_payouts is backed up AND restorable (like the migration-115 scenario regression lock).
- **Verify:** Confirmed by reading the actual code. (1) case_payouts is genuinely absent from src/types/database.ts — grep returns 0 matches and the Tables block ends at line 3650-3651 before Views/Functions; the service file even documents the drift (case-payouts.service.ts:8-12: "case_payouts isn't in the generated Database types yet, so the read goes through an untyped handle"). (2) BACKUP_TABLES (backup-snapshot.service.ts:14-47) is constrained `as const satisfies readonly (keyof Database['public']['Tables'])[]`, so case_payouts cannot be added without a TS error — and it is not in the array. (3) The restore RPC's v_tables in migration 193 (the highest-numbered restore_backup_snapshot definition; 194/195 do not touch it) does NOT contain case_payouts. (4) The parity test (backup-restore-allowlist.test.ts) only asserts the two lists agree with each other and includes a regression lock for the mig-115 scenario tables — since neither list contains case_payouts, every assertion passes and gives false confidence. (5) case_payouts (migration 186) is a persistent, soft-deletable (deleted_at), is_admin-gated financial table that feeds the NET fee in statistics (financial-summary.tsx:31-34: netFee = executed_fee_total - executed_payout_total). (6) buildBackupSnapshot is the SOLE backup writer (used by run-backup.ts + cron route); there is no alternate "dump everything" path. Net effect: a disaster-recovery restore silently drops every case payout row while reporting success — the exact silent-data-loss class migrations 115 and 193 exist to prevent, now re-introduced for case_payouts specifically because the type drift blocks the natural fix. High severity is appropriate: financial data, silent/success-reporting failure mode, manifests at restore time (worst recoverability) — consistent with the prior identical findings rated High. The only mitigating nuance (it surfaces only during a restore, not normal operation) does not lower severity because restore is the disaster-recovery moment where the loss is irreversible.

### Medium (6)

#### `DB-DRIFT-1` — Generated src/types/database.ts is stale: ~9 tables created in 107-189 are entirely missing, forcing untyped `as unknown as SupabaseClient` casts across features
- **File:** src/types/database.ts (Tables block); src/features/case-payouts/services/case-payouts.service.ts:8-33
- **Evidence:** Extracting the Tables keys from database.ts shows the newest tables present are task_attachments/task_assignment_history/task_comments — but case_comments (107), case_comment_mentions is correctly N/A, case_associated_advisors (146), push_subscriptions (150), case_properties (156), client_email_log (163), case_payouts (186), checklist_templates (189), erasure_orphan_log (144), and schema_version (143) are ALL absent. Feature code documents the workaround: case-payouts.service.ts:8-10 'case_payouts isn't in the generated Database types yet, so the read goes through an untyped handle — same pattern as case-properties / case-comments', then line 20 `const db = supabase as unknown as SupabaseClient` and line 33 `as unknown as CasePayoutRow[]`. create-intake-lead.ts:74-81 carries a similar minimal RPC cast ('database.ts predates the p_source param').
- **Why:** The DB standard mandates 'All queries through Supabase typed client / Use generated types in src/types/database.ts'. Three+ features now bypass the typed client with double-casts, losing compile-time column/shape checking on financial (payouts) and PII (properties, comments) reads/writes — a regression-magnet. It is also the ROOT CAUSE that makes DB-BACKUP-1 unfixable in place: the `satisfies readonly (keyof Database['public']['Tables'])[]` guard on BACKUP_TABLES would reject the missing table names.
- **Fix:** Regenerate database.ts from the live schema (supabase gen types) so all tables through mig 195 are typed, then remove the `as unknown as SupabaseClient` / `as unknown as Row[]` casts in case-payouts / case-properties / case-comments services and the intake RPC cast. Add a CI check (or schema-version-tied reminder) that fails when a migration adds a table absent from database.ts.
- **Verify:** Fully verified against the code; could not refute. (1) database.ts is stale: the newest tables in the Tables block (src/types/database.ts ~lines 3303-3464) are task_attachments / task_assignment_history / task_comments; a grep for case_comments, case_associated_advisors, push_subscriptions, case_properties, client_email_log, case_payouts, checklist_templates, erasure_orphan_log, schema_version returns ZERO matches — all absent. (2) Every named table has a real CREATE TABLE in migrations: 107 case_comments, 146 case_associated_advisors, 150 push_subscriptions, 156 case_properties, 163 client_email_log, 186 case_payouts, 189 checklist_templates, 144 erasure_orphan_log, 143 schema_version (chain runs to 195). (3) The workaround is exact: case-payouts.service.ts:8-12 carries the quoted comment, line 20 'const db = supabase as unknown as SupabaseClient', line 33 'as unknown as CasePayoutRow[]'; create-intake-lead.ts:74-81 (actual path src/features/intake/services/) carries the 'database.ts predates the p_source param' RPC cast. (4) The parenthetical is accurate: migration 108 case_comment_mentions creates only a trigger/function, no table, so its absence is correctly N/A. Scope is in fact BROADER than stated — a repo-wide grep shows the same double-cast across case-comments/case-payouts (service+actions), case-properties, client-email-log, case-advisors, case-block-preferences, push-subscriptions, checklist-templates (5 files), templates, team, leads, erase-case-files — strengthening the finding. Severity stays Medium: it genuinely violates the 'use generated types' DB standard and removes compile-time column checks on financial (case_payouts) and PII (case_properties/comments) paths, but each site mitigates with an explicit column list plus a hand-written Row type, so call-site shapes are typed shadows over an untyped handle — debt/regression-risk, not a runtime bug. Reviewer neither over- nor under-stated it.

#### `SQLTEST-1` — No SQL test for the canonical can_edit_case write authz on case-child tables (migs 190 + 192) — associated-advisor writes are untested
- **File:** supabase/tests/ (missing) — covers supabase/migrations/190_canonical_can_edit_case_borrower_income_writes.sql and 192_canonical_can_edit_case_banks_expenses_documents.sql
- **Evidence:** A grep of supabase/tests/** for can_edit_case / case_banks / case_expenses / borrower_incomes / borrower_obligations / documents_insert / update_borrower_in_case / save_borrower_for_case / add_empty_borrower returns ONLY retention_file_gating_test.sql, and that file only INSERTs case_expenses/documents rows as a superuser fixture for the purge gate — it never exercises any RLS write policy or RPC. Migs 190 and 192 repointed the borrower / income / obligation / case_banks / case_expenses / documents / case-documents-storage / document-tombstone write paths from the LEGACY guard (assigned_advisor_id = auth.uid() OR edit_any_case) onto public.can_edit_case / _assert_can_edit_case so an ASSOCIATED advisor (case_associated_advisors, mig 147) can write. The ONLY test that proves associated-advisor writes work at all is case_properties_authority_test.sql, which covers case_properties (mig 179) — NOT the 8 tables/RPCs touched by 190/192. db-tests.yml runs on every migration PR, so these authz-critical migrations ship with CI green but zero behavioral coverage.
- **Why:** These are the exact authz-critical write paths the base invariant names. A regression that (a) drops the associated-advisor OR-branch silently re-locks-out a legitimate role (broken core flow — the very bug 190/192 fixed), or (b) accidentally widens the predicate (e.g. a future edit that lets a view-only secretary write) would pass CI undetected. The write boundary on borrowers/incomes/banks/expenses/documents is the product's core data-integrity boundary and is currently unverified.
- **Fix:** Add a pgTAP file (mirror case_properties_authority_test.sql's fixtures: manager admin + advisor_a responsible + advisor_b associated + advisor_c unrelated junior) asserting, per table/RPC: associated advisor B CAN insert/update/soft-delete a case_bank, case_expense, document (with upload_document granted), borrower_income, borrower_obligation, and CAN run add_empty_borrower_to_case / update_borrower_in_case / save_borrower_for_case_full / soft_delete_case_expense / soft_delete_document_with_tombstone (lives_ok / FOUND), while unrelated advisor C is denied (throws_ok 42501). Include the document storage.objects case_docs_insert path and a cross-case isolation case (entity on case A not writable via can_edit_case(B)).
- **Verify:** Verified against the code. Migs 190 (C:\Users\shh92\Projects\first-crm\supabase\migrations\190_canonical_can_edit_case_borrower_income_writes.sql) and 192 (...\192_canonical_can_edit_case_banks_expenses_documents.sql) do repoint 8 write paths onto public.can_edit_case / _assert_can_edit_case to admit associated advisors: update_borrower_in_case, save_borrower_for_case_full, add_empty_borrower_to_case (new), soft_delete_borrower_income/_obligation, borrower_incomes/borrower_obligations INSERT+UPDATE RLS (190); case_banks/case_expenses/documents INSERT+UPDATE RLS, soft_delete_case_expense, case_docs_insert/update storage policies, tombstones_insert + soft_delete_document_with_tombstone (192). can_edit_case is defined in mig 147 and is the canonical guard carrying the associated-advisor branch.

Coverage gap confirmed: a Grep of supabase/tests/** for all named terms (can_edit_case, case_banks, case_expenses, borrower_incomes, borrower_obligations, documents_insert, update_borrower_in_case, save_borrower_for_case, add_empty_borrower) hits ONLY retention_file_gating_test.sql, which runs as superuser and merely INSERTs case_expenses/documents fixtures for the purge gate — it exercises no RLS write policy or RPC. The only test that uses case_associated_advisors is case_properties_authority_test.sql, which covers case_properties (mig 179), NOT any 190/192 path. The broad rls_permissions_test.sql covers cases-visibility/financials/leads/task_comments only, with two isolated juniors and no association row, so it never tests an associated-advisor write. case_create_advisor_test.sql (mig 176) and case_trusted_columns_test.sql (mig 178) contain none of the 190/192 terms. .github/workflows/db-tests.yml runs on every supabase/migrations/** PR (boots stack, applies all migs, runs pgTAP), so 190/192 shipped CI-green with zero behavioral coverage of the paths they changed.

Adjusted High->Medium: this is a missing-test finding, not a live defect. The migrations are widening-by-superset and there is no current exploitable bug; the stated impact is hypothetical future regression detection (re-lockout of associated advisors, or accidental widening). That is real and worth a pgTAP file but does not meet High (no present security/data-integrity break). Suggested fix (mirror case_properties_authority_test.sql fixtures: manager + responsible advisor_a + associated advisor_b + unrelated advisor_c; assert B can write each table/RPC and C is denied 42501, plus cross-case isolation and the storage case_docs_insert path) is appropriate.

#### `SQLTEST-2` — No SQL test for case-scoped scenario write authz (mig 195) — view-only-on-case scenario CRUD is unverified
- **File:** supabase/tests/ (missing) — covers supabase/migrations/195_scenario_writes_require_can_edit_case.sql
- **Evidence:** grep of supabase/tests/** for mortgage_scenario / save_mortgage_scenario / soft_delete_scenario / scenario_tracks / use_simulators returns nothing. Mig 195 fixed a real authz hole (R15 SIM-PERSIST-1): the mortgage_scenarios / scenario_tracks INSERT+UPDATE policies and the save_mortgage_scenario / soft_delete_scenario RPCs authorized case-scoped scenarios on can_VIEW_case (a read perm), so a user with use_simulators + view_all_cases but no edit rights (e.g. secretary on an unassigned case) could create/edit/delete scenarios inside any case they could only view. The fix swaps to can_edit_case on the case-scoped branch while preserving the case-less owner branch (case_id IS NULL AND created_by = actor).
- **Why:** This is a confirmed, freshly-patched authz escalation. With no test, a regression that reverts the can_view_case→can_edit_case swap on any of the four write paths (two RLS policies + two RPCs) silently reopens the hole, and CI on the next scenario migration would stay green.
- **Fix:** Add a pgTAP file: seed a case owned by advisor_a, a secretary/viewer with use_simulators + view_all_cases (can view, cannot edit), and an owner. Assert: viewer is DENIED save_mortgage_scenario / soft_delete_scenario for that case's scenario and DENIED direct INSERT/UPDATE on mortgage_scenarios + scenario_tracks (throws_ok 42501); advisor_a (responsible) and an associated advisor ARE allowed; the case-LESS owner branch (case_id NULL, created_by=self) still works (lives_ok) and is NOT editable by another user (cross-owner isolation).
- **Verify:** Every factual claim in the finding checks out against the code.

(1) Mig 195 is real and fresh, and fixes a genuine authz hole exactly as described. Read supabase/migrations/195_scenario_writes_require_can_edit_case.sql: the four case-scoped write paths (mortgage_scenarios_insert/update RLS, scenario_tracks_insert/update RLS, soft_delete_scenario RPC, save_mortgage_scenario RPC) all use can_edit_case on the case-scoped branch while preserving the case-less owner branch (case_id IS NULL AND created_by = auth.uid()). The SELECT policies are intentionally left on can_view_case.

(2) The pre-fix state used can_view_case (a READ perm). Grep of mig 093_mortgage_scenarios.sql shows 9 can_view_case occurrences on the case-scoped write branches (RLS insert/update + soft_delete), and 097_edit_mortgage_scenario_rpc.sql shows 2 (save RPC edit + insert paths). So the swap is real and freshly patched.

(3) The escalation is real. can_view_case (mig 147, lines 70-75) grants on view_all_cases; can_edit_case (lines 94-97) requires edit_any_case OR edit_own_case (responsible/associated advisor). A secretary with use_simulators + view_all_cases but no edit rights passed can_view_case → could create/edit/delete case-scoped scenarios. The fix closes this.

(4) No test exists. Grep of supabase/tests/** for mortgage_scenario/save_mortgage_scenario/soft_delete_scenario/scenario_tracks/use_simulators returns nothing. The 12 .sql test files cover RLS/permissions, retention, case_properties, leads, etc. — none touch scenarios. The only scenario tests in the repo (src/features/simulators/domain/scenario-stress.test.ts, scenario-threshold.test.ts) are pure domain-math unit tests, not authz tests.

(5) The regression-detection concern is valid. .github/workflows/db-tests.yml runs `supabase test db` on any PR touching supabase/migrations/** or supabase/tests/**. So a future scenario migration that reverts the swap would trigger CI but find no guarding test — CI stays green, hole silently reopens. The suggested pgTAP test is well-formed and matches the existing test pattern (mk_user/login_as/throws_ok 42501/lives_ok) used in case_properties_authority_test.sql.

Severity adjustment High→Medium: this is a test-coverage gap, not a live vulnerability — the underlying authz hole IS already patched in mig 195. The finding's own 'why it matters' frames it as protecting against a future regression, which is a real but second-order risk. A missing regression test for a freshly-patched, well-isolated invariant (four write paths in one migration) is solid Medium territory, not High. The fix is cheap and the invariant is security-critical, which keeps it above Low.

#### `SQLTEST-4` — No SQL test for restore_backup_snapshot completeness / backup<->restore table parity (mig 193)
- **File:** supabase/tests/ (missing) — covers supabase/migrations/193_backup_restore_includes_expenses_checklist_comments.sql
- **Evidence:** No pgTAP file references restore_backup_snapshot or BACKUP_TABLES. Mig 193's header states the only parity guard is the TS test backup-restore-allowlist.test.ts (keeps BACKUP_TABLES in lockstep with the restore RPC's v_tables array). That JS test cannot see the actual SQL function body — it asserts against a hand-maintained list, so the two can drift from the live RPC. Mig 193 itself was created because four persistent tables (case_expenses, case_checklist_items, task_comments, case_bank_statuses) were silently dropped on restore while the restore reported success — the same silent-data-loss class as mig 115.
- **Why:** Disaster-recovery restore is a data-loss-critical path with a history of silently omitting tables. The v_tables / v_tables_with_deleted_at arrays are pure SQL and have no DB-level test; a future table addition that forgets the restore array would silently drop that table on restore again, undetected by SQL CI.
- **Fix:** Add a pgTAP test that round-trips a snapshot: seed one row in each of the four newly-added tables (+ a representative soft-deleted row), call restore_backup_snapshot on a snapshot JSON containing them, and assert the returned per-table counts are non-zero and the rows landed with deleted_at stripped for the with_deleted_at tables. Optionally assert every table in restore's v_tables exists in pg_class (no typo'd table name).
- **Verify:** Confirmed by reading the code. (1) No pgTAP test references restore_backup_snapshot or BACKUP_TABLES — grep over supabase/tests/ for restore_backup_snapshot, BACKUP_TABLES, v_tables, jsonb_populate_recordset, restoring_backup returned zero matches. The two pgTAP files that touch the four tables (rls_permissions_test.sql, retention_file_gating_test.sql) use them only for RLS visibility and retention-purge gating, not backup/restore. (2) The sole parity guard is backup-restore-allowlist.test.ts, which its own docstring (lines 13-16) states is a STATIC text guard that 'deliberately does NOT exercise the SQL itself' — it reads BACKUP_TABLES and v_tables as text, so it cannot detect a typo'd/non-existent table name in v_tables, verify FK ordering/NOT NULL, confirm rows actually land, or confirm deleted_at is stripped. The reviewer's claim that the JS test can't see the SQL function body is accurate. (3) The silent-data-loss history is real: mig 193's header (lines 5-13) and mig 115 both document prior incidents (scenario tables in 115; the four tables case_expenses/case_checklist_items/task_comments/case_bank_statuses in 193) silently dropped on restore while reporting success via ON CONFLICT DO NOTHING + per-table count return. (4) The suggested round-trip fix is feasible and would be CI-enforced: bootstrap_test.sql already calls a SECURITY DEFINER function inside a BEGIN/ROLLBACK pgTAP transaction with seeded auth.users, and db-tests.yml runs `supabase test db` (all tests/*.sql) on PRs touching supabase/migrations/**. Severity Medium is correct as stated — this is a test-coverage gap, not a live defect (all four tables are currently present in both BACKUP_TABLES and v_tables, and case_expenses+task_comments are correctly in v_tables_with_deleted_at), but it sits on a disaster-recovery data-loss path with a documented two-time history of exactly this failure class that the existing static guard provably cannot fully cover.

#### `DRIFT-RPC-FUNCTIONS-1` — database.ts Functions block omits many shipped RPCs, forcing `as unknown as` casts that bypass type safety on authz-critical calls
- **File:** src/types/database.ts:3655-3833
- **Evidence:** The generated Functions block does not contain: can_edit_case (mig 147), can_view_task (mig 157/159), has_permission_for + list_task_mentionable_profiles (mig 188), can_view_case_for + can_view_task_for + list_case_mentionable_profiles (mig 194), add_empty_borrower_to_case (mig 190), soft_delete_document_with_tombstone (mig 192), soft_delete_case_payout (mig 186), and several older ones the code already calls (reassign_task, list_active_advisors, refund_rate_limit, current_user_active, revoke_user_sessions, record_backup_success, get_statistics_summary/monthly_trend, delete_failed_case, soft_delete_lead, soft_delete_case_property, admin_delete_member). Confirmed by grepping each name in database.ts (no matches) while they are all invoked in src (e.g. add-empty-borrower.ts:45-53, case-comments.service.ts:82-92, tasks/task-comments.service.ts:100). Each call site works around the missing type with an inline `supabase as unknown as { rpc(...) }` cast (add-empty-borrower.ts:43-50 comment: 'database.ts predates migration 190; minimal cast... Regenerate types to drop it').
- **Why:** Every cast site (notably the authz-sensitive mention/can_edit/borrower-add RPCs) is invisible to the compiler: a renamed param, dropped arg, or changed return shape in a future migration would not produce a tsc error — the failure surfaces only at runtime in production. The casts are spreading (one per new RPC) and normalize bypassing the typed client the DB invariant requires.
- **Fix:** Regenerate src/types/database.ts from the live schema (or `supabase gen types`) so all shipped RPCs are typed, then delete the inline `as unknown as { rpc... }` casts across borrowers/case-comments/tasks/case-payouts/intake services and call supabase.rpc(...) directly with compile-time-checked args.
- **Verify:** Verified against the code. database.ts Functions block (lines 3655-3833) genuinely omits every newer RPC the finding names: grep for can_edit_case, can_view_task, has_permission_for, list_task_mentionable_profiles, can_view_case_for, can_view_task_for, list_case_mentionable_profiles, add_empty_borrower_to_case, soft_delete_document_with_tombstone, soft_delete_case_payout returned ZERO matches, as did all 12 older names (reassign_task, list_active_advisors, refund_rate_limit, soft_delete_lead, etc.). The corresponding migrations (147,157,159,186,188,190,192,194) all exist and define these functions, so they are genuinely shipped. Every call site works around the missing type with an inline `supabase as unknown as { rpc(fn:'literal', args:{...}) }` cast: confirmed at permissions.ts:108-114 (can_edit_case), add-empty-borrower.ts:45-51, case-comments.service.ts:82-92, task-comments.service.ts:95-100, case-lookups.service.ts:38-43, reassign-task.ts:55. The in-code comments admit the cause ("database.ts predates migration X... Regenerate types to drop it"). The "Why it matters" claim is sound: the casts re-declare the signature locally, so tsc checks the hand-written shape, not the real schema — a future param rename/drop would NOT error at compile time. delete-payout.ts:21 is even worse: `supabase as unknown as SupabaseClient` (no Database generic) makes the fee-gated soft_delete_case_payout call fully `any`. One minor over-statement: the finding's parenthetical lists can_view_case among the mig-194 omissions, but base can_view_case IS present (line 3656) — only can_view_case_for/can_view_task_for/can_view_task are missing; this does not affect the core claim. Severity Medium is right: no active runtime bug today (casts work), but systemic type-safety erosion on authz-critical RPCs with future-facing silent-drift risk, defeating the project's typed-client DB invariant. Suggested fix (regenerate types, drop casts) is correct.

#### `DRIFT-SIGNATURE-INTAKE-1` — Typed submit_public_intake signature in database.ts is wrong vs. the live 4-arg RPC (missing p_source, params not optional-correct)
- **File:** src/types/database.ts:3808-3811; src/features/intake/services/create-intake-lead.ts:76-87
- **Evidence:** database.ts declares `submit_public_intake: { Args: { p_payload: Json; p_policy_version?: string; p_ip?: string }; Returns: string }`. The live function (175_intake_source_basis.sql:29-34) is `submit_public_intake(p_payload jsonb, p_policy_version text DEFAULT NULL, p_ip text DEFAULT NULL, p_source text DEFAULT 'public_intake')` — it has a 4th param p_source that the type omits, and the caller in create-intake-lead.ts:79,86 actually passes `p_source: action`. The caller is forced to cast (`supabase as unknown as { rpc(fn:'submit_public_intake', args:{...p_source:string}) }`) precisely because the generated type is stale.
- **Why:** The generated type actively lies about the contract for a security/legal-basis-bearing RPC: a caller trusting the type would omit p_source and silently get the 'public_intake' affirmative-consent path even for web_contact leads (over-asserting consent — the very thing mig 175 fixed). It hides the divergence behind a cast.
- **Fix:** Regenerate database.ts so submit_public_intake reflects the 4-arg signature with p_source, then drop the cast in create-intake-lead.ts and call supabase.rpc('submit_public_intake', { p_payload, p_policy_version, p_ip, p_source }) directly.
- **Verify:** Confirmed by reading the cited files and cross-referencing the migration.

1. src/types/database.ts:3808-3811 declares submit_public_intake with exactly 3 args: { p_payload: Json; p_policy_version?: string; p_ip?: string }, Returns: string. The p_source param is absent. Verbatim match to the finding.

2. The live RPC is 4-arg. supabase/migrations/175_intake_source_basis.sql:29-34 defines submit_public_intake(p_payload jsonb, p_policy_version text DEFAULT NULL, p_ip text DEFAULT NULL, p_source text DEFAULT 'public_intake'). I grepped all CREATE/DROP of this function: 175 is the latest definition; no migration after it re-defines the signature, so the 4-arg form is authoritative.

3. The caller is forced to cast precisely because the type is stale. create-intake-lead.ts:76-81 casts `supabase as unknown as { rpc(fn:'submit_public_intake', args:{ p_payload; p_policy_version; p_ip; p_source: string }) }` and passes p_source: action at line 86. The inline comment (lines 72-75) explicitly states "database.ts predates the p_source param... Regenerate the Supabase types to drop it." So the cast exists solely to work around the stale generated type.

4. The "why it matters" mechanism is real and security/legal-basis relevant. src/app/api/web-lead/route.ts:98 calls createIntakeLead(..., 'web_contact'), which flows to p_source. Migration 175:51,101-111 normalizes p_source='web_contact' to write metadata.privacy_notice and STRIP the synthesized consent flag (no agreed=true), whereas any other value takes the affirmative-consent 'public_intake' path (175:112-123). A future caller trusting the generated (3-arg) type would omit p_source and silently get the over-consent 'public_intake' path — the exact regression mig 175 fixed (175:4-17). So the type does materially misrepresent a consent-bearing contract.

Adversarial checks that did NOT refute: Returns: string is correct (Supabase maps uuid→string); the existing optional markers on p_policy_version/p_ip are correct vs the DEFAULT NULL params; the title's phrase "params not optional-correct" is slightly muddled but the load-bearing claim (p_source entirely missing) is accurate.

Severity: held at Medium rather than raised/lowered. There is no ACTIVE runtime bug today — both existing callers (the /check action via create-intake-lead and the web-lead route) already pass p_source correctly through the cast, so consent is recorded correctly in production. The defect is latent (a future caller) plus a documented type-fidelity violation on a legal-basis RPC. That latency argues against High; the consent-sensitivity and the fact the type "actively lies" argues against Low. Medium is appropriate.

Relevant files: C:\Users\shh92\Projects\first-crm\src\types\database.ts (3808-3811); C:\Users\shh92\Projects\first-crm\src\features\intake\services\create-intake-lead.ts (72-91); C:\Users\shh92\Projects\first-crm\supabase\migrations\175_intake_source_basis.sql (27-34, 51, 101-123); C:\Users\shh92\Projects\first-crm\src\app\api\web-lead\route.ts (98).

### Low (7)

#### `DB-IDX-1` — Non-concurrent CREATE INDEX on the populated tasks table (mig 159) violates the CONCURRENTLY-on-populated-tables standard
- **File:** supabase/migrations/159_task_assignment_attribution.sql:10-14
- **Evidence:** Migration 159 does `ALTER TABLE public.tasks ADD COLUMN ... assigned_by` (line 10-12) on the pre-existing, populated tasks table (created mig 009), then `CREATE INDEX IF NOT EXISTS idx_tasks_assigned_by ON public.tasks(assigned_by)` (line 14) — a plain, non-CONCURRENT index build that takes an ACCESS EXCLUSIVE / write lock on tasks during the build. CLAUDE.md DB rule: 'CREATE INDEX on a populated table must use CONCURRENTLY ... a concurrent index goes in its own migration file with no other statements'. The other new indexes in this range (146/150/156/157/163/186/189 and 159:92 idx_task_assignment_history_task) are all on tables created within the same migration (empty), so plain CREATE INDEX is correct for them — this is the only offender.
- **Why:** On the live DB the index build briefly locks writes to tasks. For Kaufman's modest task volume the stall is short, but it's a documented standard deviation and the pattern (column-add + index + triggers in one transaction-wrapped file) means it cannot be made CONCURRENTLY without splitting — which is exactly what the standard requires.
- **Fix:** For future task-table indexes, isolate the CREATE INDEX CONCURRENTLY into its own statement-only migration file. The existing 159 index is already applied to prod; no rebuild needed now, but note the deviation so it isn't copied. (If ever rebuilt, do it concurrently in a dedicated file.)
- **Verify:** Verified against the actual code. supabase/migrations/159_task_assignment_attribution.sql lines 10-15 do ALTER TABLE public.tasks ADD COLUMN assigned_by/assigned_at then a plain (non-CONCURRENT) CREATE INDEX IF NOT EXISTS idx_tasks_assigned_by ON public.tasks(assigned_by) — exactly as quoted. The tasks table is pre-existing and populated: it was created in 009_tasks.sql line 46 (long before 159), and mig 159 itself proves population by running UPDATE public.tasks (lines 77-81) and seeding history from existing rows (lines 199-214). A plain CREATE INDEX on a populated table takes a write lock during the build, which the CLAUDE.md DB rule ('CREATE INDEX on a populated table must use CONCURRENTLY ... in its own migration file') forbids. The reviewer's claim that this is the only offender in the range checks out: spot-checked 146 (idx on case_associated_advisors, CREATE TABLE same file line 19) and 157 (idx on task_attachments, CREATE TABLE same file), plus 159:92 idx_task_assignment_history_task is on task_assignment_history created at 159:83 — all empty-table builds where plain CREATE INDEX is correct, as the codebase itself documents in 107:29-30. Refutation attempt: the codebase carries an AUDIT-ACK in 172_protected_profile_immutable.sql:83-87 exempting small tables (profiles = single-digit rows, microsecond lock) from the rule, naming 'large hot tables (cases, documents, audit_log)' as the real targets. But tasks is a growing transactional per-case operational table in that same family, not a fixed-size staff table, so it falls under the literal 'populated table' rule, not the exemption — the deviation is genuine. Severity Low is correct: at Kaufman's modest task volume the lock is brief, the index is already applied to prod (no rebuild needed), and the finding is correctly framed as a documented standards deviation to avoid copying for future task-table indexes rather than an active incident.

#### `DB-NOTIF-1` — 194 case-mention scoping drops notifications to a user whose only case access is via is_case_task_member (benign false-negative, not a leak)
- **File:** supabase/migrations/194_scope_mention_notifications_to_case_task_viewers.sql:32-57,165-167
- **Evidence:** can_view_case (mig 182) grants case read via FOUR branches: view_all_cases, view_own+assigned/associated, archived, OR is_case_task_member(id). But can_view_case_for (194:39-57) — used by notify_case_comment_mentions to decide whether to notify a mentioned user — only reproduces the first three branches; it omits the task-member branch (correctly, since is_case_task_member uses auth.uid() and can't be reparameterised per-user). Net effect: a user who can see a case ONLY because they have a task on it, if @-mentioned in a CASE comment, is silently skipped (194:165-167 gates the INSERT on can_view_case_for).
- **Why:** This is a missed notification, not a data leak — the conservative direction. Severity is Low because the omitted users are an edge cohort (task-only case access) and the failure is fail-safe (no notification rather than an over-broad one). Worth noting only so it isn't mistaken for a leak and so the mention picker (list_case_mentionable_profiles, same predicate) is understood to also exclude them.
- **Fix:** If task-member case viewers should receive case mentions, add a per-user is_case_task_member_for(p_user_id, p_case_id) predicate (mirroring has_permission_for / the 188/194 pattern) and OR it into can_view_case_for. Otherwise document the intentional exclusion in the migration header. No action required if the current (conservative) behavior is desired.
- **Verify:** Verified against the actual code; every claim substantiated.

can_view_case (defined in supabase/migrations/182_task_member_case_access.sql:76-98 — the finding's "mig 182" number is correct; the title's filename guess differs but the number is right) grants case read via FOUR branches: view_all_cases (89), view_own_cases + assigned/associated (90-93), archived + view_archived_cases (94), and is_case_task_member(c.id) (95).

is_case_task_member (182:36-54) selects on tasks where assigned_to/assigned_by/created_by = auth.uid() (49-51), so it evaluates the CALLER's membership and cannot represent an arbitrary mentioned user. No is_case_task_member_for(user_id, case_id) per-user variant exists anywhere in the migrations (grep found only is_case_task_member and has_permission_for).

can_view_case_for (194:32-58) reproduces only the first three branches (44-55) and omits the task-member branch. The mention trigger notify_case_comment_mentions gates its notifications INSERT on can_view_case_for(mentioned_id, NEW.case_id) (194:167), so a user whose ONLY case access is a task on that case is silently skipped when @-mentioned in a CASE comment. The picker list_case_mentionable_profiles (194:122) uses the same predicate, and src/features/case-comments/services/case-comments.service.ts calls that RPC — so the composer also excludes them. Both confirmed.

Direction is fail-safe: can_view_case_for is a strict subset of can_view_case (no extra branches added), so it can only deny a notification, never grant one to a non-viewer — a missed notification, not a data leak. No later migration re-fixes it (195 is the highest; it concerns scenario writes).

Severity Low is correct: the affected cohort is narrow (task-only case access), and the failure mode is conservative/fail-safe. is_real=true.

#### `SQLTEST-3` — No SQL test for mention-notification view-scoping (mig 194) — cross-case PII-leak guard is unverified
- **File:** supabase/tests/ (missing) — covers supabase/migrations/194_scope_mention_notifications_to_case_task_viewers.sql
- **Evidence:** grep of supabase/tests/** for notify_case_comment_mentions / notify_task_comment_mentions / can_view_case_for / can_view_task_for / list_case_mentionable / case_mention / task_mention returns nothing. rls_permissions_test.sql covers task_comments SELECT/INSERT visibility (mig 140) but NOT the mention TRIGGERS. Mig 194 recreated both mention triggers to skip a mentioned user who fails can_view_case_for / can_view_task_for before inserting the notification (which fans out to the bell + email mirror carrying a 140-char body preview + task title) — closing an off-platform leak where @-mentioning a teammate with no case access emailed them internal client chatter.
- **Why:** The notification row feeds an email mirror, so a regression here leaks client-comment previews to staff outside the case (an external/email data-leak, not just an in-app one). The new per-user predicates (can_view_case_for / can_view_task_for) are SECURITY DEFINER and parameterised by an arbitrary user id; their correctness (especially the associated-advisor and is_private/archived branches) is entirely untested.
- **Fix:** Add a pgTAP file: insert a case_comment / task_comment whose body @-mentions (a) a user who can view the case/task and (b) a user who cannot, then assert exactly one notification row was created (for user a) and none for user b. Also unit-test can_view_case_for / can_view_task_for directly across roles (view_all, view_own+assigned, view_own+associated, no-access, is_private task) — these predicates back the mention guard AND the mention picker.
- **Verify:** Verified against the code. Migration 194 (supabase/migrations/194_scope_mention_notifications_to_case_task_viewers.sql) does exactly what the finding describes: it adds SECURITY DEFINER predicates can_view_case_for (lines 32-58) and can_view_task_for (lines 63-91), adds list_case_mentionable_profiles (lines 99-124), and recreates BOTH mention triggers notify_case_comment_mentions (lines 133-178) and notify_task_comment_mentions (lines 180-246) to skip a mentioned user who fails the view predicate before inserting the notification (guards at lines 167 and 230).

Test-coverage gap confirmed: grep of supabase/tests/** for notify_case_comment_mentions / notify_task_comment_mentions / can_view_case_for / can_view_task_for / list_case_mentionable / case_mention / task_mention returns zero matches. rls_permissions_test.sql covers task_comments SELECT/INSERT RLS (mig 140, lines 229-264) and a backup_stale notification (lines 219-225), but nothing exercises the mention triggers or the new per-user predicates. The only mention text in the suite is a code comment at line 253 describing the task_comments write-IDOR test, not the trigger. So the finding's evidence is accurate.

Severity rationale also substantiated: the notification row feeds an email mirror. src/features/notifications/services/notification-mirror-email.ts lines 99-104 build the email body from data.preview (140-char comment body) and data.taskTitle for case_mention/task_mention, and lines 38-41 state the email goes straight to the recipient's inbox carrying the row's data snapshot. Migration 161 enables this mirror (email_mentions DEFAULT TRUE). So a regression in the guard would indeed leak client-comment previews off-platform. The CI workflow .github/workflows/db-tests.yml runs `supabase test db` over supabase/tests/** on any migration change, so the suggested pgTAP file would be picked up automatically — the fix is actionable.

I adjusted severity Medium -> Low. This is a pure test-coverage gap, not a live defect: the guard is correctly implemented in the trigger AND the picker is independently scoped (defense-in-depth), and I found no evidence the predicates are wrong (the associated-advisor branch at lines 49-52 and the is_private branch at line 74 both look correct). The only risk is an undetected future regression, which is real but lower-impact than an exploitable bug. is_real=true; confidence high.

#### `CONFIG-1` — config.toml seed sql_paths points at a non-existent ./seed.sql — the demo seed is never auto-loaded
- **File:** supabase/config.toml:60-65
- **Evidence:** [db.seed] enabled = true with sql_paths = ["./seed.sql"]. `git log --all -- supabase/seed.sql` returns nothing — that file has never existed. The actual seed lives at supabase/seeds/demo-cases.sql (referenced nowhere in config). The Supabase CLI treats a glob with no matches as a no-op, so `supabase db reset` / `supabase start` (incl. the db-tests.yml CI step that boots a fresh stack) silently seed nothing despite enabled = true.
- **Why:** Misleading config: a developer reading config.toml believes the demo cases are auto-seeded on reset, but they are not — the seed must be run by hand. The wiring and the real seed file are out of sync, and the dead path can mask a future intent to actually auto-seed.
- **Fix:** Either point the glob at the real file — sql_paths = ["./seeds/demo-cases.sql"] (note: demo-cases.sql RAISEs EXCEPTION if demo_v1 rows already exist, so auto-seed-on-reset is fine on a fresh DB but would abort on a re-seed; gate behind a marker check if auto-loading is intended) — or set [db.seed] enabled = false and document that the seed is manual. Do NOT auto-load it into the CI test stack, since the seed depends on a profile with the hardcoded developer email (see SEED-1).
- **Verify:** All claims verified against the code. config.toml:60-65 confirmed verbatim: [db.seed] enabled = true (line 62), sql_paths = ["./seed.sql"] (line 65). The referenced ./seed.sql does not exist: `git log --all -- supabase/seed.sql` returns nothing (never existed in any commit/branch) and Glob for supabase/seed.sql finds no file. The real seed is supabase/seeds/demo-cases.sql (Glob confirmed; note path mismatch — config points at ./seed.sql in supabase/ root, real file is in seeds/ subdir; config's own comment on line 64 even suggests "./seeds/*.sql" which would have matched). The Supabase CLI treats a glob with zero matches as a no-op, so the seed step is silently skipped on db reset/start despite enabled = true. db-tests.yml line 27 runs `supabase start` against a fresh stack, so its seed step is indeed a no-op (and its path filter is migrations/**, tests/** — not seeds/** — so nothing in CI depends on the seed). The suggested-fix caveats are also accurate: demo-cases.sql lines 52-56 RAISE EXCEPTION if demo_v1 rows already exist (auto-seed would abort on re-seed), and lines 39-45 hard-depend on the hardcoded developer email shh92533@gmail.com (validating the SEED-1 cross-reference that it must not be auto-loaded into CI). Severity Low is correct and not overstated: the impact is a misleading/dead config path with no runtime breakage (harmless no-op), no production effect (seeds don't run in prod deploys), and no effect on RLS test correctness (tests don't depend on the demo seed). It is a config-hygiene/documentation-accuracy issue.

#### `SEED-1` — demo-cases.sql hard-depends on the developer's personal email and is not idempotent (abort-on-rerun)
- **File:** supabase/seeds/demo-cases.sql:39-56
- **Evidence:** SELECT id INTO admin_id FROM public.profiles WHERE email = 'shh92533@gmail.com' ... IF admin_id IS NULL THEN RAISE EXCEPTION 'Admin profile not found for shh92533@gmail.com'. The seed cannot run on any environment where that exact personal email has not logged in, and its 'Re-running' header (lines 15-17) confirms it is not idempotent — it aborts (existing_count > 0 RAISE) rather than upserting. National IDs are sequential synthetic values (301234567, 302345678, …) and borrower names are generic Hebrew placeholders, so no real PII/secrets — that part is safe.
- **Why:** Tying a shared seed to one developer's gmail address makes it unusable for any teammate, CI, or a fresh prod-like stack without editing the file, and the abort-on-rerun behavior means a partially-seeded DB needs manual cleanup before retry. Low because it contains no secrets/real-PII and is dev-only by intent.
- **Fix:** Resolve the admin by role instead of a literal email — SELECT id FROM profiles p JOIN roles r ON r.id=p.role_id WHERE r.key='admin' AND p.is_active LIMIT 1 — and consider making it idempotent (skip-if-exists per case rather than RAISE) or clearly label it a one-shot dev fixture. At minimum, drop the personal-email coupling.
- **Verify:** Verified all claims against supabase/seeds/demo-cases.sql and cross-referenced migrations.

1. Personal-email coupling CONFIRMED (lines 39-46): the only source of admin_id is `SELECT id FROM public.profiles WHERE email = 'shh92533@gmail.com'`, with `RAISE EXCEPTION 'Admin profile not found for shh92533@gmail.com. Log in once before seeding.'` if NULL. That email is the developer's own gmail (matches userEmail in context). admin_id is then used as created_by/updated_by/assigned_advisor_id for every inserted row. Grep confirms the literal appears in no other file — there is no env/override path, so the seed cannot run anywhere that exact account has not logged in.

2. Not idempotent / abort-on-rerun CONFIRMED (lines 48-56 + header 15-17): if any cases row has metadata->>'seed_batch' = 'demo_v1', it RAISE EXCEPTIONs instead of upserting/skipping; the header itself states 'The script aborts if any demo_v1 rows already exist. Clean up first, then re-run.' A successfully-seeded DB blocks reruns and needs a manual DELETE first.

3. No-PII/no-secrets claim CONFIRMED: national IDs are sequential synthetic values (301234567, 302345678, ...) and names are generic Hebrew placeholders. This is a dev-only fixture under supabase/seeds/. This is why the severity ceiling is Low, and it holds.

4. Suggested fix is feasible against the real schema: roles (002_auth_core.sql:13-23) has UNIQUE key TEXT; 004_lookups_seed.sql:36 seeds key='admin'; profiles (002:140-157) has role_id FK and is_active. So role-based resolution (JOIN roles WHERE key='admin' AND is_active LIMIT 1) works.

Impact is developer/CI friction plus manual cleanup on retry, not a security or correctness defect — Low is correctly rated, neither over- nor under-stated.

#### `CONFIG-2` — Local-stack auth defaults are weak: minimum_password_length = 6 and enable_signup = true for an invite-only product
- **File:** supabase/config.toml:171,176-180,216
- **Evidence:** [auth] minimum_password_length = 6 (file's own comment recommends 8+), password_requirements = "" (no complexity), enable_signup = true, [auth.email] enable_signup = true. The product is invite-only via magic-link (CLAUDE.md: admin never sets a password; no public signup). Note config.toml governs the LOCAL dev + CI stack only — hosted prod (linked project kaufman-finance-prod / uknsayoyvffkxamofczy) auth settings live in the Supabase dashboard — so this is not a prod hole.
- **Why:** Local/CI auth posture drifts from the intended invite-only, stronger-password policy; if a developer copies config.toml semantics into the dashboard, or relies on local behavior matching prod, the gap misleads. Low because it is local-stack-only and prod is configured separately.
- **Fix:** Align the local stack with the product policy: minimum_password_length = 8 (or higher), and set enable_signup = false in both [auth] and [auth.email] to mirror the invite-only flow. Add a comment that these mirror — and must be kept in sync with — the hosted dashboard settings.
- **Verify:** All cited config.toml values are accurate (verified verbatim): line 171 enable_signup = true; lines 176-177 the comment says "Minimum 6, recommended 8 or more" with minimum_password_length = 6; line 180 password_requirements = ""; line 216 [auth.email] enable_signup = true. The product is genuinely invite-only via magic-link: src/features/team/actions/invite-member.ts uses admin.auth.admin.generateLink({type:'invite'}) and sets options.data.invited_by = userRes.user.id (line 63); there is no .signUp() call anywhere under src/. So the local-stack auth posture (weak password floor + signup enabled) does drift from the intended invite-only/stronger-password policy — the finding's core claim holds.

The finding also correctly scopes this as local/CI-only and explicitly states it is NOT a prod hole; hosted prod auth lives in the Supabase dashboard. That scoping is corroborated by supabase/migrations/059_signup_hardening.sql (its comment says the correct fix is disabling self-signup in the dashboard, an operator action) and by RELEASE_REVIEW.md items #32 and OPS-G5 (which track "consider raising minimum_password_length above 6" and "Self-signup OFF in Supabase dashboard" as operator tasks).

Severity is correctly stated as Low and was not over/under-stated. Notably, the "why it matters" (drift could mislead a developer) is already partly mitigated by two in-repo compensating controls the finding does not mention: (1) src/features/auth/schemas/set-password.schema.ts enforces the real app-level policy (min 8 + at least one letter and one digit) and its own comment explicitly documents the config.toml 6/no-complexity floor as the reason it exists, pointing at the exact lines; (2) migration 059 is a DB-layer belt-and-suspenders guard — handle_new_user refuses to create a profile unless invited_by metadata is present, so even with enable_signup = true an anon signup yields a profile-less, RLS-locked-out account. These reduce real impact but don't negate the config-hygiene/drift observation. Confirmed is_real=true at Low.

#### `DRIFT-TABLES-2` — checklist_templates and case_payouts tables absent from database.ts -> untyped .from() handles in their services
- **File:** src/types/database.ts:16+ (Tables); src/features/documents/services/checklist-templates-store.service.ts:32-72; src/features/case-payouts/services/case-payouts.service.ts:18-34
- **Evidence:** Neither checklist_templates (189_checklist_templates.sql) nor case_payouts (186_case_payouts.sql) exists in the database.ts Tables block. Both services therefore read through `const db = supabase as unknown as SupabaseClient` and call `db.from('checklist_templates')` / `db.from('case_payouts')` with hand-written column strings and row-shape casts (checklist-templates-store.service.ts:34, case-payouts.service.ts:20,33 — comment: "case_payouts isn't in the generated Database types yet"). checklist_templates is additionally absent from BACKUP_TABLES and the restore v_tables, so manager edits to the preset checklist templates are lost on a restore (re-seeded to factory defaults only).
- **Why:** `.from()` on an untyped client gives zero compile-time checking of table name, selected columns, or row shape — a column rename in a future migration silently breaks the read at runtime. The checklist_templates backup gap is config-data loss (lower stakes than case_payouts financial data, but still an admin-edited table dropped on restore).
- **Fix:** Regenerate database.ts to include both tables, remove the `as unknown as SupabaseClient` handles in the two services, and decide whether checklist_templates should join BACKUP_TABLES/restore v_tables (its admin edits are otherwise non-recoverable).
- **Verify:** CONFIRMED by reading the code. (1) Both tables are real: checklist_templates created in supabase/migrations/189_checklist_templates.sql, case_payouts in 186_case_payouts.sql. (2) Neither appears in src/types/database.ts Tables block — I enumerated all 41 table keys (audit_log..user_permission_overrides); checklist_templates and case_payouts are absent. (3) Both services read through an untyped client: checklist-templates-store.service.ts:34 and :61 use `const db = supabase as unknown as SupabaseClient` then `db.from('checklist_templates')` with hand-written column strings ('template_key, group_key, name_he, name_en') and a TemplateRow cast; case-payouts.service.ts:20 does the same with `db.from('case_payouts')`, a PAYOUT_COLUMNS const, and the explicit comment (lines 8-10) "case_payouts isn't in the generated Database types yet, so the read goes through an untyped handle — same pattern as case-properties / case-comments." The `as unknown as SupabaseClient` workaround appears in 28 files, confirming it is the codebase's standard escape hatch for not-yet-generated table types, so the untyped-handle claim is accurate. (4) Backup gap confirmed: BACKUP_TABLES (backup-snapshot.service.ts:14-47, satisfies keyof Database['public']['Tables']) does NOT list checklist_templates, and the latest restore RPC (mig 193_backup_restore..., v_tables lines 34-42) does not include it either — so a disaster-recovery restore silently drops manager-edited checklist templates (the seed in mig 189 is ON CONFLICT DO NOTHING, so a re-run only restores factory defaults, never the edits). This is the exact silent-data-loss class that migs 115 and 193 were created to fix, and backup-restore-allowlist.test.ts cannot catch it because it only checks parity BETWEEN the two lists — a table in neither passes. NUANCE / finding understated the gap: case_payouts is ALSO absent from BACKUP_TABLES and restore v_tables, and that is manager-only financial data (per-case fee splits feeding net-fee statistics in migs 187/191) — higher stakes than checklist config, yet the finding only flagged checklist_templates for the backup gap. Severity: the primary stated impact (untyped .from() = no compile-time check, runtime break only on a future column rename) is genuinely Low and partly mitigated by explicit column consts + console.error guards returning [] / null. The backup-loss component (especially case_payouts financial data) is more concrete but confined to the disaster-recovery restore path and is config/manually-recoverable; keeping Low to match the finding's framing, though the case_payouts financial-backup omission arguably edges toward Medium.


## Round 18 — DB hardening — migrations 056-110

**raw 7 -> confirmed 7 / refuted 0  (High 0, Medium 3, Low 4)**

### Medium (3)

#### `RPC-1` — soft_delete_case RPC uses the legacy authz guard — excludes associated advisors, contradicting the action layer and breaking the delete flow for that role
- **File:** supabase/migrations/077_case_lifecycle_rpcs.sql:23-34
- **Evidence:** soft_delete_case is NOT superseded by any later migration (grep confirms it appears only in 077; mig 190 canonicalized borrower/income writes and mig 192 the bank/expense/document writes, but neither touched soft_delete_case). Its effective-latest body still gates on the LEGACY inline check:

  IF NOT EXISTS (SELECT 1 FROM public.cases c WHERE c.id = p_case_id AND c.deleted_at IS NULL AND (public.has_permission('edit_any_case') OR (public.has_permission('edit_own_case') AND c.assigned_advisor_id = v_actor))) THEN RETURN FALSE;

This predates associated advisors (migs 146/147) and excludes them. The calling action src/features/cases/actions/delete-case.ts:17 gates on userCanEditCase(caseId) — which (via can_edit_case, mig 147) DOES admit associated advisors — then treats the RPC's FALSE return as 'unauthorized' (line 37: `if (updated !== true) return { ok: false, error: 'unauthorized' }`). So an associated advisor holding delete_case passes the UI/action gate but the RPC silently refuses: the recycle-bin delete fails for that role. The canonical guard public._assert_can_edit_case (mig 147) already encapsulates the correct (edit_any OR edit_own AND (assigned OR associated)) rule + the not-deleted check.
- **Why:** Base invariant: case-scoped writes must canonicalize on can_edit_case; the legacy 'assigned_advisor_id = auth.uid() OR edit_any_case' guard is explicitly called a bug because it excludes associated advisors. soft_delete_case is a case-scoped write that diverges from the action layer's own authority model, producing a broken core flow (delete that silently no-ops) for an associated advisor who legitimately has delete_case.
- **Fix:** Replace the inline EXISTS block in soft_delete_case with the canonical guard: PERFORM public._assert_can_edit_case(p_case_id); (after the delete_case permission check), keeping the delete_case gate. This matches how the checklist (mig 099), borrower (mig 190), and bank/expense/document (mig 192) RPCs already resolve the same legacy pattern. Add a small migration (CREATE OR REPLACE) so it self-registers in schema_version.
- **Verify:** CONFIRMED by reading the code.

1. soft_delete_case is defined exactly ONCE and never superseded. An exact-signature grep `CREATE OR REPLACE FUNCTION public.soft_delete_case\s*\(` over supabase/migrations returns a single hit: 077_case_lifecycle_rpcs.sql:8. The two extra hits in a looser grep matched `soft_delete_case_expense` (migs 081/192) and `can_edit_case` substring text, NOT this function. Migs 190 (borrower/income) and 192 (bank/expense/document) canonicalized other RPCs onto the new guard but did not touch soft_delete_case.

2. The effective body uses the LEGACY inline guard (077:23-34): `edit_any_case OR (edit_own_case AND c.assigned_advisor_id = v_actor)` — no is_case_associated_advisor branch. This predates associated advisors (migs 146/147).

3. The canonical guard _assert_can_edit_case (147:104-131) and can_edit_case (147:81-101) DO admit associated advisors via `c.assigned_advisor_id = v_actor OR public.is_case_associated_advisor(c.id)`. is_case_associated_advisor is defined in 146:42-54.

4. Divergence verified end-to-end: delete-case.ts:17 gates on userCanEditCase(caseId), which calls the can_edit_case RPC (permissions.ts:114) — admitting associated advisors. delete-case.ts:37 then treats the RPC's FALSE return as 'unauthorized'. So an associated (non-responsible) advisor holding edit_own_case + delete_case passes both action gates (lines 17, 18), but soft_delete_case's legacy check evaluates FALSE (not edit_any, and assigned_advisor_id != actor), returns FALSE, and the delete silently no-ops with a generic error toast.

5. The UI menu item (case-more-menu.tsx via case-action-bar.tsx; canDelete computed at page.tsx:91 as `permissions.delete_case === true`) is shown to any delete_case holder, so the associated advisor does reach the action and observes the broken flow. The domain helper case-edit-gate.ts and the detail page's canEditCase wiring both explicitly model associated advisors as edit-capable, so soft_delete_case is the lone divergence.

SEVERITY: keeping Medium. It is a real correctness/consistency bug producing a broken core flow (delete silently fails) for a shipped role, and the codebase already treats this exact legacy-guard pattern as a bug worth a canonical fix (migs 190, 192). NOT a security vuln — it fails CLOSED (too restrictive), and the trigger is narrow (associated-but-not-responsible advisor who also holds delete_case). The suggested fix (replace the inline EXISTS with PERFORM public._assert_can_edit_case(p_case_id) after the delete_case check, in a new CREATE OR REPLACE migration that self-registers in schema_version) is correct and matches the established remediation pattern.

Files read: supabase/migrations/077_case_lifecycle_rpcs.sql, 146_case_associated_advisors.sql, 147_associated_advisor_case_access.sql; src/features/cases/actions/delete-case.ts; src/lib/auth/permissions.ts (userCanEditCase); src/features/cases/domain/case-edit-gate.ts; src/features/cases/components/case-more-menu.tsx, case-action-bar.tsx; src/app/(app)/cases/[id]/page.tsx.

#### `SOFT-DELETE-CASE-LEGACY-GUARD` — soft_delete_case still uses the pre-associated-advisor legacy guard, excluding associated advisors that the action pre-check and UI both admit
- **File:** supabase/migrations/077_case_lifecycle_rpcs.sql:23-34
- **Evidence:** soft_delete_case (mig 077, effective-latest — no later redefinition; grep confirms the name is only DEFINED in 077, all other hits are comments) gates on the LEGACY check:

  IF NOT EXISTS (SELECT 1 FROM public.cases c WHERE c.id = p_case_id AND c.deleted_at IS NULL AND (public.has_permission('edit_any_case') OR (public.has_permission('edit_own_case') AND c.assigned_advisor_id = v_actor))) THEN RETURN FALSE;

This is the exact `assigned_advisor_id = auth.uid() OR edit_any_case` pattern that migs 190/192 swept onto public.can_edit_case / _assert_can_edit_case everywhere else (borrowers/incomes/obligations/banks/expenses/documents/tombstone-RPC). can_edit_case (mig 147:81-100) additionally admits public.is_case_associated_advisor; soft_delete_case does not. Meanwhile the calling action gates the fast-feedback pre-check on the CANONICAL guard: deleteCaseAction (src/features/cases/actions/delete-case.ts:17) calls userCanEditCase(caseId) (= can_edit_case, includes associated advisors) + userHasPermission('delete_case'), and the UI (case-more-menu.tsx) shows the Delete item from a canDelete prop. So an associated advisor with delete_case + edit_own_case passes the action pre-check, sees + clicks Delete, confirms the AlertDialog, and the RPC silently RETURNs FALSE → delete-case.ts:37 maps `updated !== true` to 'unauthorized'.
- **Why:** Authz inconsistency / broken core flow for the associated-advisor role: the role can edit every other facet of a case it is associated with (190/192 widened all child writes to it) and the UI offers the Delete affordance, but the soft-delete RPC rejects it. Result is a confusing post-confirmation 'unauthorized' error, and the case-detail edit-gate (C-036, which derives affordance visibility from can_edit_case) shows an action that fails at the server — the precise mismatch class C-036 exists to prevent. Blast radius is limited (associated advisors holding delete_case), hence Medium, not High.
- **Fix:** Repoint soft_delete_case at the canonical guard, mirroring 190/192: replace the inline legacy EXISTS check with `PERFORM public._assert_can_edit_case(p_case_id);` after the existing delete_case permission check, then UPDATE ... SET deleted_at = now(), updated_by = v_actor WHERE id = p_case_id AND deleted_at IS NULL; RETURN FOUND. _assert_can_edit_case is a strict superset of the legacy check (adds associated advisors only), so this only widens to users who already legitimately edit the case. Note restore_case + permanently_delete_case stay is_admin()-only by design and need no change.
- **Verify:** CONFIRMED by reading the code. Every claim in the finding checks out.

1) soft_delete_case is defined ONLY in mig 077 (supabase/migrations/077_case_lifecycle_rpcs.sql:8) — never redefined. Grep for "soft_delete_case" shows the case-level RPC appears only in 077; all later hits (081/156/179/186/192) are the distinct soft_delete_case_expense/_property/_payout RPCs, not the case-level one. So 077's body is effective-latest.

2) The 077 guard (lines 23-34) is the legacy check: edit_any_case OR (edit_own_case AND c.assigned_advisor_id = v_actor). It does NOT include public.is_case_associated_advisor.

3) The canonical guard does include associated advisors: can_edit_case / _assert_can_edit_case (mig 147:81-131) add `OR public.is_case_associated_advisor(c.id)`. Migs 190 (borrowers/incomes/obligations) and 192 (banks/expenses/documents) explicitly swept every child-write path off the exact legacy pattern onto _assert_can_edit_case/can_edit_case — mig 190's header even states the legacy check "EXCLUDES an associated advisor who legitimately has can_edit_case." soft_delete_case is the lone holdout.

4) The action pre-check uses the CANONICAL guard: delete-case.ts:17 calls userCanEditCase (permissions.ts:102 delegates to can_edit_case RPC, mig 147 — includes associated advisors) + userHasPermission('delete_case'). On RPC returning FALSE, delete-case.ts:37 maps updated!==true to 'unauthorized'.

5) The UI offers Delete even more liberally than the finding states: case-detail page.tsx:91 computes canDelete = permissions.delete_case === true (NOT AND canEditCase — contrast lines 94/95 where status/advisor DO require canEditCase). case-more-menu.tsx:117 renders the Delete item whenever canDelete. So any user with delete_case sees it; the finding's associated-advisor persona is a valid subset.

CONCRETE DIVERGENCE proving the inconsistency: for an associated advisor with edit_own_case + delete_case + archive_case (associated, not assigned, no edit_any_case), Archive SUCCEEDS (toggle-archive.ts:21 pre-check passes AND the cases_update RLS policy was widened to associated advisors in mig 147:38-53) while Delete FAILS (077 legacy guard rejects → RETURN FALSE → 'unauthorized' after the user confirms the AlertDialog). This is exactly the C-036 affordance-visible-but-server-rejects mismatch, on a destructive action.

SEVERITY: Medium is correct. Not High — the RPC fails CLOSED (over-denies; no unauthorized deletion / no data exposure), and the affected persona is narrow (associated advisor holding delete_case with only edit_own_case). More than Low — it's a genuine cross-layer authz inconsistency the project has an explicit invariant (C-036) to prevent, surfacing as a broken core flow (confusing post-confirm 'unauthorized' on a primary destructive action) for a role that mig 190/192 deliberately widened everywhere else. Suggested fix (repoint at _assert_can_edit_case, a strict superset) is safe and correct; restore/permanent-delete staying is_admin()-only is by design.

#### `MIG073-RPC-PUBLIC-GRANT` — insert_overdue_notifications (SECURITY DEFINER) never revokes the default PUBLIC EXECUTE grant — any authenticated user can forge notifications
- **File:** supabase/migrations/073_sla_dedupe_index_and_rpc.sql:39-88
- **Evidence:** 073 creates public.insert_overdue_notifications(JSONB) as SECURITY DEFINER and ends with only `GRANT EXECUTE ON FUNCTION public.insert_overdue_notifications(JSONB) TO service_role;` (line 88). There is NO `REVOKE ALL ... FROM PUBLIC`. In Postgres a newly-created function carries an implicit EXECUTE grant to PUBLIC, so `authenticated` (and `anon`) retain EXECUTE despite the header comment 'service_role + the cron-only path can call this. Don't grant to authenticated' (lines 85-87). Grepped the entire supabase/migrations tree: the function is never REVOKEd from PUBLIC and never re-defined, so this is the effective-latest grant state. The legitimate caller (src/features/sla/services/sla-check.service.ts:301) uses the service-role admin client, so the intended path is unaffected — but an authenticated user can POST /rest/v1/rpc/insert_overdue_notifications with attacker-chosen p_rows. Because the function is SECURITY DEFINER it bypasses the notifications table's `notifications_no_direct_insert` WITH CHECK(FALSE) policy (mig 060) and inserts rows with caller-controlled user_id, type, case_id and data JSONB (lines 57-63). This is the IDENTICAL un-revoked-default-PUBLIC class of bug that migration 164 explicitly fixed for consume_rate_limit ('048 was executable with the PUBLIC anon key (default EXECUTE was never revoked)'), and the contrasting correct pattern (REVOKE ALL FROM PUBLIC then GRANT) is used consistently elsewhere in this same range (077:166-174, 078:116-120, 081:142-143, 093:231-232, 094:45-46).
- **Why:** An authenticated low-privilege user (e.g. junior advisor) can spam any user's notification bell — including admins — with arbitrary content: arbitrary user_id, any type permitted by notifications_type_check, and a fully attacker-controlled `data` payload that the bell UI renders. Blast radius is bounded (rows are FK-constrained to real profiles/cases and deduped by uq_notifications_overdue_dedupe for the overdue type), so it is notification-spoofing / nuisance rather than data loss — hence Medium, not High. It violates the least-privilege grant invariant (REVOKE FROM PUBLIC on SECURITY DEFINER functions) and the table's deliberate 'only the definer trigger may write notifications' design.
- **Fix:** Add `REVOKE ALL ON FUNCTION public.insert_overdue_notifications(JSONB) FROM PUBLIC, anon, authenticated;` before the existing GRANT to service_role, mirroring migration 164's treatment of consume_rate_limit/refund_rate_limit. Ship it as its own small migration that self-registers in schema_version, and follow 164's deploy note (the only caller already uses the service-role client, so no code change or special deploy ordering is required here).
- **Verify:** Confirmed by reading the code. supabase/migrations/073_sla_dedupe_index_and_rpc.sql:39-88 creates public.insert_overdue_notifications(JSONB) as SECURITY DEFINER SET search_path=public, inserting user_id/type/case_id/data from caller-supplied JSONB (lines 57-63), and ends with ONLY `GRANT EXECUTE ... TO service_role` (line 88) — there is NO `REVOKE ALL ... FROM PUBLIC`. In Postgres a new function carries an implicit PUBLIC EXECUTE grant, so authenticated/anon retain EXECUTE despite the header comment (85-87) stating service-role-only intent. Grepped the whole tree: the function appears only in 073 (def), database.ts (type), and sla-check.service.ts (caller); it is never re-defined or revoked, and no blanket `REVOKE ON ALL FUNCTIONS`/`ALTER DEFAULT PRIVILEGES` exists, so 073 is the effective-latest grant state. The legitimate caller (sla-check.service.ts:1 imports createAdminClient; RPC at :301-304) uses the service-role admin client, so the intended path is unaffected — matching the finding. SECURITY DEFINER bypasses the notifications_no_direct_insert policy (060:85-89, FOR INSERT TO authenticated WITH CHECK(FALSE)). The type-check (153:20-30) permits case_status_overdue plus 8 types, and type/user_id/case_id/data all come straight from the JSONB, so an authenticated user can POST /rest/v1/rpc/insert_overdue_notifications with attacker-chosen rows to spoof any victim's notification bell. The cited mirror bug is accurate: mig 164 (164_rate_limit_refund_lockdown.sql:14-21,48-52) explicitly fixed the identical default-PUBLIC-EXECUTE class for consume_rate_limit using REVOKE ALL FROM PUBLIC,anon,authenticated then GRANT to service_role, and the correct REVOKE pattern is present at 077:166-169, 078:116-117, 081:142, 093:231, 094:45. Severity Medium is correct: rows are FK-constrained to real profiles/cases and deduped by uq_notifications_overdue_dedupe, so the blast radius is notification-spoofing/nuisance, not data loss — but it violates the least-privilege REVOKE-FROM-PUBLIC invariant and the table's definer-only write design. Suggested fix (add REVOKE ALL ... FROM PUBLIC, anon, authenticated before the service_role grant, shipped as its own self-registering migration; no code change since the only caller already uses the service-role client) is sound.

### Low (4)

#### `RPC-2` — set_primary_bank trusts a client-supplied p_user_id for created_by/updated_by attribution and lacks SET search_path
- **File:** supabase/migrations/118_set_primary_bank_no_delete.sql:18-82
- **Evidence:** The effective-latest set_primary_bank (the function body in 118_set_primary_bank_no_delete.sql; note the in-file header mislabels it 'migration 103') is SECURITY INVOKER and accepts p_user_id UUID, writing it verbatim into updated_by (lines 33,55,72) and both created_by/updated_by (line 80) WITHOUT verifying p_user_id = auth.uid(). The action src/features/case-banks/actions/set-primary-bank.ts:33 passes the session user id, but a direct PostgREST RPC call could pass any uuid. The actual row write is still RLS-gated (case_banks INSERT/UPDATE now require can_edit_case, mig 192), so an attacker cannot write to a case they can't edit — but they can stamp another user's id as the editor/creator. Other RPCs reject this (e.g. soft_delete_document_with_tombstone, mig 192 line 183: `IF auth.uid() IS NULL OR auth.uid() <> p_user_id THEN RAISE ... 42501`). Separately, set_primary_bank is the only function in the 056-110 set with no `SET search_path` clause (all its table refs are schema-qualified as public.case_banks, so the practical risk is low, but it deviates from the project convention).
- **Why:** Audit/attribution integrity: created_by/updated_by on case_banks become spoofable on a direct RPC call, so the 'who set the primary bank' record can be falsified. Blast radius is limited because write authority itself remains gated by can_edit_case via RLS.
- **Fix:** Drop the p_user_id parameter and derive the actor from auth.uid() inside the function (or assert auth.uid() = p_user_id at the top and RAISE 42501 on mismatch, as soft_delete_document_with_tombstone does). Add `SET search_path = public` to match the rest of the codebase. Update the action call site accordingly.
- **Verify:** CONFIRMED by reading the code. supabase/migrations/118_set_primary_bank_no_delete.sql (the latest set_primary_bank definition — no migration after 118 redefines it) is SECURITY INVOKER, takes p_user_id UUID, and writes it verbatim into updated_by (lines 34,56,72) and created_by/updated_by (line 80) with NO check that p_user_id = auth.uid(). It is GRANT EXECUTE ... TO authenticated (line 84), so any authenticated user can call it directly via PostgREST and pass an arbitrary uuid. The server action src/features/case-banks/actions/set-primary-bank.ts:33 does pass the real session id (userRes.user.id), so the app path is clean, but the RPC itself is the trust boundary.

The row write IS RLS-gated: mig 192 (lines 34-43) sets case_banks INSERT/UPDATE WITH CHECK/USING public.can_edit_case(case_id), and because set_primary_bank is SECURITY INVOKER, that RLS applies. So an attacker cannot write to a case they cannot edit — the impact is limited to spoofing the created_by/updated_by attribution (who set the primary bank) on a case they CAN edit. That makes the finding's blast-radius scoping accurate and Low the correct severity.

The contrasting convention is real: mig 192's soft_delete_document_with_tombstone (line 183) does exactly the guard the finding cites — IF auth.uid() IS NULL OR auth.uid() <> p_user_id THEN RAISE ... 42501 — and soft_delete_case_expense (mig 192:72) derives the actor from auth.uid() internally with no p_user_id param at all.

The SET search_path deviation is also real: I scanned every function-defining migration from 050-119; essentially all have SET search_path >= 1, while the two set_primary_bank versions (102 and 118) have 0. The only other 0 in range is 056_optimistic_locking.sql, which defines just bump_row_version() (a trivial trigger helper), so set_primary_bank is the genuine RPC outlier. Practical risk is low because all its table references are schema-qualified as public.case_banks, which the finding itself acknowledges. The in-file header mislabeling it 'Migration 103' (line 2) is also accurate. The finding does not overstate severity; the suggested fix (derive actor from auth.uid() or assert auth.uid()=p_user_id and RAISE 42501, plus add SET search_path = public) matches the established codebase pattern.

#### `RPC-3` — layout_bootstrap serializes the encrypted google_calendar_refresh_token credential column into the bootstrap envelope (select-* of a credential column)
- **File:** supabase/migrations/066_layout_bootstrap_rpc.sql:49-51
- **Evidence:** layout_bootstrap (SECURITY DEFINER, runs on every authenticated page load) builds the profile via `SELECT to_jsonb(p) - 'metadata' INTO v_profile FROM public.profiles p WHERE p.id = v_actor`. to_jsonb(p) emits ALL columns; only 'metadata' is stripped. The profiles table has google_calendar_refresh_token TEXT (002_auth_core.sql:149, 'encrypted at app layer'), which therefore ends up inside the JSONB envelope returned to the Next.js server. The TS consumer src/lib/layout/bootstrap.ts:93-102 currently maps out only first_name/last_name/phone/email/language/role, so the token does not reach the browser today — but the protection rests entirely on that allowlist mapper not regressing. The backup snapshot service treats this exact column as a secret and explicitly excludes it (src/features/backup/services/backup-snapshot.service.ts:67), and the project rule forbids select('*')-style pulls of credential/token columns toward a client. cases_dashboard_bootstrap (mig 090) avoids this by building an explicit jsonb_build_object of named fields.
- **Why:** Defense-in-depth: an encrypted OAuth refresh token crosses the DB->server boundary on every page render and is one careless mapper edit (or a future raw pass-through of envelope.profile) away from leaking to the client. It violates the 'never pull credential/token columns toward a client' discipline even though it is not currently exposed to the browser.
- **Fix:** Either subtract the credential column in the RPC (`to_jsonb(p) - 'metadata' - 'google_calendar_refresh_token'`, plus google_calendar_connected if not needed), or — better, matching mig 090 — replace to_jsonb(p) with an explicit jsonb_build_object of just the fields the layout actually consumes (first_name, last_name, phone, email, language).
- **Verify:** Confirmed by reading the code. layout_bootstrap (SECURITY DEFINER, runs on every authenticated page load) serializes the entire profiles row into the JSONB envelope returned to the Next.js server, including the secret google_calendar_refresh_token column. The finding cites the mig 066 version (066_layout_bootstrap_rpc.sql:49 = `to_jsonb(p) - 'metadata'`, which strips only metadata and keeps the token). I verified the function is REDEFINED in a LATER migration: 088_critical_task_priority.sql:102 uses `SELECT to_jsonb(p) INTO v_profile` with NO subtraction at all — so the active version is broader than the finding describes (token AND metadata both serialized). No later migration redefines it (122 references it only in a comment). Cross-references all check out: profiles.google_calendar_refresh_token TEXT '-- encrypted at app layer' (002_auth_core.sql:149); backup-snapshot.service.ts:67 explicitly redacts this exact column as a secret with a defense-in-depth rationale; the suggested clean pattern matches 090_cases_dashboard_bootstrap.sql:31-37 which uses explicit jsonb_build_object of named fields. Not currently exposed to the browser: src/lib/layout/bootstrap.ts:93-102 allowlist-maps only first_name/last_name/phone/email/language/role, and both consumers (app/(app)/layout.tsx, topbar) read only mapped fields, never the raw envelope — so the sole protection is the mapper not regressing, exactly as the finding states. Severity Low is correct: genuine select-* of a credential column toward a client and a real defense-in-depth violation, but no live browser exposure today.

#### `AUDIT-BLOCK-MUTATIONS-NO-SEARCH-PATH` — audit_log_block_mutations trigger function has no SET search_path (effective body is mig 133)
- **File:** supabase/migrations/133_audit_pii_erasure.sql:28-40
- **Evidence:** The effective-latest audit_log_block_mutations (redefined in 049 -> 063 -> 133; 133 wins) is declared `LANGUAGE plpgsql` with neither SECURITY DEFINER nor SET search_path. It is the immutability guard on audit_log (BEFORE UPDATE OR DELETE) and only calls current_setting() + RAISE. Every other SECURITY DEFINER function in this range correctly pins `SET search_path = public` (e.g. 047/049 cleanup, 077, 104, 139, 144, 173). audit_log_block_mutations is SECURITY INVOKER, so it runs with the caller's rights and search_path injection is not a privilege-escalation vector here.
- **Why:** No exploitable risk today (invoker rights + only built-in calls), but it is the single function in the audit/retention surface that diverges from the project's stated SQL invariant ('SECURITY DEFINER fns MUST SET search_path; pin search_path on trigger fns for consistency'). Pinning it removes any future footgun if someone adds a schema-resolved call inside the guard.
- **Fix:** Add `SET search_path = public` to the function header in a CREATE OR REPLACE (idempotent), keeping the body byte-identical. Optional consistency-only; safe to defer.
- **Verify:** CONFIRMED by reading the code. supabase/migrations/133_audit_pii_erasure.sql:28-40 redefines public.audit_log_block_mutations() as `RETURNS TRIGGER LANGUAGE plpgsql AS $$...$$` with NO SECURITY DEFINER and NO SET search_path. Body only calls current_setting('app.purge_audit'/'app.redact_audit', true) + RETURN COALESCE(NEW,OLD) + RAISE EXCEPTION — no schema-resolved calls.

Effective-latest claim verified: grep across all migrations shows only three touches of the function name — 049 (creates it), 063 (re-binds the trigger only; an in-file comment at 063:141 states "audit_log_block_mutations is unchanged; only the trigger binding is new"), and 133 (CREATE OR REPLACE). So 133 is indeed the winning definition. The original 049 definition (049:21-36) ALSO lacked SECURITY DEFINER/SET search_path, so 133 carried the gap forward — it didn't introduce it but is the current effective body.

Inconsistency claim verified: in 049 itself the sibling cleanup_old_audit_logs (049:46-72) has SECURITY DEFINER + SET search_path = public. In 133 the other new function audit_redact_on_hard_delete (133:49-82) pins SET search_path = public. Every comparator I read in the audit/retention surface pins it: 063 ensure_audit_log_partitions_ahead + cleanup_old_audit_logs, 139 cleanup_soft_deleted_records, 144 get_last_erasure_at/notify_admins_erasure_stale/record_erasure_success, 173 retention_purge_enabled/cleanup_old_audit_logs/cleanup_soft_deleted_records — all SECURITY DEFINER + SET search_path = public. audit_log_block_mutations is the lone outlier.

Severity Low is correct (kept as stated). The function is SECURITY INVOKER (no SECURITY DEFINER), so it runs with the caller's rights — no privilege-escalation vector. It calls only built-in functions (current_setting, RAISE) with no unqualified schema-resolved object access, so search_path injection cannot change its behavior today. This is a pure consistency/hardening nit, not an exploitable bug; the finding accurately self-describes it as "no exploitable risk today" and "optional consistency-only; safe to defer." The suggested fix (add SET search_path = public via idempotent CREATE OR REPLACE, body byte-identical) is sound.

Files read: supabase/migrations/133_audit_pii_erasure.sql (full), 049_audit_log_hardening.sql (full), plus grep/header inspection of 063, 139, 144, 173.

#### `MIG056-110-INDEX-NOT-CONCURRENT` — CREATE INDEX statements on already-populated tables are non-CONCURRENT (acknowledged deploy trade-off, low impact at current scale)
- **File:** supabase/migrations/051_fk_indexes.sql:18-59
- **Evidence:** Several migrations in this range add plain `CREATE INDEX` (no CONCURRENTLY) on tables that are populated in production — e.g. 051 indexes leads/borrowers/documents/tasks/notifications/audit_log FK columns and rebuilds idx_tasks_due (lines 57-59); 042 indexes cases FKs; 060 adds idx_tasks_tags + uq_profiles_email_lower + uq_leads_national_id_active; 068/076/081/093/106 add per-table indexes. CLAUDE.md states 'CREATE INDEX on a populated table must use CONCURRENTLY' and that a concurrent index must live alone in its own file. None of these do. This is consistent with the documented deploy model (migrations are applied by hand in the Supabase SQL Editor, each file wrapped in a transaction — which structurally forbids CONCURRENTLY inside a multi-statement file) and the entire pre-143 migration history predates that rule being enforced.
- **Why:** A plain CREATE INDEX takes a SHARE lock that blocks concurrent writes to the table for the build duration. At ~80 cases the affected tables are tiny and each build completes well under a second, so live-traffic impact is negligible today; the concern is forward-looking (the rule exists for when these tables grow). Reported as Low / consistency only — not a behavioral defect, and not specific to 056-110.
- **Fix:** No action needed for the already-applied historical migrations. For FUTURE indexes on populated tables, follow the CLAUDE.md discipline: one `CREATE INDEX CONCURRENTLY` per migration file with no other statements, applied outside a transaction. Optionally document the accepted trade-off for the historical batch so it isn't re-flagged.
- **Verify:** Confirmed by reading the cited migrations. Plain CREATE INDEX (no CONCURRENTLY) does appear in 042_cases_fk_indexes.sql:8-11, 051_fk_indexes.sql:18-59 (incl. DROP+rebuild idx_tasks_due at 57-59), 060_audit_followups.sql:97-99/118-120/132-136, 068:50-52, 076:24-30, 081:31-33, 093:31-37/67-69, and 106:121-159/180-225. The CLAUDE.md rule the finding cites (CONCURRENTLY on populated tables; concurrent index alone in its own transaction-free file) is genuine. However the finding's claim is correctly self-scoped as Low/consistency-only and the code substantiates that framing strongly: (1) most indexes in 076/081/093/106 are created on tables defined by CREATE TABLE IF NOT EXISTS in the same file, i.e. brand-new EMPTY tables where CONCURRENTLY is irrelevant — the repo even documents this in 107_case_comments.sql:29-30 ('New, empty table -> a plain CREATE INDEX is fine'); the only truly populated-table cases are 042/051/060/068. (2) The trade-off is already explicitly acknowledged in-repo: 172_protected_profile_immutable.sql:83-87 carries an AUDIT-ACK (CONCURRENTLY) comment stating the exact reasoning (single/double-digit row tables, microsecond lock, rule targets large hot tables, no CONCURRENTLY precedent, transactional index ships atomically). No runtime/behavioral/security/data-loss defect: indexes build correctly, just non-concurrently, with negligible impact at ~80 cases. is_real=true because the pattern objectively exists against the documented rule; severity stays Low (finding neither over- nor under-stated) since it is an accepted, already-documented, forward-looking consistency note, not a defect, and the suggested fix is 'no action needed for historical migrations'.


## Round 20 — release engineering / ops / supply chain / health

**raw 18 -> confirmed 17 / refuted 1  (High 0, Medium 5, Low 12)**

### Medium (5)

#### `CI-3` — No `next build` gate on PRs — a broken production build is only caught at deploy time
- **File:** .github/workflows/ci.yml:13-27 (verify job); Dockerfile:21; .github/workflows/deploy.yml:85
- **Evidence:** The `verify` job runs `npx tsc --noEmit`, `npm run lint`, and `npm test` only — there is no `next build` step (grep for `next build`/`npm run build` across `.github/` matches ONLY deploy.yml line 85, which is in the `workflow_dispatch`-only disabled deploy workflow). Per project memory, prod auto-deploys `main` via Vercel's git integration, so the first time a production build runs is on push to main (Vercel) or on the Vultr deploy.sh build. `tsc --noEmit` does not catch Turbopack/standalone build-boundary failures — the project's own MEMORY records a class of bug where a 'use client' file importing server code passes tsc/lint but fails `npm run build` ('Run `npm run build` before pushing UI that crosses the boundary').
- **Why:** Build-only failures (client/server import boundary, RSC violations, next.config evaluation, the migrations-dir read at build time) reach `main` green and break the production deploy, which is the documented failure mode. A build step on PRs turns a prod-deploy break into a red check.
- **Fix:** Add a `- name: Build / run: npm run build` step to the `verify` job in ci.yml (after Test). Provide dummy/build-safe env values for the @t3-oss env validation so the build runs in CI, or set `SKIP_ENV_VALIDATION=1` if the env schema supports it.
- **Verify:** Confirmed by reading the actual files. ci.yml:13-27 (verify job) runs only `npx tsc --noEmit` (Typecheck), `npm run lint` (Lint), and `npm test` (Test) after `npm ci` — there is NO `next build`/`npm run build` step. Grep for `next build|npm run build|vercel build` across .github/ returns exactly ONE hit: deploy.yml:85 (`vercel build --prod`), and deploy.yml:26-31 documents that workflow as DISABLED with `on: workflow_dispatch` only — so it never runs on PRs or push. The third workflow, db-tests.yml, runs supabase/pgTAP and also does not build. package.json:8 confirms `build` = `next build`; Dockerfile:21 runs `npm run build` (the Vultr/Docker prod build path). Thus the first time a production `next build` executes is post-merge (Vercel git integration on push to main, or the Vultr deploy.sh/Docker build). The claim that tsc/lint miss build-boundary failures is corroborated by the project's OWN memory, which records a real bug class where a `'use client'` file importing a server-side value passes tsc/lint but fails `npm run build` ("Run `npm run build` before pushing UI that crosses the boundary") — so this is a documented, not hypothetical, failure mode in this repo. The suggested fix is viable: env.ts uses @t3-oss createEnv (line 25) without `skipValidation` and there are zero `SKIP_ENV_VALIDATION` references, so the t3-env layer would honor that flag; one caveat is env.ts:195-208 has module-eval `throw`s (key domain-separation checks) that fire on server import regardless of t3-env skip, so CI would still need distinct dummy INTEGRATION_ENCRYPTION_KEY/BACKUP_ENCRYPTION_KEY values for the build to complete — a refinement to the fix, not a flaw in the finding. Severity: this is a defense-in-depth CI/process gap, not a runtime security/data bug, and the prod deploys have an auto-rollback safety net (per memory), but a build-only break genuinely reaches green main and breaks the production deploy — exactly what a PR build check would prevent. Medium is defensible; arguably borderline Low given the rollback net, but kept at Medium because the failure mode is documented and reaches production.

#### `OPS-1` — DEPLOYING.md states prod is Vultr (NOT Vercel) — the reverse of reality; would misdirect an operator during an incident
- **File:** docs/DEPLOYING.md:3-5,62-78
- **Evidence:** DEPLOYING.md line 3-4: "Production runs as a Docker container on the Vultr host — NOT Vercel. (The Vercel + `.github/workflows/deploy.yml` path is historical and disabled.)" This directly contradicts every other authoritative source: vercel.json declares the prod crons; src/app/api/cron/backup/route.ts:22 header says "Vercel Cron — see vercel.json"; src/lib/logger.ts:1-3 says logs go "to stdout/stderr so Vercel captures it"; and docs/INCIDENT_RESPONSE.md:49-53,119-120 correctly labels "Vercel (client production, crm.kaufman-finance.com)" vs "Vultr (staging / secondary)". The actual topology (per the project's own incident runbook) is Vercel = client prod, Vultr = staging.
- **Why:** This is the single most dangerous doc in the set. An operator opening DEPLOYING.md during an outage or a hot-fix will SSH to the Vultr host and run deploy.sh, believing they are deploying production. The live client site (crm.kaufman-finance.com on Vercel) stays broken while they 'successfully' deploy to staging. Recovery time blows up at the worst possible moment, and the rollback section (§Deploy safety, first-crm:prev) applies to the wrong target entirely.
- **Fix:** Rewrite DEPLOYING.md to match INCIDENT_RESPONSE.md: client production = Vercel (crm.kaufman-finance.com), Vultr = staging/secondary. Document the real Vercel deploy + migrations-before-code procedure as the primary path and clearly demote the Vultr/Docker flow to 'staging deploy'. Cross-link both so the two docs can never drift apart again.
- **Verify:** CONFIRMED. docs/DEPLOYING.md lines 3-4 assert "Production runs as a Docker container on the Vultr host — NOT Vercel. (The Vercel + .github/workflows/deploy.yml path is historical and disabled.)" This is contradicted by every other current authoritative source I read:

- docs/INCIDENT_RESPONSE.md (the project's own incident runbook) lines 48-53 and the appendix lines 119-120 explicitly label "Vercel (client production, crm.kaufman-finance.com)" as the client-prod rollback target and "Vultr (staging / secondary, Docker)" as a separate, lesser target — the exact reverse of DEPLOYING.md.
- vercel.json holds the live production crons (backup 0 2, backup-watchdog, cleanup-orphaned-blobs, erasure-watchdog, status-sla-check, task-reminders) — these only fire if Vercel hosts prod.
- src/app/api/cron/backup/route.ts:22 header: "Nightly automated backup to Drive (Vercel Cron — see vercel.json)".
- src/lib/logger.ts:1-3: logs go "to stdout/stderr so Vercel captures it".
- .github/workflows/deploy.yml is titled "Deploy (migrations + Vercel)" and IS disabled (on: workflow_dispatch only), but its disablement comment (line 26-29) is dev-era ("no prod Supabase / Vercel project yet, 2026-05-26") — now stale because prod Vercel exists.

DEPLOYING.md's cited authority, docs/FRANKFURT_MIGRATION_HANDOFF.md, is dated 2026-05-28 and describes the Vultr/Docker host for DIFFERENT domains (herskoviccrm.com / botview.live / ratebridge.live), NOT crm.kaufman-finance.com — i.e. an older/secondary topology, not the client production target. The two docs/ runbooks cross-reference each other yet state opposite production targets, so they have drifted exactly as the finding describes. An operator opening DEPLOYING.md during an outage would SSH to Vultr and run deploy.sh, deploying to staging while the live Vercel client site stays broken; the rollback section (first-crm:prev) targets the wrong host.

Could not refute — the contradiction is concrete and the corroborating current sources are unanimous (Vercel = client prod).

SEVERITY ADJUSTMENT: reviewer rated High; I lowered to Medium. The harm is real and incident-time, but (a) this is a documentation defect, not executable code/security/data-loss; (b) DEPLOYING.md's own §Related-runbooks already links to the CORRECT INCIDENT_RESPONSE.md, giving a careful operator a path to catch the inversion. "Single most dangerous doc" slightly overstates it. The suggested fix (rewrite DEPLOYING.md so client prod = Vercel, demote Vultr to staging, cross-link both) is exactly right.

#### `OPS-2` — RESTORE_RUNBOOK 'Known gap' warning is stale (scenario tables ARE restored) and the runbook never mentions the 4 tables added in mig 193
- **File:** docs/RESTORE_RUNBOOK.md:51-54
- **Evidence:** RESTORE_RUNBOOK.md:51-54 warns: "Known gap (open issue): the restore RPC's hardcoded table list does not include mortgage_scenarios / scenario_tracks ... saved simulator scenarios are not recovered by a Drive restore." That gap was closed long ago: supabase/migrations/115_restore_includes_scenarios.sql added both to v_tables, and supabase/migrations/193_backup_restore_includes_expenses_checklist_comments.sql added case_expenses, case_checklist_items, task_comments, case_bank_statuses. The parity is now machine-enforced (src/features/backup/services/backup-restore-allowlist.test.ts asserts BACKUP_TABLES == restore v_tables, and explicitly regression-locks the scenario tables). The runbook header (line 6) still points at migration 058 as 'current definition' (now 193) and §1 describes the restore set without the four mig-193 tables.
- **Why:** An operator deciding recovery strategy during a real data-loss event reads a runbook that says simulator scenarios CANNOT be recovered by Drive restore — false, and could push them to a heavier/riskier PITR rollback unnecessarily, or make them distrust an otherwise-complete restore. Conversely, the doc gives no assurance that the four newly-protected business tables (financial case_expenses, checklist progress, conversation threads) round-trip, even though they now do.
- **Fix:** Delete the stale 'Known gap' callout (lines 51-54). Update the header (line 6) to cite migration 193 as the current RPC definition. In §1 note the now-complete restorable set incl. case_expenses / case_checklist_items / task_comments / case_bank_statuses / mortgage_scenarios / scenario_tracks, and reference backup-restore-allowlist.test.ts as the drift guard.
- **Verify:** Confirmed against the code. docs/RESTORE_RUNBOOK.md lines 51-54 carry a "Known gap (open issue)" callout asserting mortgage_scenarios/scenario_tracks are NOT recovered by a Drive restore. That is false: supabase/migrations/115_restore_includes_scenarios.sql (lines 37-49) added both tables to the RPC's v_tables and v_tables_with_deleted_at, and supabase/migrations/193_backup_restore_includes_expenses_checklist_comments.sql (lines 34-47) is the current RPC body, which carries the scenario tables forward and additionally restores case_expenses, case_checklist_items, task_comments, and case_bank_statuses (plus task_assignment_history). Mig 193 self-registers schema_version 193 (line 92), making it the live definition. The runbook header (lines 4-8) still cites 058_restore_strip_deleted_at.sql as the "current definition" — stale by two redefinitions (115, 159, 193). Parity is machine-enforced: src/features/backup/services/backup-snapshot.service.ts BACKUP_TABLES (lines 14-47) lists all the same tables, and backup-restore-allowlist.test.ts asserts BACKUP_TABLES==restore v_tables (lines 75-89) and explicitly regression-locks the scenario tables (lines 96-99). §1 of the runbook describes the restore set without any of the six business tables. I could not refute any element of the finding; every cited line matches. Severity Medium is appropriate (not Low) because this is an incident-time operator runbook whose stale text actively misdirects recovery strategy — it could push an operator to an unnecessary full-DB PITR rollback for scenario data that the lighter merge-only Drive restore now recovers, or make them distrust a complete restore. It is documentation-only (no code defect), which caps it below High.

#### `OPS-3` — Handled errors never reach Sentry (stdout-only); go-live docs describe wiring logger→Sentry as an undone manual TODO that contradicts the now-existing Sentry infra
- **File:** src/lib/logger.ts:42-74; docs/AUDIT_HANDOFF.md:144-160; docs/KAUFMAN_GO_LIVE.md:91-100
- **Evidence:** Sentry is fully wired (sentry.server.config.ts, src/instrumentation.ts onRequestError, instrumentation-client.ts, src/lib/sentry/pii-scrub.ts), and route error boundaries call Sentry.captureException (src/app/global-error.tsx:22, src/app/(app)/error.tsx:23). BUT logger.ts emits only to console.* — there is NO Sentry call in it, and a grep for captureException/captureMessage across src finds none outside the client init + the two .tsx boundaries. The dominant server-action pattern is `console.error('[tag] ...', safeDbError(err)); return { ok:false, error:'unknown' }` (~46 files use safeDbError). Those handled errors are therefore Vercel-Function-log-only and invisible to Sentry alerting/aggregation. Meanwhile AUDIT_HANDOFF.md:146 still claims "The lib/logger.ts infrastructure is in place but nothing forwards errors to a real APM" and both AUDIT_HANDOFF.md:153-158 and KAUFMAN_GO_LIVE.md:93-98 instruct the operator to hand-edit logger.ts to add `Sentry.captureMessage(...)` — a step that was never taken and whose surrounding 'nothing is wired' framing is now false. instrumentation-client.ts:8 even references "the Sentry.captureException calls in the route" as if logger forwards, which it does not.
- **Why:** The two most security/data-sensitive failure classes — DB constraint/RLS denials and silent action failures — log via safeDbError and so never alert. Operators relying on Sentry as the APM (per the go-live checklist) will have a blind spot for exactly the handled-error path the codebase deliberately routes everything through. The stale doc compounds it: a follow-on operator either re-does work that's partly there or ships a conflicting capture path.
- **Fix:** Decide the policy and make doc+code agree: either (a) add a single Sentry.captureException/captureMessage hook inside logger.error() guarded on SENTRY_DSN (reusing the existing scrubber) so handled errors aggregate, then update AUDIT_HANDOFF/KAUFMAN_GO_LIVE to say it's done; or (b) if stdout-via-Vercel-Function-logs is the intended sink for handled errors, state that explicitly and remove the stale 'edit logger.ts' instructions. Fix the instrumentation-client.ts:8 comment that references nonexistent logger forwarding.
- **Verify:** VERIFIED TRUE by reading the code.

Core claims confirmed:
1. src/lib/logger.ts emit() (lines 42-74) only calls console.error/warn/log. No Sentry call anywhere in the file. Confirmed.
2. Sentry IS fully wired for the UNHANDLED path: sentry.server.config.ts (Sentry.init gated on SENTRY_DSN), src/instrumentation.ts (onRequestError → captureRequestError), src/instrumentation-client.ts (browser init), src/lib/sentry/pii-scrub.ts exists. Route boundaries call Sentry.captureException: global-error.tsx:22, (app)/error.tsx:22, and ALSO (auth)/error.tsx:22 (a THIRD boundary the finding undercounted — said "two").
3. Repo-wide grep for captureException|captureMessage finds NOTHING outside the client init + its comment + those three .tsx boundaries. No logger→Sentry hook exists.
4. Dominant handled-error pattern confirmed: e.g. update-borrower-field.ts:69-73 = console.error('[tag] ...', safeDbError(error)); return { ok:false, error:'unknown' }. safeDbError (db-error-log.ts) is a pure {code,message} shaper with NO forwarding. Used in 42 files (finding said ~46 — minor overstatement, immaterial).
5. CRITICAL CORROBORATION: there is NO captureConsoleIntegration/consoleLoggingIntegration in any Sentry config and next.config.ts is wrapped only with withNextIntl, NOT withSentryConfig. So the console.error calls in the 42 actions are NOT auto-captured by any Sentry integration either. The handled-error path is genuinely stdout/Vercel-Function-log-only and invisible to Sentry. The gap is REAL, not refuted by some hidden capture path.
6. Docs are stale verbatim: AUDIT_HANDOFF.md:146 ("nothing forwards errors to a real APM" — now partly false since boundaries+onRequestError DO forward) and :153-158 + KAUFMAN_GO_LIVE.md:93-98 both instruct hand-editing logger.ts to add Sentry.captureMessage, and the wizard step claims it'll create sentry.{client,server,edge}.config.ts files that already exist. Confirmed.

Minor inaccuracies in the finding (do NOT change verdict):
- The finding claims instrumentation-client.ts:8 "references nonexistent logger forwarding." FALSE: that comment references "the Sentry.captureException calls in the route error boundaries," which DO exist and ARE accurate. The comment is correct; it does not claim logger forwards. This sub-claim is overstated.
- The actual gap is slightly WIDER than the finding states: feature actions call console.error directly, not logger.error (logger is effectively unused outside its own test + a doc comment in request-context.ts). So even the doc-prescribed logger.ts edit would NOT capture the dominant pattern.

Severity Medium is correct: pure observability/ops gap (no data loss, no security exposure — PII scrubbers are in place), defeating Sentry aggregation/alerting for the codebase's deliberately-routed handled-error path (DB/RLS denials + silent action failures), compounded by stale go-live docs an operator will act on. Above Low (defeats the APM for the dominant error path AND docs actively mislead), below High (no functional/security/data impact).

#### `OPS-4` — KAUFMAN_GO_LIVE env-var table omits NEXT_SERVER_ACTIONS_ENCRYPTION_KEY, which is build-required in production
- **File:** docs/KAUFMAN_GO_LIVE.md:36-58; src/lib/env.ts:11-19,95
- **Evidence:** src/lib/env.ts validates NEXT_SERVER_ACTIONS_ENCRYPTION_KEY as a required base64 AES key when isProductionRuntime is true (lines 17-19, schema applied at 95) — missing/invalid value fails env validation at build. The go-live env-var collection table (KAUFMAN_GO_LIVE.md:39-58, the authoritative 'add all of these to Vercel Production scope' list) enumerates Supabase keys, APP_NAME/URL, INTEGRATION/BACKUP keys, both STRICT flags, CRON_SECRET, RESEND, GOOGLE_OAUTH_*, SENTRY_DSN — but does NOT list NEXT_SERVER_ACTIONS_ENCRYPTION_KEY. (.env.example:18-22 documents it, but the go-live checklist is the prod-setup source of truth.)
- **Why:** An operator provisioning the Vercel prod project strictly from the go-live checklist will omit a build-required secret; the first prod build fails env validation, or (if a value leaks in from elsewhere) action-key instability across rebuilds causes open tabs to submit rejected Server Action IDs. It's a self-inflicted launch blocker hiding in the one doc meant to prevent launch blockers.
- **Fix:** Add a row to the KAUFMAN_GO_LIVE.md:39-58 table: NEXT_SERVER_ACTIONS_ENCRYPTION_KEY = `openssl rand -base64 32` (REQUIRED in production, must be stable across rebuilds). Cross-check the table against env.ts's required-server set so no other required var is missing.
- **Verify:** CONFIRMED. Verified all three load-bearing claims against the code:

1. env.ts genuinely makes NEXT_SERVER_ACTIONS_ENCRYPTION_KEY build-required in production. Line 4: isProductionRuntime = process.env.NODE_ENV === 'production'. Lines 17-19: serverActionsEncryptionKeySchema = isProductionRuntime ? z.string().refine(isBase64AesKey,...) : (...).optional() — i.e. NON-optional, required base64 AES key in prod, optional otherwise. Wired into server block at line 95 and runtimeEnv at line 135. @t3-oss createEnv throws at build time on a missing/invalid required var (comment lines 21-24). The finding's cites (env.ts:11-19,95) are accurate to the current file.

2. docs/KAUFMAN_GO_LIVE.md self-declares as the "Single consolidated source of truth" (line 3). Its Vercel env-var table (lines 39-58, headed "Add all of these for Production scope") enumerates 18 vars (Supabase x4, APP_NAME/URL, INTEGRATION/BACKUP keys + both STRICT flags, CRON_SECRET, RESEND x2, GOOGLE_OAUTH x4, SENTRY_DSN) but does NOT include NEXT_SERVER_ACTIONS_ENCRYPTION_KEY. Grep over the whole doc returns zero matches — it is absent entirely, not just from the table.

3. .env.example:18-22 does document it (openssl rand -base64 32). Confirmed.

Tried to refute and could not:
- deploy.yml (the Vercel path the doc references) runs `vercel pull --environment=production` then `vercel build --prod` — it pulls env from the Vercel Production scope (the very scope the table populates) and does NOT auto-generate this key. So an operator provisioning strictly from the table omits it and `vercel build --prod` fails env validation. Next sets NODE_ENV=production for a prod build, so isProductionRuntime is true and the var is required — no escape hatch.
- scripts/deploy.sh:77-83 independently lists NEXT_SERVER_ACTIONS_ENCRYPTION_KEY among "the build-time-required keys the env schema enforces in prod" and aborts if missing — corroborating that it is genuinely build-required, not optional.
- RELEASE_REVIEW.md independently flagged the SAME gap: item 33 [P1] (line 768) acceptance criterion explicitly says the key should be "added to KAUFMAN_GO_LIVE env table" (recognizing it's currently absent), and OPS-G4 (line 1071) gates go-live on it being set in Vercel. A separate reviewer reaching the same conclusion strengthens, not weakens, the finding.

Severity: Medium is fair and accurate. It is a real launch blocker living in the authoritative prod-setup doc, and the var is genuinely build-required. Mitigating factors keeping it at Medium (not High): it is a doc-only defect (no security/data exposure), and the failure mode is a loud, fail-fast build error from createEnv that names the missing var, which is diagnosable. The worse branch the finding notes (a value leaking in inconsistently → action-key skew rejecting open-tab submissions) is plausible but secondary. Suggested fix (add a table row with the openssl recipe + a stability note, and cross-check the table against env.ts's required-server set) is appropriate.

### Low (12)

#### `CI-1` — `shadcn` is a production dependency, pulling the high-severity `hono` advisory + `msw`/MCP-SDK into the runtime image
- **File:** package.json:46 (dependencies.shadcn ^4.8.0); Dockerfile:31,38
- **Evidence:** `shadcn@4.8.0` is in `dependencies`, not `devDependencies`. It is a CLI scaffolding tool with ZERO runtime imports in src/ (grep for `from 'shadcn'` is empty). Its dependency tree is `shadcn -> @modelcontextprotocol/sdk@1.29.0 -> hono@4.12.23` and `shadcn -> msw@2.14.6`. `npm audit` reports `hono <=4.12.24` as **Severity: high** (GHSA-wwfh-h76j-fc44 path traversal on Windows via encoded backslash; GHSA-88fw-hqm2-52qc CORS reflects any Origin with credentials; plus body-limit/Set-Cookie bypasses). `node_modules/hono` is `dev:false` in package-lock (production tree). The Dockerfile copies the full `node_modules` (line 31) then runs `npm prune --omit=dev` (line 38) — since shadcn is a prod dep, prune KEEPS it, so hono/msw/MCP-SDK all ship into the running prod/staging container.
- **Why:** A CLI dev tool with known-vulnerable transitive deps is present in the production runtime image, bloating it and keeping the high-severity `hono` advisory live in prod. Moving shadcn to devDependencies removes the entire `hono` High finding and `msw` from the production install in one edit (it stays available for `npx shadcn` during dev).
- **Fix:** Move `"shadcn": "^4.8.0"` from `dependencies` to `devDependencies` in package.json, run `npm install` to refresh the lockfile, and re-run `npm audit --omit=dev` to confirm `hono` drops out of the production tree. Continue invoking it via `npx shadcn` for component scaffolding.
- **Verify:** Every factual claim is verified against the actual files. package.json:46 lists "shadcn": "^4.8.0" under dependencies (not devDependencies); the root lockfile entry (package-lock.json line 41, packages."" .dependencies) confirms the same. Dockerfile copies the full node_modules (line 31) then runs `npm prune --omit=dev` (line 38) — by definition prune keeps prod deps, so shadcn and its transitive tree survive into the runner image. Grep for `from 'shadcn'`/`require('shadcn')` in src/ returns nothing → zero runtime imports; shadcn is a CLI-only scaffolding tool. The dependency chain is confirmed in the lockfile: shadcn -> @modelcontextprotocol/sdk@1.29.0 -> hono@4.12.23, and shadcn -> msw@2.14.6. Crucially, the lockfile shows hono is depended on ONLY by @modelcontextprotocol/sdk, which is depended on ONLY by shadcn, and msw ONLY by shadcn — all marked dev:false (production tree). So shadcn is the sole reason these ship to prod. I ran `npm audit --omit=dev`: it reports hono at severity HIGH (GHSA-wwfh-h76j-fc44, serve-static path traversal on Windows via encoded backslash), 1 high total in the production tree. Moving shadcn to devDependencies removes the whole hono/msw/MCP chain from the prod install in one edit — verified the chain is exclusive to shadcn. 

Adjusted severity High->Low for contextual exploitability: while the hono advisory's own CVSS is High, the vulnerable code is dead in THIS app. hono is never imported or executed (it's a dormant transitive of an unused CLI binary); the serve-static path-traversal requires a running hono server serving static files, which never occurs in this Next.js runtime. The real impact here is image bloat + a persistent "high" line in npm audit + the hygiene principle of not shipping known-vulnerable code to prod — not a reachable exploit. The fix is a genuine one-line, zero-risk change (move to devDependencies, `npx shadcn` still works in dev), so it's worth doing, but the runtime risk to the application is Low, not High.

#### `CI-2` — Node version mismatch: CI + deploy use Node 20, but the Dockerfile (Vultr build + runtime) uses Node 22
- **File:** .github/workflows/ci.yml:19 (node-version: 20); .github/workflows/deploy.yml:67 (node-version: 20); Dockerfile:3,9,23 (FROM node:22-alpine); .github/dependabot.yml:44-48
- **Evidence:** `ci.yml` and `deploy.yml` both pin `actions/setup-node` to `node-version: 20`. The Dockerfile builds and runs on `node:22-alpine` for all three stages (deps/builder/runner). dependabot.yml (lines 44-48) hard-pins `@types/node` to `^20` with the comment "CI + Vercel both run Node 20" — which contradicts the Docker runtime, and Vercel defaults to Node 22 for new Next 16 projects. There is no `.nvmrc`, no `.node-version`, and no `engines` field anywhere to make the intended runtime authoritative (verified: `ls .nvmrc .node-version` empty, no `engines` in package.json).
- **Why:** CI typechecks/tests/audits against Node 20 type definitions and runtime semantics while the deployed artifact runs Node 22. Code that passes CI can behave differently (or surface different API availability) at runtime, and `@types/node@^20` understates APIs available on Node 22. This is exactly the class of 'TS-green code that differs at runtime' the dependabot comment was trying to prevent — now pointing at the wrong version.
- **Fix:** Pick one Node major as the source of truth (Node 22, matching the Dockerfile and Vercel's Next 16 default), then make it authoritative: add `"engines": { "node": "22.x" }` to package.json + an `.nvmrc` (`22`), bump `setup-node` to `node-version: 22` in ci.yml and deploy.yml, and update the dependabot `@types/node` ignore/comment to track `^22`.
- **Verify:** All cited facts verified against the actual files. ci.yml pins node-version: 20 in BOTH jobs (line 19 verify, line 40 audit). deploy.yml:67 pins node-version: 20. Dockerfile:3,9,23 all use FROM node:22-alpine (deps/builder/runner). dependabot.yml:44-48 blocks @types/node major bumps with the comment "CI + Vercel both run Node 20", and package.json:55 pins "@types/node": "^20" (installed 20.19.41). Confirmed via shell that there is NO .nvmrc, NO .node-version, and NO engines field anywhere — so nothing makes the intended Node major authoritative. The mismatch is genuine: CI typechecks/tests on Node 20 + Node 20 type defs while the Dockerfile (Vultr build+runtime per project deploy notes) builds and runs the artifact on Node 22. The structural gap (no engines/.nvmrc to pin a single source of truth) is real and the suggested fix is sound.

Severity adjusted Medium -> Low. Mitigating factors: (1) Node 20 and 22 are adjacent maintained LTS lines with very high API/runtime-semantics overlap, so "TS-green code that differs at runtime" is theoretical here, not demonstrated — the finding cites no actual API or behavior that diverges. (2) deploy.yml is gated to workflow_dispatch only and is documented as DISABLED during development (lines 26-31), so its Node 20 pin is not actively driving prod. (3) The finding's "Vercel defaults to Node 22" claim is NOT verifiable from the repo (vercel.json has only crons, no node pin; .vercel/project.json carries no version), so that part rests on external knowledge rather than code. This is a legitimate config-hygiene/correctness concern but carries no security or data-integrity impact and no proven runtime break, placing it at the top of Low rather than Medium.

#### `CI-4` — Workflows declare no `permissions:` block — `GITHUB_TOKEN` runs at the repo default scope
- **File:** .github/workflows/ci.yml; .github/workflows/deploy.yml; .github/workflows/db-tests.yml (no `permissions:` in any)
- **Evidence:** `grep -rn 'permissions:' .github/workflows/` returns nothing. None of the three workflows scope down the `GITHUB_TOKEN`. With no explicit block, the token inherits the repository/organization default, which on older repos is read-write to contents, packages, etc. CI/db-tests/audit jobs only need `contents: read`.
- **Why:** A compromised or malicious dependency executing during `npm ci`/`npm install`/`supabase start`/lint in a PR-triggered workflow runs with a write-capable token, widening supply-chain blast radius (the audit job notably runs `npm install` which executes lifecycle scripts of arbitrary bumped deps). Least-privilege limits what a poisoned step can do.
- **Fix:** Add a top-level `permissions: { contents: read }` to ci.yml and db-tests.yml. For deploy.yml grant only what the deploy needs. Also set the repo default token to read-only (Settings -> Actions -> Workflow permissions).
- **Verify:** Verified against all three workflow files (read in full) and a grep over .github/workflows/ which returns zero matches for "permissions:". Confirmed: none of ci.yml, deploy.yml, or db-tests.yml declares a top-level or job-level permissions block, so GITHUB_TOKEN inherits the repo/org default scope. The finding's specific evidence checks out: ci.yml runs on pull_request + push:main (lines 7-10); the audit job runs `npm install --no-audit --no-fund` (line 45) which DOES execute dependency lifecycle scripts, then `npm audit` (line 47); verify job runs npm ci/tsc/lint/test on PRs (lines 21-27); db-tests.yml runs `supabase start`/`supabase test db` on pull_request (lines 6-30); deploy.yml is workflow_dispatch-only currently (line 31) but uses secrets and lacks scoping. This is a genuine least-privilege / supply-chain hardening gap recognized by GitHub and OpenSSF Scorecard (Token-Permissions). However the reviewer's Medium overstates it: (1) the blast radius is conditional on the org/repo default still being read-write — GitHub defaults new repos to read-only since Feb 2023, so this may already be mitigated; (2) GITHUB_TOKEN is auto-scoped to this single repo and expires at job end; (3) critically, none of these use the dangerous pull_request_target-with-untrusted-checkout pattern — they use plain pull_request, where fork-PR tokens are already forced read-only by GitHub regardless of the base default, so the write-capable exposure only applies to same-repo PRs/pushes. It is defense-in-depth, not a concrete present vulnerability, so Low is the accurate severity. The suggested fix (add top-level `permissions: { contents: read }` to ci.yml/db-tests.yml, scope deploy.yml to its needs, and set repo default to read-only) is correct and low-risk.

#### `CI-5` — dependabot `security-critical` group targets packages not in the project, missing the libraries actually present
- **File:** .github/dependabot.yml:16-23
- **Evidence:** The `security-critical` group patterns are `@supabase/*`, `isomorphic-dompurify`, `file-type`, `googleapis`, `google-auth-library`. Verified against package.json: `isomorphic-dompurify`, `googleapis`, and `google-auth-library` are NOT dependencies. The actual security-sensitive libs in package.json — `sanitize-html` (HTML sanitization, the role `isomorphic-dompurify` was meant to cover), `web-push`, `@sentry/nextjs`, `exceljs`, `resend` — are not in the group, so a CVE in them is grouped/scheduled as ordinary `tooling`/ungrouped rather than expedited.
- **Why:** The fast-track security-update grouping is pointed at phantom packages while the libraries that actually parse untrusted input (sanitize-html on borrower-entered HTML, file-type/exceljs on uploads) are not prioritized. A real CVE in sanitize-html would not get the intended expedited single-PR treatment.
- **Fix:** Replace the stale patterns: drop `isomorphic-dompurify`/`googleapis`/`google-auth-library`, add `sanitize-html`, `web-push`, `exceljs`, `file-type` (already there), `@sentry/*`, and `resend` to the `security-critical` group.
- **Verify:** Confirmed by reading .github/dependabot.yml:16-23 and package.json. The security-critical group patterns are @supabase/*, isomorphic-dompurify, file-type, googleapis, google-auth-library. Cross-checked against package.json AND package-lock.json: isomorphic-dompurify, googleapis, and google-auth-library are NOT dependencies (not even transitive — zero matches in the lockfile). The googleapis/google-auth-library grep hits in src/features/integrations/services/google-drive.ts:24-25 and google-oauth.ts:27-33 are only URL string literals (https://www.googleapis.com/...); the project hand-rolls Drive/OAuth via raw fetch, it does not use those npm packages. So 3 of the 5 group patterns are phantoms (only @supabase/* and file-type actually match installed packages). Meanwhile the actually-present security-sensitive libs are NOT in the group: sanitize-html (line 45 — the real HTML sanitizer, used across create-case/update-case/update-case-field/save-case-draft and lib/utils/sanitize-html.ts on borrower-entered input; the spec's isomorphic-dompurify reference is stale), exceljs (line 33 — parses uploaded spreadsheets in import.service.ts), web-push (line 50), @sentry/nextjs (line 22), resend (line 44). A CVE in any of these would fall into tooling/ungrouped rather than the expedited single-PR security-critical track. The why-it-matters rationale is substantiated. Severity Low is correct: non-matching dependabot patterns are inert (no error, just never fire), so this is config-hygiene / defense-in-depth degradation, not a runtime bug. Suggested fix is accurate.

#### `CI-6` — Inconsistent + unpinned GitHub Action versions; db-tests uses older majors than the rest
- **File:** .github/workflows/db-tests.yml:19,22 (actions/checkout@v4, supabase/setup-cli@v1); ci.yml:16-17,37-38 + deploy.yml:42,45,64-65 (actions/checkout@v6, actions/setup-node@v6, supabase/setup-cli@v2)
- **Evidence:** db-tests.yml pins `actions/checkout@v4` and `supabase/setup-cli@v1`, while ci.yml/deploy.yml pin `actions/checkout@v6`, `actions/setup-node@v6`, and `supabase/setup-cli@v2`. All actions (including third-party `supabase/setup-cli`) are pinned to mutable tags, not commit SHAs.
- **Why:** Tag pinning means a compromised or retagged third-party action (`supabase/setup-cli`) silently changes what runs in CI — the canonical supply-chain risk for Actions. The version drift in db-tests.yml also means the security-critical RLS suite runs on an older, separately-maintained toolchain than the rest of CI.
- **Fix:** Bump db-tests.yml to `actions/checkout@v6` and `supabase/setup-cli@v2` to match. For third-party actions, pin to a full commit SHA (e.g. `supabase/setup-cli@<sha> # v2.x`) and let dependabot's github-actions ecosystem (already configured, lines 78-87) bump them.
- **Verify:** Every factual claim verified verbatim against the actual files. db-tests.yml:19 = `actions/checkout@v4`, db-tests.yml:22 = `supabase/setup-cli@v1`. ci.yml:16-17 = `actions/checkout@v6` + `actions/setup-node@v6`; ci.yml:37-38 = same pair in the audit job. deploy.yml:42 = `actions/checkout@v6`, deploy.yml:45 = `supabase/setup-cli@v2`, deploy.yml:64-65 = `actions/checkout@v6` + `actions/setup-node@v6`. So both sub-claims hold: (1) all actions are pinned to mutable major tags, none to a commit SHA — confirmed across all three workflow files; (2) db-tests.yml runs older majors (checkout@v4, setup-cli@v1) than ci/deploy (checkout@v6, setup-cli@v2), a genuine version drift on the security-critical RLS/pgTAP suite. The suggested-fix cross-reference is also accurate: dependabot.yml lines 78-87 do configure a `github-actions` ecosystem (monthly, version-update only), which explains why it hasn't unified the majors. supabase/setup-cli is correctly identified as a third-party action, so SHA-pinning it is the canonical hardening per GitHub's own guidance. The risk is real but theoretical (requires a third-party maintainer/tag compromise) and affects only CI tooling — no product runtime, RLS, auth, or data path. Severity Low is correctly stated, neither over- nor under-rated. Could not refute any element.

#### `CI-8` — Standing deferred item: CSP still allows `unsafe-inline` (no nonce) — owned by this ops round
- **File:** next.config.ts / security headers (R1-shell-1, per round context)
- **Evidence:** Round context lists R1-shell-1 (CSP nonce / drop `unsafe-inline`) as deferred TO this ops round and asks it be flagged as the standing item rather than re-litigated. Carried forward as the open release-readiness item for header hardening.
- **Why:** `unsafe-inline` in the CSP weakens XSS mitigation for an app rendering borrower-entered content (sanitize-html is the only line of defense). Tracked, not new.
- **Fix:** Schedule the CSP nonce migration (per-request nonce in proxy.ts, drop `unsafe-inline` from script-src) as the dedicated follow-up; out of scope to implement in this review.
- **Verify:** Confirmed in code. next.config.ts:40-41 ship `script-src 'self' 'unsafe-inline'` and `style-src 'self' 'unsafe-inline'` in all environments. The code comment at next.config.ts:56-62 explicitly documents the deferral ("switching to a nonce-per-request CSP requires middleware injection and is a separate follow-up"). src/proxy.ts contains NO CSP nonce injection (only x-request-id + updateSession), so the suggested per-request-nonce mechanism is genuinely not yet implemented. Decisive corroboration: src/app/layout.tsx:68-78 has an inline `<script dangerouslySetInnerHTML>` (crypto.randomUUID polyfill) carrying the literal comment `TODO(R1-shell-1 / review Round 20): this inline script is one of the blockers for dropping script-src 'unsafe-inline'` — exactly matching the finding's R1-shell-1 id and Round 20 ownership. The XSS rationale is valid: sanitize-html (src/lib/utils/sanitize-html.ts) is used in case actions to sanitize borrower-entered content, so it is a real defense layer that unsafe-inline weakens. Could not refute: no nonce wiring, no commit removing unsafe-inline, item is open. Severity Low is correct and matches the finding — this is a documented, tracked, pre-existing release-readiness item (not a new vuln) with compensating controls (sanitize-html, object-src 'none', base-uri 'self', frame-ancestors 'none', form-action 'self'). The finding correctly frames it as the standing item this ops round owns rather than something to implement now.

#### `OPS-CSP-1` — CSP keeps script-src 'unsafe-inline' (and style-src 'unsafe-inline') — the standing R1-shell-1 item
- **File:** next.config.ts:38-54 (cspDirectives), :85-87 (CSP header)
- **Evidence:** Line 40: `script-src 'self' 'unsafe-inline'` (plus `'unsafe-eval'` in dev), and line 41: `style-src 'self' 'unsafe-inline'`. The header comment (lines 56-72) explicitly documents this as deferred because Next 16 emits inline bootstrap/hydration scripts and a nonce-per-request CSP needs proxy injection. This is the known-deferred item the round brief told me to flag rather than re-litigate.
- **Why:** `'unsafe-inline'` in script-src means a successful HTML/attribute injection (XSS) is not blocked by CSP — the policy provides no script-execution backstop. The rest of the policy is strong (frame-ancestors 'none', object-src 'none', base-uri 'self', form-action 'self', img/connect allowlists, HSTS preload), so this is the single highest-leverage hardening left. Not a release blocker; the app has DOMPurify and React's default escaping as primary defenses.
- **Fix:** Track as the dedicated CSP follow-up (R1-shell-1): generate a per-request nonce in src/proxy.ts, propagate it via a request header into the root layout's <script>/<style nonce=...>, and replace 'unsafe-inline' in script-src (and ideally style-src) with 'nonce-<value>' 'strict-dynamic'. Next 16's hydration scripts accept a nonce when threaded through. Until then, leave as documented-deferred.
- **Verify:** Confirmed against next.config.ts. Line 40: `script-src 'self' 'unsafe-inline'` (with `'unsafe-eval'` appended only in dev via `${isProduction ? '' : " 'unsafe-eval'"}`). Line 41: `style-src 'self' 'unsafe-inline'`. Both exactly as cited. The deferral comment (lines 56-62) documents the omission, citing Next 16 inline bootstrap/hydration scripts and that a nonce-per-request CSP needs middleware injection as a separate follow-up. The CSP is emitted on every route (source '/:path*', lines 84-87, 134-135). The rest of the policy is genuinely strong as claimed: frame-ancestors 'none' (48), object-src 'none' (51), base-uri 'self' (49), form-action 'self' (50), img/connect allowlists (42,47), HSTS preload (75). src/proxy.ts exists but contains no nonce/CSP logic (grep: no matches), so the suggested per-request-nonce fix is genuinely not yet implemented — consistent with documented-deferred. 'unsafe-inline' in script-src does remove CSP as an XSS execution backstop, validating the Why-it-matters. One minor prose inaccuracy that does not affect validity: the finding says the app uses 'DOMPurify' but there is no DOMPurify dependency (absent from package.json); the actual sanitizer is `sanitize-html` (src/lib/utils/sanitize-html.ts, used across actions/services). The substantive defense-in-depth point (server-side HTML sanitization + React default escaping) holds. Severity Low is correctly calibrated — the reviewer explicitly framed it as not a release blocker and a known-deferred flag, which the strong surrounding policy plus sanitization justify.

#### `OPS-ENV-1` — NEXT_SERVER_ACTIONS_ENCRYPTION_KEY validation is gated on NODE_ENV at build time, but deploy.sh's required-key preflight does not check key VALIDITY
- **File:** src/lib/env.ts:17-19 (serverActionsEncryptionKeySchema); scripts/deploy.sh:79-84 (required-key grep)
- **Evidence:** env.ts only *requires* NEXT_SERVER_ACTIONS_ENCRYPTION_KEY when `process.env.NODE_ENV === 'production'` (line 4, 17). In deploy.sh the Docker build runs `next build` which sets NODE_ENV=production, so the schema's base64/AES-length refine (isBase64AesKey) DOES run and a malformed key fails the build — good. deploy.sh's own preflight (lines 79-84) only greps that the line EXISTS (`^${k}=`), not that the value is valid base64-of-16/24/32-bytes. A present-but-garbage value passes the grep and only fails later inside the build step.
- **Why:** Low impact only because the build step is a true fail-closed gate (build aborts on an invalid key, production untouched per the deploy flow). The grep preflight is a convenience early-abort, not the real gate, so the worst case is a less-clear error message a few minutes later in the build rather than at preflight. No security or data-loss exposure.
- **Fix:** Optional: have the deploy.sh preflight loop validate the encryption keys' shape (e.g. base64-decode length for NEXT_SERVER_ACTIONS_ENCRYPTION_KEY, >=32 chars for INTEGRATION/BACKUP keys) so a typo is caught at step 2 with a precise message instead of mid-build. Not required for release.
- **Verify:** Verified against the actual code; every factual claim holds.

(1) src/lib/env.ts:4 `const isProductionRuntime = process.env.NODE_ENV === 'production'` and lines 17-19 gate the schema: in prod it is `z.string().refine(isBase64AesKey, ...)` (required), otherwise the same refine but `.optional()`. (2) The refine (lines 13-16, `isBase64AesKey`) genuinely checks base64 charset + length%4 + that `Buffer.from(v,'base64').length` is 16/24/32 bytes, so a present-but-malformed value is rejected — not just a length check. (3) The build is a true fail-closed gate: Dockerfile builder stage (lines 14-21) exports the env-file secret then runs `npm run build` (= `next build`, package.json:7), and `next build` sets NODE_ENV=production, so `isProductionRuntime` is true and the required+refined key validates at build time. The `env` module is imported broadly (server actions, api/health, middleware, even a client hook), so `createEnv` is evaluated during the build and a garbage key aborts `next build`; deploy.sh:120 `|| die "build failed — production untouched"` and the pre-swap smoke test (step 5) mean prod is never touched on a bad key.

(4) The gap is real and exactly as described: deploy.sh:79-84 preflight only does `grep -qE "^${k}="` — line existence — for NEXT_SERVER_ACTIONS_ENCRYPTION_KEY (and INTEGRATION/BACKUP keys). A present-but-garbage value, or even an empty `KEY=`, passes the grep and only fails minutes later inside the docker build.

Could not refute it; the code exhibits the described mismatch. But the reviewer's own scoping is accurate and self-limiting: the real gate (the build) is fail-closed, production is untouched until the post-build/post-smoke swap, and the only consequence is a less precise error a few minutes later instead of at preflight step 2. No security or data-loss exposure. Low is correct (arguably generous, since it is purely a diagnostics-timing nicety, not a defect in the protective behavior). The suggested fix (validate key shape in the preflight loop) is reasonable and the reviewer correctly marks it optional / not release-blocking.

Relevant files: C:\Users\shh92\Projects\first-crm\src\lib\env.ts (lines 4, 13-19, 95); C:\Users\shh92\Projects\first-crm\scripts\deploy.sh (lines 79-84, 118-120, 139-145, 191-196); C:\Users\shh92\Projects\first-crm\Dockerfile (lines 14-25); C:\Users\shh92\Projects\first-crm\package.json (line 7).

#### `OPS-DOCS-1` — Round context says 'prod = Vercel (auto-deploys main)'; the repo's own source of truth says prod is Vultr/Docker and Vercel is disabled — confirm before relying on either gate
- **File:** docs/DEPLOYING.md:3-5; .github/workflows/deploy.yml:26-31 (on: workflow_dispatch); vercel.json (no buildCommand/ignoreCommand)
- **Evidence:** DEPLOYING.md line 3-4: 'Production runs as a Docker container on the Vultr host — NOT Vercel. (The Vercel + .github/workflows/deploy.yml path is historical and disabled.)' deploy.yml is `on: workflow_dispatch` only (lines 30-31) with a header comment 'DISABLED during development'. vercel.json has crons but NO buildCommand/ignoreCommand/migration step. This MATTERS for the migrations-before-code question: the schema-version health gate + auto-rollback only protect the deploy.sh/Vultr path (deploy.sh:147-189 applies migrations or aborts pre-swap; :198-210 auto-rollback). If main were actually auto-deploying to Vercel, NOTHING would enforce migrations-before-code there — Vercel git deploy ships code on push with no pre-swap /api/health gate, so a forgotten migration would serve 500s/schema_behind to live users (the 503 only fails health pings, it does not roll Vercel back). Per the on-disk docs the active path is Vultr, where the gate is correctly enforced, so this is currently a non-issue — but the discrepancy with the round brief should be reconciled so the operator knows which path is live.
- **Why:** The entire 'migrations-before-code' and 'auto-rollback on failed deploy' safety story lives in scripts/deploy.sh. It is robust there (smoke-test on throwaway port before swap, schema gate aborts pre-swap, auto-rollback to :prev, dir rotation). On a Vercel git-auto-deploy path none of that exists. Substantiated as safe under the documented (Vultr) architecture; flagged only so the live-target ambiguity is resolved, not left to assumption.
- **Fix:** Confirm the live production target. If Vultr/Docker (per DEPLOYING.md): no change needed — the gate is correct. If Vercel is/becomes live for main: add a migrations-before-code mechanism there (re-enable deploy.yml's `needs: migrate` job and disable Vercel's git auto-deploy, OR a Vercel buildCommand that runs `supabase db push` before build), because the runtime schema-version 503 does NOT prevent Vercel from serving the new code to users.
- **Verify:** Every factual claim in the finding is substantiated by the actual files. (1) docs/DEPLOYING.md:3-4 states verbatim "Production runs as a Docker container on the Vultr host — NOT Vercel. (The Vercel + .github/workflows/deploy.yml path is historical and disabled.)" (2) .github/workflows/deploy.yml is `on: workflow_dispatch` only (line 30-31) with a "DISABLED during development (2026-05-26)" header comment (lines 26-29) and `needs: migrate` on the deploy job (line 62) — so the CI migrations-before-Vercel gate is dormant. (3) vercel.json contains only a `crons` array — no buildCommand, ignoreCommand, or migration step. (4) scripts/deploy.sh step 6 (lines 147-189) runs supabase link + db push BEFORE the swap and `die`s on failure; step 5 (139-145) smoke-tests on throwaway port; step 8 (198-210) does a final /api/health check and auto-rolls-back to first-crm:prev on failure — exactly as cited. (5) src/app/api/health/route.ts confirms the 503 schema_behind gate (lines 88-115, 172-177) is a status response that deploy.sh's wait_http consumes pre-swap; it has no mechanism to roll back a Vercel git deploy.

The reviewer's core technical claim is correct: the entire migrations-before-code + auto-rollback safety story exists only in deploy.sh (Vultr path). A Vercel git-auto-deploy on push would have no pre-swap health gate, so a forgotten migration would serve 500s/schema_behind to live users; the runtime 503 only fails health pings, it does not revert Vercel. This is a genuine documentation/context-reconciliation issue rather than a code defect — and the round-brief's "prod = Vercel (auto-deploys main)" directly contradicts the repo's own source of truth (DEPLOYING.md says Vultr). The user's own project memory notes Kaufman is "LIVE on Vercel" while Vultr is staging, so the live-target ambiguity is real and unresolved, making the reconciliation worth flagging. Under the documented (Vultr) architecture the gate is correctly enforced, so there is no shipped-code bug — hence Low is the right severity (the reviewer rated it Low and that holds). Minor nit: the disabled-comment span is lines 26-29 (not 26-31) and the trigger is 30-31, a trivial line-number imprecision that does not affect the substance.

#### `OPS-5` — Go-live / audit docs say deploy.yml 'drives prod deploys now', but the workflow is disabled (workflow_dispatch) with a header stating no prod project exists
- **File:** .github/workflows/deploy.yml:26-31; docs/KAUFMAN_GO_LIVE.md:60-62,151-152; docs/AUDIT_HANDOFF.md:106-110
- **Evidence:** .github/workflows/deploy.yml:26-29 header: "DISABLED during development (2026-05-26): no prod Supabase / Vercel project yet, so every push to main fails the supabase link step. Re-enable by restoring on: push: branches: [main] ..."; line 30-31 the trigger is `on: workflow_dispatch` (manual only — does NOT run on push). Yet KAUFMAN_GO_LIVE.md:62 asserts "`.github/workflows/deploy.yml` drives prod deploys now" and Phase 5.1 (line 151) says pushing to main runs migrations then deploys; AUDIT_HANDOFF.md:108 repeats it. Project memory (deploy_procedure) describes the actual flow as manual migration apply + Vercel deploy, with push != auto-deploy.
- **Why:** Three docs disagree on the single most important release mechanic: is prod deployed by GitHub Actions (deploy.yml), Vercel git auto-deploy, or a manual procedure? An operator can't tell which, and following KAUFMAN_GO_LIVE 1.5 (disable Vercel auto-deploy because 'the workflow drives now') while deploy.yml is actually dispatch-only would leave NO deploy path wired at all.
- **Fix:** Reconcile to one truth. Either re-enable deploy.yml (restore on: push, configure the six secrets) and keep the docs, or — matching current practice — mark deploy.yml as not-the-prod-path in its header and rewrite KAUFMAN_GO_LIVE 1.5/1.6/5.1 + AUDIT_HANDOFF §4 to document the real deploy procedure (manual migrations-before-code + Vercel).
- **Verify:** Verified verbatim against the source files. deploy.yml lines 26-29 carry a "DISABLED during development (2026-05-26): no prod Supabase / Vercel project yet" header, and lines 30-31 set the trigger to `on: workflow_dispatch` (manual-only; does NOT fire on push). Yet docs/KAUFMAN_GO_LIVE.md:62 (Phase 1.5) asserts "`.github/workflows/deploy.yml` drives prod deploys now", line 151 (Phase 5.1) says pushing to main runs migrations then deploys, lines 76-79 add a "Verify deploy.yml end-to-end" step, and line 280 repeats it; docs/AUDIT_HANDOFF.md:108/111/125 (§4) repeat the same claim ("The workflow drives deploys now", push to verify). The contradiction is real and material. The "no deploy path at all" hazard is genuine: following Phase 1.5 / §4a literally (disable Vercel git auto-deploy because the workflow drives now) while deploy.yml is dispatch-only leaves nothing wired on push. Additional corroboration the reviewer did not cite but which strengthens it: docs/DEPLOYING.md:3-5 correctly states prod runs as a Docker container on Vultr (not Vercel) and that "The Vercel + .github/workflows/deploy.yml path is historical and disabled", with manual SQL-Editor migrations — so it is a genuine three-way doc disagreement, consistent with project memory (deploy_procedure: manual psql apply, push != auto-deploy) and with the repo's own RELEASE_REVIEW.md which flags the identical issue at lines 296/320/356/612. Severity kept at Low: as scoped this is a documentation-consistency defect (no code vulnerability); its real-world bite (operator disabling the only deploy trigger, or leaving Vercel auto-deploy ON and shipping code ahead of un-applied migrations) is the separately-tracked migration-gate hazard, not OPS-5 itself. Confidence high — every cited line was checked against the file contents.

#### `OPS-6` — README.md is unmodified create-next-app boilerplate — wrong run/deploy/edit instructions for this project
- **File:** README.md:1-37
- **Evidence:** README.md is the stock Next.js template: it tells a reader to edit `app/page.tsx` (this app uses src/app with route groups), to open localhost:3000, references the 'Geist' font (project uses Heebo + Frank Ruhl Libre per CLAUDE.md), and links the generic Vercel marketing deploy page. Nothing about Supabase setup, required env vars, the bilingual/RTL app, or the real deploy path.
- **Why:** The README is the first file any new operator/dev or a future agent opens. For a project at release-readiness with detailed runbooks elsewhere, a boilerplate README provides zero real onboarding and risks someone following its wrong instructions (localhost:3000, edit app/page.tsx) before finding docs/. Low blast radius but it's the front door.
- **Fix:** Replace README.md with a short real index: one-paragraph project description, prerequisites, copy .env.example -> .env.local + required keys, npm run dev, and links to docs/DEPLOYING.md (once corrected), RESTORE_RUNBOOK.md, INCIDENT_RESPONSE.md, BOOTSTRAP.md, KAUFMAN_GO_LIVE.md.
- **Verify:** Confirmed by reading README.md (lines 1-37): it is verbatim stock create-next-app boilerplate. Every specific claim checks out against the code: (1) Line 19 tells readers to edit `app/page.tsx`, but `ls app/` shows no root app dir — the project uses src/app with route groups ((app)/(auth)/(public)). (2) Line 21 references the 'Geist' font, but src/app/layout.tsx imports Frank_Ruhl_Libre + Heebo from next/font/google and `grep Geist src/` returns nothing. (3) Lines 17/32-36 give generic localhost:3000 + Vercel marketing/Next.js deploy links with zero project context. (4) No mention of Supabase, the documented required env vars, or the bilingual/RTL app. The suggested-fix is well-grounded: .env.example exists and documents real required keys (Supabase URL/anon/service-role, DATABASE_URL, INTEGRATION/BACKUP encryption keys), and real runbooks exist (DEPLOYING/RESTORE_RUNBOOK/INCIDENT_RESPONSE/BOOTSTRAP/KAUFMAN_GO_LIVE) — though they live under docs/ (the finding's suggested paths omit the docs/ prefix for four of them, a trivial nit in the suggestion, not the finding). Severity Low is accurate: documentation-only, no runtime/security/data impact, and correct runbooks already exist elsewhere — so real but genuinely low blast radius. Could not refute it.

#### `OPS-7` — .env.example strict-mode comment references the obsolete enc:v1: prefix
- **File:** .env.example:80-83
- **Evidence:** .env.example:82 describes BACKUP_ENCRYPTION_STRICT as: "the restore action refuses any backup file that doesn't carry the `enc:v1:` prefix." The current/preferred scheme is enc:v2: (per docs/RESTORE_RUNBOOK.md:88-92 and src/lib/crypto/secrets.ts encryptWithKeyV2 usage in src/app/api/cron/backup/route.ts:56-58). The comment's intent (strict = refuse plaintext) is correct, but the named prefix is stale.
- **Why:** Minor, but an operator reasoning about which backups strict mode will accept gets the wrong version token. Could cause confusion when validating a v2 file or deciding when it's safe to flip strict mode.
- **Fix:** Update the .env.example:82 comment to say strict mode refuses any file lacking an enc:v1:/enc:v2: encryption prefix (i.e. plaintext), matching secrets.ts and RESTORE_RUNBOOK.md §3.
- **Verify:** Confirmed. .env.example:80-81 says BACKUP_ENCRYPTION_STRICT=true makes "the restore action refuses any backup file that doesn't carry the `enc:v1:` prefix." The named prefix is stale: src/lib/crypto/secrets.ts:29-30 defines both PREFIX_V1 ('enc:v1:') and PREFIX_V2 ('enc:v2:'), and lines 17-20/45-46 explicitly call v2 the preferred scheme. The cron backup route (src/app/api/cron/backup/route.ts:56-58) emits enc:v2: whenever BACKUP_ENCRYPTION_SALT_V2 is set (v1 only as fallback), and docs/RESTORE_RUNBOOK.md:88-92 documents enc:v2: as "the current/preferred scheme." So a current encrypted backup commonly carries enc:v2:, not enc:v1:. Also, the comment slightly mischaracterizes strict mode: decryptWithKey (secrets.ts:130-134) and the restore action (restore-backup.ts:61-73) show strict only rejects files with NO enc: prefix (legacy plaintext) — both enc:v1: and enc:v2: pass strict (v2 routed at secrets.ts:118-122). The comment's intent (strict = refuse plaintext) is right but the named token (enc:v1:) understates by omitting v2. Tried to refute (is v1 the only intended prefix?) — no: v2 is the actively-emitted preferred scheme and strict demonstrably accepts it. This is documentation-only (a comment in .env.example), no runtime impact; Low is correct. Note the same wording slip also appears at restore-backup.ts:52 and docs/KAUFMAN_GO_LIVE.md:182. Suggested fix is accurate: say strict refuses any file lacking an enc:v1:/enc:v2: prefix (i.e. plaintext).

### Refuted (1)

- `CI-7` Branch protection is documented-but-manual; CI status checks do not block merges until toggled — The finding's central claim — that the branch-protection toggle "may never have been set," is "advisory only," and "there is no way to verify" — is refuted by the live GitHub state. `gh api repos/Shmuel18/first-crm/branches/main/protection` returns HTTP 200 with required_status_checks.contexts=["verify"] and checks=[{context:"verify",app_id:15368}]. So branch protection IS configured and the `verify` job (the typecheck/lint/test job in .github/workflows/ci.yml lines 13-27) IS a merge-blocking required status check. The "red CI can ship straight to prod" risk chain does not hold.

What IS accurate in the finding: the ci.yml header comment (lines 3-5) does describe the toggle as a manual one-time setup, and the repo files alone (rulesets API returns []) contain no committed proof — verification required the GitHub API, which I ran.

Residual partial validity (the only real bit): required contexts = ["verify"] ONLY; the `rls-tests` job in db-tests.yml is NOT a required check. The suggested fix's secondary half (also require rls-tests) targets a genuine gap. But db-tests.yml is path-filtered (runs only on PRs touching supabase/migrations/** or supabase/tests/**, lines 7-11), and a required-but-not-run check is a known GitHub footgun, so omitting it is partly defensible rather than a clear defect. Additionally the stated "Vercel auto-deploys to prod" chain is weak: deploy.yml is on: workflow_dispatch only (disabled 2026-05-26, line 26-31), and per project process the live deploy is driven manually.

Because the primary assertion (gate is advisory / toggle possibly unset / unverifiable) is false against the actual configuration, and the remaining concern is a minor optional-hardening nit rather than the described release-gate failure, the finding is not real as stated. Files read: .github/workflows/ci.yml, db-tests.yml, deploy.yml, vercel.json; GitHub API branch protection + rulesets.


---

# MASTER SUMMARY (rounds 16-20) + release readiness

**Confirmed:** 60 (3 High / 17 Medium / 40 Low). Per-round: R16 13 (0H/2M/11L) ·
R17 7 (0H/1M/6L) · R18 7 (0H/3M/4L) · R19 16 (3H/6M/7L) · R20 17 (0H/5M/12L).
DB rounds 17-19 were told NOT to re-report issues already fixed by migs 190-195
(this session). NO code changed during the scan.

## The 3 Highs (all R19) — really TWO themes
1. **Backup STILL incomplete (DB-BACKUP-1 + DRIFT-BACKUP-PAYOUTS-1):** my mig 193
   added 4 tables but MISSED 5 more durable ones — case_payouts (financial),
   case_properties, case_associated_advisors (access links), case_comments
   (team thread), checklist_templates. Silent data loss on restore. BLOCKED by
   DB-DRIFT-1: those tables aren't in database.ts, so the `satisfies keyof Tables`
   guard rejects them. Fix order: regenerate database.ts → add the 5 to
   BACKUP_TABLES + restore RPC (mig 196) → strengthen the parity test to assert
   whole-schema coverage.
2. **Stale root INTAKE_MIGRATIONS_151-153.sql (INTAKE-BUNDLE-1):** a root-level
   bundle that re-opens the anon door on submit_public_intake (regresses mig
   166/175). NOT in supabase/migrations (not auto-applied) but a live footgun if
   anyone runs it. Fix: delete/quarantine it.

## Genuine GAPS in this session's fix sweep (Medium) — the canonicalization missed two paths
- **AUTH-01 (R17):** case_borrowers UPDATE/DELETE RLS still legacy → role-change +
  remove-borrower fail for associated advisors. (Theme A missed it.)
- **RPC-1 (R18):** soft_delete_case RPC still legacy → recycle-bin delete fails for
  associated advisors. (Theme A missed it.)
- **SIM-ROUTE-1 (R16):** the mix read-only gate checks can_edit_case but NOT
  use_simulators → a case-editor lacking sim-write sees a failing editor. (SIM-PERSIST-2 gap.)
  => one small "mig 197" canonicalizes case_borrowers + soft_delete_case on can_edit_case;
     SIM-ROUTE-1 is a 1-line readOnly tweak.

## Other clusters (Medium/Low)
- **Least-privilege on SECURITY DEFINER fns:** has_permission/is_admin (AUTH-03),
  cleanup_rate_limit_counters (DEF-1), insert_overdue_notifications (MIG073) never
  REVOKE EXECUTE FROM PUBLIC. No bypass (they fail-closed) but off-standard.
- **set_primary_bank (RPC-2):** trusts client p_user_id for attribution + no search_path.
- **DB hygiene:** case_number MAX+1 race (DB-001), case_expenses.amount no >=0 CHECK
  (DB-002), FK indexes non-CONCURRENTLY (DB-003/DB-IDX-1), unbounded NUMERIC money
  (DB-004), audit trigger no search_path.
- **type drift (DB-DRIFT-1 + DRIFT-*):** ~9 tables + many RPCs missing from
  database.ts → `as unknown as` casts bypass type safety. Root cause of the backup gap.
- **SQL test gaps (SQLTEST-1..4):** no pgTAP for the authz fixes (190/192/195),
  backup parity (193), or mention scoping (194) — the security-critical guards are untested.
- **Simulator (R16):** missing i18n key simulators.report.generating (SIM-UI-1),
  fmtAgorot no NaN guard (SIM-PDF-1), autosave stale-success/dropped-final-edit,
  track-table >250 lines, tab a11y, money-rounds-on-keystroke.
- **Ops/docs (R20):** DEPLOYING.md says prod=Vultr (it's Vercel) — OPS-1; stale
  RESTORE_RUNBOOK (OPS-2); logger→Sentry not wired (OPS-3); GO_LIVE env table omits
  NEXT_SERVER_ACTIONS_ENCRYPTION_KEY (OPS-4); README is CNA boilerplate (OPS-6);
  shadcn in prod deps pulls hono advisory (CI-1); no next-build PR gate (CI-3);
  CSP still unsafe-inline (CI-8/OPS-CSP-1, the standing R1-shell-1 item).

## Release readiness: CONDITIONAL GO
- Day-to-day operation is healthy (both targets green, schema 195, all R9-15 Highs/Mediums fixed).
- CONDITIONS before relying on disaster-recovery / before broad go-live:
  1. Backup completeness (DB-BACKUP-1) + regenerate database.ts (DB-DRIFT-1) — restore data-loss.
  2. Delete the stale intake bundle (INTAKE-BUNDLE-1) — security-regression footgun.
  3. case_borrowers + soft_delete_case authz (AUTH-01 / RPC-1) — broken flows for associated advisors.
  4. Fix the misleading ops docs (OPS-1 DEPLOYING.md) before the next incident.
- Everything else is Low/quality and can follow.
